# AGENTS.md — nebula

Instructions for any AI coding agent (Claude, Codex, Cursor, etc.) working in this repo. Read `component-library-architecture.md` for the full rationale and inventory; this file is the condensed, load-bearing rule set — follow it even where the architecture doc is more discursive. Read `DESIGN_REFERENCE.md` before any visual/UX work (new component, restyle, layout) — it's the project owner's standing design charter (Minimals-dashboard-quality benchmark, Nebula's color scheme locked, every other token — spacing/radius/elevation/motion/etc. — open to refine as one cohesive system, never one-off per component).

## What this is

A composable React UI platform: design tokens → headless (unstyled, ARIA-complete) components → hooks → a Tailwind-powered styled design system. Ships as independently-versioned `@nebula/*` packages in a pnpm + Nx monorepo.

## Current state (keep this section accurate as packages land)

| Package | Status |
| --- | --- |
| `packages/utilities` | Done for now: `clamp`, `debounce`, `throttle`, `deepMerge`, `isHTMLElement`, `isFocusable`. No workspace deps — bottom of the graph. |
| `packages/hooks` | Done for now: `useControllableState`, `useClickOutside`, `useMediaQuery`, `useScrollLock`, `useFocusTrap`, `useResizeObserver`, `useIntersectionObserver`, `useMutationObserver`, `useLocalStorage`, `useDebounce`, `useId`, `useBoolean`, `useToggle`, `useStableCallback`, `useEventListener`, `usePrevious`, `useMounted`. Intentionally has no dependency on `@nebula/utilities` (see its README) or any other workspace package. |
| `packages/primitives` | Full low-level layer, zero in-workspace deps. **Composition:** `Primitive` (truly polymorphic — `as` + `asChild`, via `PolymorphicComponentPropsWithRef<E>`), `Slot`/`Slottable`. **Layout:** `Box`, `Flex`, `Grid`, `Stack`, `Inline`, `Center`, `Container`, `AspectRatio`. **Text:** `Text`, `Heading`, `Paragraph`, `Code`, `Pre`, `Link`. **Accessibility:** `VisuallyHidden`, `FocusScope`, `DismissibleLayer`, `Boundary`, `RovingFocusGroup`/`FocusItem`. **Form:** `Button`, `Input`, `Textarea`, `Label`, `Form`. **Visibility:** `Portal`, `Presence`, `Overlay`. **Positioning:** `Popper`/`PopperAnchor`/`PopperContent` (side/align/offset placement, collision-flip, viewport clamp; exports a pure `computePosition` function) — built from scratch rather than adding an external dependency, keeping this package's zero-workspace-deps rule intact; consumed by `headless`' `Popover`; also now publicly exports `usePopperContext`/`PopperContextValue` (previously internal to `popper-context.ts`) so a consumer can push a virtual anchor — an object duck-typing only `getBoundingClientRect()` — directly into `Popper`'s context without a real `PopperAnchor` element; added for `headless`'s `ContextMenu`, which positions at the pointer rather than off a real trigger element. Plus `cn`, `composeRefs`, `composeEventHandlers`, `createContextScope`. **Also now has:** `VStack` (thin alias of `Stack`, own `displayName`), `HStack` (new — `Flex` row, `align="center"`, no wrap, unlike `Inline`/`Wrap`), `Wrap` (thin alias of `Inline`), `Spacer` (new — `flex-grow` filler, `aria-hidden`), `Image` (new — unstyled polymorphic `img` wrapper; no load/error tracking, that's `react-ui`'s `Avatar`'s job). `Span`, `ScrollArea`, a dedicated `Arrow` on `Popper`, and an unstyled primitive-layer `Separator`/`FocusTrap` component are explicitly out of scope for now (per project owner direction) — not oversights. See `packages/primitives/README.md` for the full feature breakdown. `packages/react-ui`'s `Button` builds on `primitives`' `Button`, not raw `Primitive`. **Fixed a real pre-existing bug this session** (found while building `react-ui`'s `Text`/`Heading` wrappers): `text/index.ts`, `heading/index.ts`, and `types/index.ts` were each missing a type re-export their own component file already had (`TextOwnProps`, `HeadingOwnProps`, `PolymorphicComponent` respectively) — the component files themselves were always correct, only the package's own barrels hadn't kept up. Fixed both the `src/` barrels and the corresponding already-built `dist/text`/`dist/heading`/`dist/types` `index.d.ts` files directly (each subpath's per-file `.d.ts` already had the correct exports from its original real `tsc` build, so only the three barrel files needed editing, not a full regen). |
| `packages/headless` | **Renamed twice.** Task #17 (earlier) renamed `@nebula/headless` → `@nebula/styleless`. This session renamed it back, `@nebula/styleless` → `@nebula/headless` — not reverting, but adopting the project owner's confirmed 5-tier model where `headless` (behavior/ARIA only) and `styleless` (unstyled-but-structurally-complete `Button`/`Input`/`Card`/etc., a genuinely new, not-yet-built package — see `LAYER_TAXONOMY.md`) are two distinct layers, not one. Same package contents as before, only the name and its place in the chain changed. `Tabs`/`TabList`/`Tab`/`TabPanel` (full WAI-ARIA Tabs pattern — roving tabindex, automatic/manual activation, scoped context), `Checkbox` (tri-state, hidden-native-input form participation), `Switch`, `RadioGroup`/`RadioGroupItem` (reuses `primitives`' `RovingFocusGroup`/`FocusItem` instead of hand-rolling roving-tabindex again), `Accordion`/`AccordionItem`/`AccordionHeader`/`AccordionTrigger`/`AccordionContent` (`type="single"`\|`"multiple"`, arrow-key nav via `RovingFocusGroup`/`FocusItem`, panel wrapped in `primitives`' `Presence` for animatable expand/collapse), `Dialog`/`DialogTrigger`/`DialogPortal`/`DialogOverlay`/`DialogContent`/`DialogTitle`/`DialogDescription`/`DialogClose` (WAI-ARIA Dialog/Modal — `DialogContent` chains `Presence` + `DismissibleLayer` + `FocusScope` via `asChild` into one real DOM node), `Popover`/`PopoverTrigger`/`PopoverPortal`/`PopoverContent`/`PopoverClose` (non-modal Dialog sibling — anchor-positioned via `primitives`' `Popper`, composed into `Popover`'s own scope so one `__scopePopover` threads through both; `FocusScope` `trapped={false}`; trigger toggles instead of only-opens), `Tooltip`/`TooltipTrigger`/`TooltipPortal`/`TooltipContent` (hover/focus-driven, not click — `TooltipTrigger` starts a `delayDuration`-ms timer (default 700) on pointer-enter/focus via `context.onTriggerEnter`, closes immediately on pointer-leave/blur; a click suppresses the tooltip reopening from the focus event the click also produces. `TooltipContent` is deliberately NOT wrapped in `DismissibleLayer`/`FocusScope` like `Popover`/`Dialog` are — a tooltip never takes focus and has no outside-click surface — so Escape-to-close is wired directly via a `document.addEventListener('keydown', ...)` effect instead. No `TooltipProvider`-level shared "skip delay" group across sibling tooltips (Radix has this optimization; scoped out here as a documented simplification — each `Tooltip` manages its own delay independently).), `Menu`/`MenuTrigger`/`MenuPortal`/`MenuContent`/`MenuItem`/`MenuCheckboxItem`/`MenuRadioGroup`/`MenuRadioItem`/`MenuSeparator`/`MenuLabel` (full WAI-ARIA menu-button pattern — `MenuContent` chains `Presence` + `DismissibleLayer` + `FocusScope` + `RovingFocusGroup` via `asChild`; deliberately no submenus and no typeahead, both documented simplifications; `Tab` closes the menu via a manual handler since `DismissibleLayer` only covers Escape/outside-click), `DropdownMenu*` (thin renamed re-export of every `Menu` part — a menu triggered by a visible button *is* what `Menu` was built around, so there's no second implementation), `ContextMenu*` (`ContextMenuContent`/`Item`/etc. are the same renamed re-exports; only `ContextMenuTrigger` is new — it opens on the native `contextmenu` event and positions the menu via a virtual point-anchor object, duck-typing just `getBoundingClientRect()`, pushed directly into `Popper`'s context via `primitives`' now-public `usePopperContext`), and `Menubar`/`MenubarMenu`/`MenubarTrigger`/`MenubarContent`/... (a horizontal row of independent `Menu`s sharing one "at most one open" `value`; `MenubarTrigger` is a real second implementation — click toggles, but once one menu in the bar is already open, focusing a sibling trigger via pointer-hover or Left/Right-arrow switches the open menu to it without a click; `Menubar`'s own scope is deliberately unscoped-from-`Menu`/`Popper`, since sibling `MenubarMenu`s never nest inside each other the way `createContextScope` is meant to guard against), `Progress`/`ProgressIndicator` (WAI-ARIA Progress Meter pattern — `role="progressbar"` with `aria-valuemin`/`valuemax`/`valuenow` and an optional `getValueLabel`-formatted `aria-valuetext`; `value={null}` is indeterminate, omitting `aria-valuenow` entirely per spec rather than treating `0` and "unknown" as the same state; `ProgressIndicator` is the plain, unstyled `data-state`/`data-value`/`data-max`-bearing fill element `react-ui` sizes via CSS), `Spinner` (`role="status"` + a visually-hidden label via `primitives`' `VisuallyHidden` — no visual spin animation at this layer), `Skeleton` (`aria-hidden` placeholder — no shimmer at this layer). The last three confirmed the audit's "almost pure CSS" read: `Spinner`/`Skeleton` needed no scoped context or compound sub-parts at all, unlike everything else in this package. Also built: `AlertDialog`/`AlertDialogTrigger`/`AlertDialogPortal`/`AlertDialogOverlay`/`AlertDialogContent`/`AlertDialogTitle`/`AlertDialogDescription`/`AlertDialogCancel`/`AlertDialogAction` (composes `Dialog`'s root/context directly — only the root, which forces `modal` unconditionally, and `AlertDialogContent`, which has no `onPointerDownOutside` prop at all and always blocks outside-click dismissal, are real second implementations; everything else is a thin renamed re-export of `Dialog`'s parts), `Toggle` (`aria-pressed`, no hidden native input — not a form field the way `Checkbox`/`Switch` are), `ToggleGroup`/`ToggleGroupItem` (`type="single"|"multiple"`, same discriminated-union root split `Accordion` uses; `role="group"` even in single mode, since this is a group of toggle buttons per the WAI-ARIA Button pattern, not the Radio Group pattern; `ToggleGroupItem` is built by wrapping `Toggle` itself, controlling its `pressed` prop from context rather than duplicating `Toggle`'s rendering logic), `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent` (the standalone single-panel primitive `accordion-context.ts`'s header comment flagged as a future migration target for `Accordion` — built as its own primitive here; `Accordion` itself hasn't been migrated onto it, a separate follow-up), and `Breadcrumb`/`BreadcrumbList`/`BreadcrumbItem`/`BreadcrumbLink`/`BreadcrumbPage`/`BreadcrumbSeparator` (purely structural — `nav`/`ol`/`li` markup + `aria-current="page"`, no state machine, same "almost pure CSS/markup" profile as `Progress`/`Spinner`/`Skeleton`; `BreadcrumbSeparator` is `role="presentation"`/`aria-hidden` since list order alone already conveys sequence to assistive tech). Also now has `Field`/`FieldLabel`/`FieldControl`/`FieldDescription`/`FieldError` (generates shared ids once and wires every sub-part's `htmlFor`/`id`/`aria-describedby`/`aria-invalid` off them; `FieldControl`'s `aria-describedby` is set unconditionally to both the description and error ids regardless of whether those sub-parts are actually rendered — a documented simplification, since an `aria-describedby` pointing at a nonexistent id is a no-op for assistive tech), `Listbox`/`ListboxOption` (WAI-ARIA Listbox pattern, `type="single"|"multiple"` split mirroring `ToggleGroup`; single-select is "selection follows focus" like a native `<select>`/`RadioGroup`, multi-select requires an explicit click/Enter/Space like `ToggleGroup`; typeahead queries `[role="option"]` elements directly under the root rather than a separate registration system), and `Select`/`SelectTrigger`/`SelectValue`/`SelectPortal`/`SelectContent`/`SelectItem` (WAI-ARIA collapsible-listbox pattern — `SelectContent` collapses `Listbox` onto `PopperContent` the same way `PopoverContent` collapses `FocusScope` onto it, so `SelectItem` is a thin `ListboxOption` wrapper that only adds label registration on top; `SelectValue` looks up the selected item's label from that registry rather than a consumer tracking it by hand; a visually-hidden native `<select>` mirrors the value for `<form>` participation when `name` is given, same hidden-input technique `Checkbox`/`Switch`/`RadioGroup` use). `NativeSelect` (a plain `<select>` wrapper, no popup behavior needed) was added to **`packages/primitives`** instead — same treatment `Input`/`Textarea` already get there, since it has no state machine beyond what the browser provides; `headless` intentionally has no separate `NativeSelect`/`Textarea`/`Form`/`Input`, mirroring how `Input` was already handled there. Also now has `CheckboxGroup`/`CheckboxGroupItem` (`role="group"`, not `radiogroup` — no roving tabindex, since native checkboxes in a group are each independently `Tab`-reachable, unlike radio buttons; `CheckboxGroupItem` wraps `Checkbox` itself the same way `ToggleGroupItem` wraps `Toggle`), `NumberInput`/`NumberInputField`/`NumberInputIncrement`/`NumberInputDecrement` (built around a real `<input type="number">` for native spinbutton semantics + keyboard up/down-arrow increment; the +/- stepper buttons are `tabIndex={-1}` since they're an additional mouse/touch affordance on top of already-equivalent keyboard behavior, not the only way to reach it), `Combobox`/`ComboboxInput`/`ComboboxPortal`/`ComboboxContent`/`ComboboxItem` (WAI-ARIA editable-combobox-with-list-autocomplete pattern — deliberately NOT built on `Listbox` the way `Select` is, since real DOM focus has to stay on `ComboboxInput` while `aria-activedescendant` tracks a virtual "highlighted" option instead; arrow-key highlight movement queries whichever `[role="option"]` elements the consumer's own filtering left mounted, same "query the DOM" simplification `Listbox`'s typeahead uses), and `Autocomplete*` (thin renamed re-export of every `Combobox` part — `Combobox` was already built around free-text entry that doesn't require matching an option, which is exactly what "Autocomplete" means, so there's no second implementation, same call `DropdownMenu`/`Menu` made). Also now has `Slider`/`SliderTrack`/`SliderRange`/`SliderThumb` (WAI-ARIA Slider / Slider Multi-Thumb pattern — `value` is always a `number[]`, one entry per thumb, so there's no separate `RangeSlider`: a two-value array plus two `SliderThumb`s already is one; dragging uses `setPointerCapture` directly on the track/thumb elements rather than document-level listeners, and `SliderTrack`'s own pointerdown jumps whichever thumb is nearest the clicked position before continuing the drag), `Rating`/`RatingItem` (`role="radiogroup"`/`"radio"`, structurally close to `RadioGroup` plus one addition — `hoveredValue` for a pointer-hover fill preview that doesn't commit until a real click/Enter/Space — which is why it's its own context rather than reusing `RadioGroup`'s directly), and `OTPInput`/`OTPInputSlot` (one real `<input maxLength={1}>` per digit — not a fake div-based slot — auto-advancing on type, Backspace-when-empty moving back, and a full clipboard paste or a fast multi-character `onChange` on any single slot both funneled through one `setValueRange` splice-and-refocus helper on the root). Also now has `ColorPicker`/`ColorPickerTrigger`/`ColorPickerPortal`/`ColorPickerContent`/`ColorPickerHexInput` (mints one ambient, unscoped `Popover` internally — the same reuse `MenubarMenu` makes of `Menu` — for the swatch-trigger-opens-popup shell; there's no WAI-ARIA pattern for "color picker" to follow exactly, so this first pass deliberately covers the keyboard/screen-reader-accessible surface — a swatch trigger + a real validated hex `<input>` — rather than also shipping a 2D saturation/brightness drag square, which is mouse-only by nature and left as a `react-ui`-layer visual enhancement for later) and `FileUpload`/`FileUploadDropzone`/`FileUploadInput`/`FileUploadFileList`/`FileUploadFileItem`/`FileUploadRemoveTrigger` (`FileUploadDropzone` is a real `<label>` wrapping the real native `<input type="file">`, so click-to-open and full keyboard/screen-reader operability come from the browser's own label-activates-its-control behavior rather than a hand-rolled `role="button"`; drag-and-drop is an additional mouse/touch affordance layered on top, not a replacement for it). **Navigation batch, this session:** `Pagination`/`PaginationList`/`PaginationItem`/`PaginationLink`/`PaginationPrevious`/`PaginationNext`/`PaginationEllipsis` (a `<nav>` over a plain `<ul>` of page-number buttons, per the WAI-ARIA APG's own framing of pagination as "just a labeled navigation landmark," not a distinct pattern — `usePaginationRange` is a standalone, pure, exported helper computing the truncated `[1, 'ellipsis', 4, 5, 6, 'ellipsis', 10]`-shaped array so consumers never hand-roll that math themselves), `Stepper`/`StepperList`/`StepperItem`/`StepperTrigger`/`StepperIndicator`/`StepperTitle`/`StepperDescription`/`StepperSeparator` (also a `<nav>`+list; `StepperItem` derives `'complete'|'current'|'upcoming'` status from comparing its `index` against the root's `step`; `StepperTrigger` refuses to jump ahead of the current step unless the root's `allowSkipAhead` is set, and uses `aria-current="step"` — the enumerated token ARIA defines specifically for this, as opposed to `PaginationLink`'s `"page"`), `Tree`/`TreeItem`/`TreeItemToggle`/`TreeGroup` (WAI-ARIA Tree View pattern — deliberately NOT built on `@nebula/primitives`' `RovingFocusGroup` like `Listbox`/`Menu` are, since a tree's arrow-key model (ArrowRight/Left expand/collapse and move to child/parent, not just linear ArrowUp/Down) has no flat-list equivalent; `TreeGroup` fully **unmounts** (not just `hidden`s, unlike `AccordionContent`) while collapsed, which is what keeps a plain `[role="treeitem"]` DOM query returning exactly the currently-reachable items in visual order; `TreeItem` itself (not a nested button) is the focusable/clickable row, per the APG pattern; `type="single"|"multiple"` mirrors `Listbox`), and `NavigationMenu`/`NavigationMenuList`/`NavigationMenuItem`/`NavigationMenuTrigger`/`NavigationMenuPortal`/`NavigationMenuContent`/`NavigationMenuLink` (each `NavigationMenuItem` mints its own ordinary unscoped `Popover` for its trigger/content pair — the same "one shared primitive instance per repeated part" trick `MenubarMenu` plays with `Menu` — so the root only tracks which item's `value` is open plus a shared hover-intent close timer both `NavigationMenuTrigger` and `NavigationMenuContent` arm/disarm; opening is hover-driven (`openDelay`/`closeDelay`) rather than click-only like `Popover`; deliberately simplified vs. Radix — no shared `Viewport` crossfade animation and no `NavigationMenuIndicator`, both pure visual polish a consumer can layer on top of the exposed `data-state`/`aria-expanded`). **Overlay batch, this session:** `Drawer`/`DrawerTrigger`/`DrawerPortal`/`DrawerOverlay`/`DrawerContent`/`DrawerTitle`/`DrawerDescription`/`DrawerClose` (a thin re-export of `Dialog`'s root/parts exactly like `AlertDialog` — a drawer/"sheet" is behaviorally identical to a modal dialog, the only difference is which edge the panel enters from, which is exactly what `DrawerContent`'s new `side` prop, exposed as `data-side`, hands to the consumer's own CSS; only `DrawerContent` is a real second implementation), `HoverCard`/`HoverCardTrigger`/`HoverCardPortal`/`HoverCardContent` (hover-driven like `Tooltip` — `openDelay` before opening — but unlike `Tooltip`, closing is *not* instant: `HoverCardContent` commonly holds real interactive content, so both `HoverCardTrigger` and `HoverCardContent` arm/disarm one shared `closeDelay` timer, the technique `NavigationMenuTrigger`/`NavigationMenuContent` share; `FocusScope`'s auto-focus is suppressed entirely via `onMountAutoFocus`/`onUnmountAutoFocus` — a hover-opened panel must never steal keyboard focus from wherever the user actually is, the same tradeoff Radix's own `HoverCard` makes), and `Toast`/`ToastPortal`/`ToastViewport`/`ToastTitle`/`ToastDescription`/`ToastAction`/`ToastClose` (`role="status"`/`aria-live="polite"`/`aria-atomic="true"` — a toast is fundamentally a live region, not a `Dialog` variant, so unlike every other Overlay component here it has no shared root/context coordinating "which one is open": each `Toast` independently owns its own `open` state and `duration` auto-dismiss timer, since several are normally visible simultaneously; the countdown pauses on pointer-enter and resumes with whatever time was left — not a fresh `duration` — on pointer-leave; `ToastAction` requires a separate `altText` prop since its visible text alone, e.g. "Undo", is ambiguous once removed from the toast's surrounding context). **Collections batch, this session:** `TreeView`/`TreeViewItem`/`TreeViewItemToggle`/`TreeViewGroup` (thin renamed re-export of every `Tree` part — the Collections-category "TreeView" and the Navigation-category "Tree" are the same WAI-ARIA Tree View widget under two different wishlist names, same call `Autocomplete` makes reusing `Combobox`) and `Command`/`CommandInput`/`CommandList`/`CommandItem`/`CommandGroup`/`CommandEmpty`/`CommandSeparator` (the WAI-ARIA combobox-with-list pattern like `Combobox`, but always "open" — `CommandList` is never hidden the way `ComboboxContent` is, so there's no open/close state at all, matching the command-palette shape where the whole thing is usually already wrapped in a `Dialog` the consumer supplies; selecting an item runs *that item's own* `onSelect` callback rather than filling an input, since Command's items are actions, not values). **Project owner decision this session: `DataTable`, `Carousel`, `Virtualizer`, `Draggable`, `Droppable`, `Sortable` will NOT get a separate `headless` layer** — unlike this package's other components, they don't have a clean "ARIA behavior vs. visual styling" split worth decoupling (a data table's complexity is sort/selection state over a native `<table>`, a virtualizer has no ARIA semantics of its own at all), so they'll be built directly in `packages/react-ui` instead (`Virtualizer` as a `useVirtualizer` hook in `packages/hooks`). This closes out the entire `headless` backlog — see `COMPONENT_COVERAGE_AUDIT.md`'s Collections/Drag & Drop sections for the full per-item reasoning. |
| `packages/react-ui` | **Absorbed the former `packages/theme`** — no other package ever needed tokens/theming (primitives/headless are deliberately unstyled), so keeping them split was a package boundary with one real consumer. Now owns: `Button` (`cva` variants `primary`/`secondary`/`danger` × `sm`/`md`/`lg`, `loading` state, radius from `--radius-button`, hover via `brightness` filter), `Input` (styled wrapper over `primitives`' `Input`), `Card`/`CardHeader`/`CardTitle`/`CardDescription`/`CardContent`/`CardFooter` (purely presentational, no matching `headless` compound needed; `CardHeader` takes a `bordered` prop — `true` by default, a dashed `border-b` reading `--card-border` to visually separate it from `CardContent` — and an optional `icon` node rendered beside the title/description stack, `aria-hidden` since the accessible name still comes from `CardTitle`'s visible text; skip `CardHeader` entirely and render `CardTitle` directly as `Card`'s child if you want a bare title with no border/icon layout at all), `Badge` (all 8 semantic color roles as variants — the component that shows off the full palette, unlike `Button`'s 3), `Avatar`/`AvatarImage`/`AvatarFallback` (image load/error tracked via local state + context, `delayMs` to avoid flashing the fallback), `Separator` (`decorative` default `role="none"`, `false` gives `role="separator"` + `aria-orientation`), and now-styled wrappers over `headless`'s `Accordion` (chevron rotates off `data-state`, no icon dependency — inline SVG), `Dialog` (built-in icon close button, `hideCloseButton` to omit), `Popover`, `Tooltip` (reads `--tooltip-*`, deliberately the `neutral`/`neutral-content` semantic pair rather than `Popover`/`Dialog`'s `base-100`/`base-content` — see `component.ts`'s `tooltipTokens` comment), `Tabs`/`TabList`/`Tab`/`TabPanel` (underline treatment via `data-[state=active]` + `-mb-px` overlap onto `TabList`'s border), `Checkbox` (built-in checkmark/dash indicator icons, toggled via `group`/`group-data-[state=...]` off the button's own `data-state` — same technique `AccordionTrigger`'s chevron uses), `Switch` (track + sliding thumb `<span>`, same `group-data-*` technique), `RadioGroup`/`RadioGroupItem` (indicator circle + dot rendered *before* the item's own `children`, since the underlying `headless` item's children are its accessible name/visible label, not a separate `<label>`), `Progress` (fill percentage computed once from the same `value`/`max` props the consumer passes in and applied as an inline `translateX` on the indicator — doesn't re-expose a separate `ProgressIndicator` part, same "no subcomponent needed" reasoning `Checkbox`'s built-in icons use; indeterminate state uses Tailwind's built-in `animate-pulse` rather than a custom sliding-bar `@keyframes`), `Spinner` (border-ring + Tailwind's built-in `animate-spin`, no custom keyframes), and `Skeleton` (`animate-pulse` fill, default `--radius-box`, override via `className` for other shapes, e.g. `rounded-full`) — all ARIA/keyboard behavior in every one of these comes unchanged from `headless`, this layer only adds classes. **Also owns:** the 3-layer token system (`tokens/primitive.ts`/`semantic.ts`/`component.ts` compiled by `tokens/generate.ts` into a checked-in `src/theme.css`, plus `tokens/contrast-audit.ts` — see `CONTRAST_AUDIT.md`) **and** `ThemeProvider`/`useTheme` (light/dark/system, persisted via `@nebula/hooks`). **Token system was rewritten wholesale to a DaisyUI-style OKLCH vocabulary** (`base-100/200/300`+`content`, `primary`/`secondary`/`accent`/`neutral`+`content`, `info`/`success`/`warning`/`error`+`content`, per-component `radius`/`size` roles, `border`/`depth`/`noise`, font stacks, native `color-scheme` property) — adopted from a theme file the project owner provided, replacing the old hex/Tailwind-gray scale entirely. **Every styled component reads colors through its own `<component>Tokens` entry in `component.ts`** (`var(--badge-primary-bg)`, never `var(--color-primary)` directly) — a consistent per-component indirection layer applied to every component in this package, not just `Button`, so overriding one component's look never risks also affecting another that happens to read the same semantic color; see `component.ts`'s header comment. `ThemeProvider` toggles both a `.dark` class and the original `data-theme` attribute on `<html>`; `theme.css`'s dark selector matches either. **`CONTRAST_AUDIT.md` currently shows 23/28 passing.** The two load-bearing failures introduced by the OKLCH port (`Button`'s default `primary` variant's filled-state text, and its focus ring) are fixed — `primaryContent`/`accentContent` were replaced with dark, low-chroma, same-hue shades (matching the pattern the theme already used for `success`/`warning`/`error`) rather than the source file's near-white values, and the focus ring now reads `--color-base-content` instead of `--color-primary`. The remaining 5 failures (inline `error`/`success`/`warning` text in light mode, `base.300` as a divider in both modes) aren't yet load-bearing — no shipped component renders those pairings today — and are documented as deferred design decisions in `CONTRAST_AUDIT.md` rather than fixed speculatively. `@nebula/headless` is now a `react-ui` dependency (added for the `Accordion`/`Dialog`/`Popover`/`Tooltip`/`Tabs`/`Checkbox`/`Switch`/`RadioGroup` wrappers) — a real `pnpm install` has since been run, so `node_modules/@nebula/headless` is a genuine workspace symlink now, not the earlier in-sandbox manual one. New component tokens (`tooltipTokens`, `tabsTokens`, `checkboxTokens`, `switchTokens`, `radioTokens`, and this session's `progressTokens`/`spinnerTokens`/`skeletonTokens`) only reference semantic colors already covered by `CONTRAST_AUDIT.md` (`primary`/`base-100`/`base-300`/`base-content`/`neutral`/`neutral-content`), so no new audit entries were needed — still 23/28 passing. `progressTokens`/`spinnerTokens`/`skeletonTokens` specifically reuse the same `base-300`/`primary` pairing `switchTokens`' track/thumb already does; none of the three are text-on-background pairings (a progress fill, a spinner ring, a skeleton placeholder are all non-text), so they wouldn't need a `CONTRAST_AUDIT.md` entry even if the pairing were new — same exemption `separatorTokens`' divider relies on. **This session: styled wrappers for every one of the 9 new `headless` Navigation/Overlay/Collections primitives** — `Pagination`/`PaginationList`/`PaginationItem`/`PaginationLink`/`PaginationPrevious`/`PaginationNext`/`PaginationEllipsis` (root is a chrome-free re-export like `DialogTrigger`; `PaginationLink`'s `data-[state=active]` fills with `--pagination-link-active-bg`/`-text`), `Stepper`/`StepperList`/`StepperItem`/`StepperTrigger`/`StepperIndicator`/`StepperTitle`/`StepperDescription`/`StepperSeparator` (`StepperIndicator` shares one `primary` fill between `current`/`complete` states — a completed step shouldn't look "less done" than the active one), `Tree`/`TreeItem`/`TreeItemToggle`/`TreeGroup` (`TreeItemToggle` rotates 90° off `data-state=expanded`; indentation is a flat `pl-4` per `TreeGroup` nesting level, not depth-computed), `NavigationMenu`/`NavigationMenuList`/`NavigationMenuItem`/`NavigationMenuTrigger`/`NavigationMenuPortal`/`NavigationMenuContent`/`NavigationMenuLink` (`NavigationMenuItem`/`Portal` are chrome-free re-exports; `NavigationMenuContent` reuses `Popover`/`Dialog`'s `base-100`/`base-300`/`base-content` surface triple), `Drawer`/`DrawerTrigger`/`DrawerPortal`/`DrawerOverlay`/`DrawerContent`/`DrawerTitle`/`DrawerDescription`/`DrawerClose` (only `DrawerContent` is a real second implementation — four `data-[side=*]` variants each with their own fixed edge + `data-[state=closed]` translate-out transform; everything else reuses `headless`'s own `Dialog`-reexport-as-`Drawer` re-exports, restyled against `drawerTokens` rather than `dialogTokens` so the two can be restyled independently even though they currently share the same semantic values), `HoverCard`/`HoverCardTrigger`/`HoverCardPortal`/`HoverCardContent` (root/trigger/portal are chrome-free re-exports, matching `Popover`/`Tooltip`'s trigger treatment), `Toast`/`ToastPortal`/`ToastViewport`/`ToastTitle`/`ToastDescription`/`ToastAction`/`ToastClose` (reads `--toast-*`, the same `neutral`/`neutral-content` "pop off the page" pairing `tooltipTokens` uses; `ToastViewport` is fixed bottom-right, `flex-col-reverse` so newest stacks on top), `TreeView`/`TreeViewItem`/`TreeViewItemToggle`/`TreeViewGroup` (thin renamed re-exports of this package's own `Tree` parts, mirroring `headless`'s `TreeView`-reexports-`Tree` relationship), and `Command`/`CommandInput`/`CommandList`/`CommandItem`/`CommandGroup`/`CommandEmpty`/`CommandSeparator` (`CommandInput` is a bottom-rule field, not a boxed `Input`, so it reads as one continuous surface with the palette card around it; `CommandItem`'s `data-[highlighted]` fill is virtual-highlight state, not real focus, per the headless source). All 8 new component-token entries (`stepperTokens`, `treeTokens`, `navigationMenuTokens`, `drawerTokens`, `hoverCardTokens`, `toastTokens`, `commandTokens`, plus `paginationTokens` from the previous session) only reference already-audited semantic colors (`primary`/`base-100`/`base-200`/`base-300`/`base-content`/`neutral`/`neutral-content`) — no new `CONTRAST_AUDIT.md` entries needed, still 23/28 passing. **Not yet regenerated:** `theme.css` needs a local `pnpm --filter @nebula/react-ui generate-tokens` run for these 8 new token objects (plus this session's later `carouselTokens`/`dataTableTokens`/`dndTokens`) to actually reach the checked-in CSS (blocked in-sandbox — see below). **Also this session: `Carousel`/`DataTable`/`Draggable`/`Droppable`/`Sortable`** — built directly in `react-ui` with no `@nebula/headless` layer underneath (the project owner decision from the headless backlog session), each with its own plain `React.createContext` (not `@nebula/primitives`' `createContextScope` — see `carousel-context.ts`'s header comment for why: nothing else in the workspace ever needs to compose another component into these scopes, unlike `Popover`'s scope-threading). `Carousel`/`CarouselContent`/`CarouselItem`/`CarouselPrevious`/`CarouselNext`/`CarouselIndicators` (WAI-ARIA Carousel pattern — `role="region"`/`aria-roledescription="carousel"`, each slide `role="group"`/`aria-roledescription="slide"`; slide count and each `CarouselItem`'s `index` are consumer-supplied props, same convention `StepperItem`'s `index` uses, rather than a `Tree`-style mount-time registration system; CSS-transform-driven, not scroll-snap-driven, so button navigation never fights a user's own scroll gesture; deliberately no built-in autoplay — a well-documented a11y anti-pattern unless pausable and default-off, squarely a consumer decision). `DataTable`/`DataTableHeader`/`DataTableBody`/`DataTableRow`/`DataTableHead`/`DataTableCell`/`DataTableCaption`/`DataTableSelectionCell`/`DataTableSelectAllCell` (a real `<table>` plus sort state — native `aria-sort` on `<th>`, toggled by a real `<button>` inside it — and row-selection state via two dedicated cells that reuse `react-ui`'s own `Checkbox`; sorting the actual row data is left to the consumer, same "consumer owns filtering" convention `Combobox`/`Command` use — this only tracks *which* column/direction and *which* row ids are selected). `Draggable`/`Droppable`/`Sortable`/`SortableItem` (built on the native HTML5 Drag and Drop API, namespaced via a custom `application/x-nebula-drag-id` `DataTransfer` MIME type so this doesn't collide with a page's own unrelated native drag-and-drop; `SortableItem` inlines `Draggable`+`Droppable`'s event wiring directly rather than composing them, since a sortable row is always both at once; **documented limitation, not an oversight**: the native HTML5 DnD API has no keyboard equivalent at all, so none of these fake keyboard drag support — a fully accessible reorder UI needs an additional non-drag affordance, e.g. move-up/move-down buttons, layered on top by the consumer). **Not yet added:** stories/tests for these five — unlike every other component in this package, they have zero `@nebula/headless` test coverage underneath them to lean on, so this is a real gap (not the usual "lighter tests, ARIA already covered elsewhere" situation) worth prioritizing before anything consumes them for real. New component tokens `carouselTokens`/`dataTableTokens`/`dndTokens` all reuse already-audited `primary`/`base-100`/`base-200`/`base-300`/`base-content` pairings — still 23/28 on `CONTRAST_AUDIT.md`. `dist/` for these 5 new subpaths hasn't been generated (nothing in-workspace consumes them yet, unlike the headless→react-ui case, so it wasn't blocking anything — generate it the same `tsc`-based way, described two rows below, the first time something needs to import one of these subpaths before a real `pnpm build` runs). **This session: styled wrappers for the Form-Select/Menu family** — `Select`/`SelectTrigger`/`SelectValue`/`SelectPortal`/`SelectContent`/`SelectItem` (root/value/portal are chrome-free re-exports; `SelectTrigger` styled like `Input` with a chevron rotating via `group-data-[state=open]:rotate-180`; `SelectItem`'s checkmark uses the same `group`/`group-data-[state=selected]:block` technique `MenuCheckboxItem`'s indicator uses), `Combobox`/`ComboboxInput`/`ComboboxPortal`/`ComboboxContent`/`ComboboxItem` (`ComboboxInput` styled identically to `Input`; `ComboboxItem`'s `data-[highlighted]:bg-...` is virtual-highlight state, not real focus, per the headless source), `Autocomplete*` (thin renamed re-exports of this package's own `Combobox` parts, mirroring `headless`'s `Autocomplete`-reexports-`Combobox` relationship), `Menu`/`MenuTrigger`/`MenuPortal`/`MenuContent`/`MenuItem`/`MenuCheckboxItem`/`MenuRadioGroup`/`MenuRadioItem`/`MenuSeparator`/`MenuLabel` (root/trigger/portal chrome-free; `MenuItem` styles on plain `focus:` — not `:focus-visible` — since a `MenuItem` genuinely moves real DOM focus for both keyboard nav and pointer-hover, unlike `Combobox`/`Command`'s virtual-highlight items), `DropdownMenu*` (thin renamed re-export of every `Menu` part, same as `headless`), `ContextMenu*` (same re-export pattern; `ContextMenuTrigger` is re-exported straight from `@nebula/headless` unstyled, since it wraps arbitrary consumer content with zero chrome of its own), and `Menubar`/`MenubarMenu`/`MenubarTrigger`/`MenubarPortal`/`MenubarContent`/`MenubarItem`/`MenubarCheckboxItem`/`MenubarRadioGroup`/`MenubarRadioItem`/`MenubarSeparator`/`MenubarLabel` (`Menubar`'s root is the one real second implementation in this whole batch — unlike this package's own chrome-free `Menu` root, `Menubar` renders its `RovingFocusGroup` directly as visible DOM, so it needed real flex-row/border/bg styling; `MenubarTrigger` also needed real chrome — bar buttons like "File"/"Edit" are plain labels directly in the bar, not an already-styled `Button` passed `asChild` the way `MenuTrigger` assumes — styled with hover + `data-state=open` fill; everything else is a thin renamed re-export of this package's own `Menu` parts). **Found and fixed two real upstream gaps in `@nebula/headless`'s `Menubar`** while building this: its root had no `className` prop at all (couldn't be laid out visually) and wasn't wrapped in `React.forwardRef` (couldn't attach a ref) — both fixed by threading `className`/`ref` through to the underlying `RovingFocusGroup`, which already supported both. Four new component-token entries added: `selectTokens`, `comboboxTokens`, `menuTokens`, `menubarTokens` — all reuse already-audited `base-100`/`base-200`/`base-300`/`base-content` pairings, still 23/28 on `CONTRAST_AUDIT.md`. `headless/dist/menubar` was regenerated via the `tsc`-based technique (described two rows below) to pick up the `className`/`ref` fix. **Also this session: `AlertDialog`, `Breadcrumb`, `ColorPicker`, `Rating`, `FileUpload`, `Slider`** — `AlertDialog`/`AlertDialogTrigger`/`AlertDialogPortal`/`AlertDialogOverlay`/`AlertDialogContent`/`AlertDialogTitle`/`AlertDialogDescription`/`AlertDialogCancel`/`AlertDialogAction` (root/trigger/portal/overlay/title/description are thin renamed re-exports of this package's own `Dialog` parts; `AlertDialogContent` reuses `DialogContent`'s exact card treatment minus the built-in icon close button, since an alert dialog is never dismissed by an incidental click; `AlertDialogCancel`/`AlertDialogAction` are both built on the same re-exported `DialogClose` contract but given different default looks — a plain bordered button vs. a filled `--button-danger-bg` one — matching the headless source's own note that the two are behaviorally identical, only their default styling should differ), `Breadcrumb`/`BreadcrumbList`/`BreadcrumbItem`/`BreadcrumbLink`/`BreadcrumbPage`/`BreadcrumbSeparator` (purely structural — flex layout on `BreadcrumbList`, hover color on `BreadcrumbLink`, `font-medium` on `BreadcrumbPage`; `BreadcrumbSeparator` defaults to a chevron icon when no `children` given, same convention `FileUploadRemoveTrigger`'s default "×" icon uses), `ColorPicker`/`ColorPickerTrigger`/`ColorPickerPortal`/`ColorPickerContent`/`ColorPickerHexInput` (root is chrome-free — mints its own ambient `Popover` internally, same as this package's own `Popover` root; `ColorPickerTrigger` is the one real second implementation, a swatch button whose fill *is* the current value — set inline by the headless source — so this only adds the border/rounding/focus-ring frame; `ColorPickerPortal`/`ColorPickerContent` are thin renamed re-exports of this package's own `PopoverPortal`/`PopoverContent`, mirroring the headless source's own reuse; `ColorPickerHexInput` needs the same `--input-*` classes `react-ui`'s own `Input` uses applied directly, since it's built on `@nebula/primitives`' bare `Input`, not this package's styled one), `Rating`/`RatingItem` (`RatingItem` renders a built-in filled/outline star by default, purely off the headless source's `data-state`, same "no icon dependency" approach `AccordionTrigger`'s chevron uses), `FileUpload`/`FileUploadDropzone`/`FileUploadInput`/`FileUploadFileList`/`FileUploadFileItem`/`FileUploadRemoveTrigger` (root/input are chrome-free re-exports; `FileUploadDropzone`'s `data-dragging-over` fills the border with `--file-upload-dropzone-active-border`; `FileUploadRemoveTrigger` defaults to a small "×" icon), and `Slider`/`SliderTrack`/`SliderRange`/`SliderThumb` (root is chrome-free; `SliderTrack`/`SliderRange`/`SliderThumb` all switch between horizontal/vertical layout off the headless source's `data-orientation`, and `SliderRange`/`SliderThumb` position themselves via the inline `--nebula-slider-range-start`/`-end`/`--nebula-slider-thumb-percent` custom properties the headless source sets, consumed here as Tailwind arbitrary values rather than this layer recomputing any positioning math itself; no separate `RangeSlider` wrapper, same "`value` is always an array" reasoning the headless source documents). Five new component-token entries added: `breadcrumbTokens`, `colorPickerTokens`, `ratingTokens`, `fileUploadTokens`, `sliderTokens` (`AlertDialog` needed none — it fully reuses existing `dialogTokens`/`buttonTokens.danger`) — all reuse already-audited `base-100`/`base-200`/`base-300`/`base-content`/`primary` pairings, still 23/28 on `CONTRAST_AUDIT.md`. **Also this session: `Field`, `TextArea`, `PasswordField`, `SearchField`, `NumberInput`, `CheckboxGroup`, `Toggle`/`ToggleGroup`, `Collapsible`, `OTPInput`** — `Field`/`FieldLabel`/`FieldControl`/`FieldDescription`/`FieldError` (`Field` itself only adds a `flex flex-col gap-1.5` layout; `FieldControl` is chrome-free — it wraps whichever control is passed `asChild`, which already carries its own styling; all the `id`/`aria-*` wiring is unchanged from the headless source), `TextArea` (wraps `@nebula/primitives`' unstyled `Textarea` the exact same way this package's own `Input` wraps `@nebula/primitives`' `Input` — no `@nebula/headless` layer for either), `PasswordField`/`SearchField` (both genuinely new `react-ui`-layer-only components built directly on this package's own `Input`, no `@nebula/headless` counterpart at all — `PasswordField` swaps the native `<input>`'s `type` between `"password"`/`"text"` via a local show/hide toggle rather than hiding text via CSS, so autofill/password managers keep working correctly either way; `SearchField` is a preset `type="search"` `Input` plus a leading search icon, since the platform's own clear-button/Escape-to-clear affordance already comes free with that native input type), `NumberInput`/`NumberInputField`/`NumberInputIncrement`/`NumberInputDecrement` (mirrors the headless source's own naming; field gets the same `--input-*` treatment `ColorPickerHexInput` applies directly for the same "built on bare `@nebula/primitives` `Input`" reason; increment/decrement default to a "+"/"-" icon when no `children` given, same convention `RatingItem`'s default star uses), `CheckboxGroup`/`CheckboxGroupItem` (`CheckboxGroupItem` gets the identical box+checkmark treatment this package's own `Checkbox` uses, since the headless source renders the same underlying `Checkbox` DOM structure per item), `Toggle`/`ToggleGroup`/`ToggleGroupItem` (`data-state="on"|"off"` fills the button when pressed; `ToggleGroup`'s `data-orientation` switches between a horizontal row and a vertical stack; `ToggleGroupItem` reuses `Toggle`'s exact same fill treatment, since it's a `Toggle` underneath per the headless source), `Collapsible`/`CollapsibleTrigger`/`CollapsibleContent` (root is chrome-free; no animation built into `CollapsibleContent` — a consumer adds `data-[state=open]:animate-in`/`data-[state=closed]:animate-out` classes or a plain CSS transition themselves, per the headless source's own doc comment), and `OTPInput`/`OTPInputSlot` (`OTPInput` is the one wrapper this whole session that adds a genuinely new DOM element — a `flex gap-2` layout `<div>` — rather than passing `className` through to an existing headless element, since the headless root renders no DOM of its own at all; `OTPInputSlot` gets the same "built on bare `Input`" `--input-*` treatment as `NumberInputField`/`ColorPickerHexInput`, sized as one square digit box). Four new component-token entries added: `fieldTokens`, `toggleTokens`, `toggleGroupTokens`, `collapsibleTokens` (`PasswordField`/`SearchField`/`NumberInput`/`OTPInput` needed none — they all reuse existing `inputTokens` directly; `CheckboxGroupItem` reuses existing `checkboxTokens`) — all reuse already-audited semantic colors, still 23/28 on `CONTRAST_AUDIT.md`. **Also this session: the Layout family — `Header`, `Footer`, `Sidebar`, `Navbar`, `Main`, `Section`, `Surface`, `Paper`** — all purely presentational, no `@nebula/headless` compound underneath any of them, same "thin `cn()` wrapper directly around `Primitive`" treatment `Card`/`Badge`/`Separator` already use (single-file-per-component, unlike this session's interactive-family folders, since none of these have sub-parts beyond the one root element). `Header`/`Footer`/`Sidebar` all read the same `base-100`/`base-300`/`base-content` surface triple `Card`/`Popover`/`Dialog` use, kept as separate `headerTokens`/`footerTokens`/`sidebarTokens` entries rather than one shared token object so each stays independently themeable per `component.ts`'s own per-component-indirection rule; `Navbar` reads the same pair minus a border (usually nested inside a `Header` that already has one); `Sidebar`'s `side` prop only flips which edge gets the border, actual placement is left to the consumer's own flex/grid; `Main`/`Section` carry no color at all — deliberately the "neutral content region" `Header`/`Footer`/`Sidebar` frame, just `flex-1`/`space-y-4` layout; `Surface` is the plainest possible themed background (no border/shadow/radius, one step below `Paper`); `Paper` is visually identical to `Card` (same `paperTokens` surface triple) but without `Card`'s compound header/title/footer sub-parts — reach for `Paper` for a generic raised panel, `Card` for anything wanting that title/description/footer structure; `Paper`'s `elevation` prop (`0`–`3`) only swaps shadow depth, never border/radius/background. Six new component-token entries added: `headerTokens`, `footerTokens`, `sidebarTokens`, `navbarTokens`, `surfaceTokens`, `paperTokens` — all reuse already-audited `base-100`/`base-200`/`base-300`/`base-content` pairings, still 23/28 on `CONTRAST_AUDIT.md`. This closes out `component-library-architecture.md` §4's Layout row almost entirely (8/11 — only `AppShell`/`Divider`, both already covered under different names one layer up or over in this same package, and nothing else, remain unchecked). **Also this session: Typography (`Text`, `Heading`, `Code`, `Blockquote`, `Kbd`) and the Button family (`IconButton`, `ButtonGroup`, `SplitButton`, `FAB`) — both fully closed out (5/5 each)** — `Text`/`Heading` wrap `@nebula/primitives`' unstyled, polymorphic components of the same name (adding only this theme's default body color, plus `--font-heading` for `Heading`); `Code` overrides `@nebula/primitives`' `Code`'s hardcoded pre-token-system `bg-gray-100` with themed `--code-bg`/`-text` (see the `packages/primitives` row below for a real gap this surfaced); `Blockquote`/`Kbd` are purely presentational with no lower-layer counterpart at all, same treatment this session's `Header`/`Footer`/etc. use. `IconButton` reuses `Button`'s own `buttonVariants` recipe directly (only the size classes are overridden to be square) so it always matches `Button`'s theming exactly; `ButtonGroup`/`SplitButton` are both a purely-CSS child-combinator visual-merge trick with no shared state of their own (unlike `ToggleGroup`'s actual roving-tabindex state machine) — `SplitButton` is the same trick fixed to a horizontal pair, meant to be composed with a `DropdownMenu` trigger as its second segment; `FAB` reuses `--button-*` tokens directly (no separate `fabTokens`) since it's a specialized `Button` shape, not a differently-themed one, and deliberately doesn't apply `fixed` positioning itself (a page-layout concern, same rule `Sidebar`'s `side` prop follows). Four new component-token entries added: `textTokens`, `codeTokens`, `blockquoteTokens`, `kbdTokens` — all reuse already-audited semantic colors, still 23/28 on `CONTRAST_AUDIT.md`. **Also this session: `AvatarGroup`, `Chip`, `Tag`, `Stat`, `Video`, `Audio`** — `AvatarGroup` overlaps a row of `Avatar`s via negative margin plus a ring in the page background color (`--avatar-group-ring`), with `max` truncating `children` via a plain `React.Children` slice (no registration system) and rendering a `+N` `Avatar`-styled overflow indicator for the rest; `Chip` is the dismissible/interactive pill `Badge`'s own doc comment explicitly points at building as a separate component rather than growing an `onDismiss` prop onto `Badge` itself — only renders its built-in "×" button when `onDismiss` is actually passed, and uses its own `chipTokens` (`neutral`/`primary` only, unlike `Badge`/`Tag`'s full eight-role palette, since a chip only needs "default" vs. "actively applied"); `Tag` reuses `badgeTokens`' eight semantic colors directly (no separate token object) but outlined instead of filled — the one visual distinction from `Badge`, so a filled status pill and an outlined category label read as genuinely different affordances despite sharing a palette; `Stat`/`StatLabel`/`StatValue`/`StatDescription` is a single-file compound with no lower-layer counterpart, same "purely presentational structure" treatment `Card` documents; `Video`/`Audio` are themed `<video>`/`<audio>` wrappers adding only rounded-corner styling — native browser `controls` (shown by default) already give full keyboard/screen-reader operability, so there's nothing to decouple into a lower layer, same reasoning `SearchField`'s native `type="search"` affordances document. Three new component-token entries added: `avatarGroupTokens`, `chipTokens`, `statTokens` (`Tag`/`Video`/`Audio` needed none — `Tag` reuses `badgeTokens`, `Video`/`Audio` have no semantic color at all) — all reuse already-audited semantic colors or the non-text-pairing exemption `separatorTokens`/`avatarGroupTokens`'s ring already rely on, still 23/28 on `CONTRAST_AUDIT.md`. **Also this session: `List`, `DescriptionList`, `DataGrid`, `TreeTable` — closing out the Data row entirely (5/5)** — `List`/`ListItem` are purely presentational (`<ul>`/`<ol>` via an `ordered` prop + `<li>`), no matching `headless` compound, explicitly contrasted in its own doc comment with `Menu`/`Tree`'s real interactive behavior; `DescriptionList`/`DescriptionTerm`/`DescriptionDetails` is a single-file compound (`<dl>`/`<dt>`/`<dd>`) with a two-column grid layout so term/details pairs line up, same "no lower-layer counterpart" treatment `Stat`/`Card` document. `DataGrid` composes `@nebula/hooks`' `useVirtualizer` directly rather than this package's own `DataTable` — a native `<table>`'s row model actively resists the absolute positioning virtualized rows need — so it renders the WAI-ARIA Grid pattern (`role="grid"`/`"row"`/`"columnheader"`/`"gridcell"`) over plain `div`s instead, with a column-config API (`{key, header, render, width?}[]`) rather than JSX-composed rows, a sticky un-virtualized header, and virtual rows absolutely positioned via `transform: translateY()` inside a spacer sized to `totalSize`; sorting/filtering/selection are deliberately left to the consumer, same convention `DataTable`/`Combobox` use. `TreeTable` goes the opposite direction — it composes `DataTable`/`DataTableHeader`/`DataTableBody`/`DataTableRow`/`DataTableHead`/`DataTableCell` directly rather than reinventing table chrome (same "reuse a sibling's parts" convention `AlertDialog`/`Menubar` already established), recursively flattening a `{id, children?}` tree depth-first at render time (real HTML tables can't nest a `<tr>` inside a `<tr>`, so this is the only structurally valid way to render a tree as rows), with per-row indentation and a local `Set`-based expand/collapse toggle — mirroring `Tree`'s own local-state precedent in `@nebula/headless` rather than adding a controlled-prop shape nobody asked for. Three new component-token entries added: `listTokens`, `descriptionListTokens`, `dataGridTokens` (`TreeTable` needed none — it fully reuses existing `dataTableTokens`) — all reuse already-audited semantic colors, still 23/28 on `CONTRAST_AUDIT.md`. **Also this session: `Calendar`, `DatePicker`, `DateRangePicker`, `TimePicker`, `MultiSelect`, `Timeline`, `Sheet`** — `Calendar` is a from-scratch month-grid date picker core built directly in `react-ui` (no `@nebula/headless` layer, same project-owner call `Carousel`/`DataTable`/`DataGrid` make), following the WAI-ARIA Date Picker Dialog grid pattern (`role="grid"`/`"row"`/`"gridcell"` over real `<button>`s, `aria-selected` correctly placed on the `gridcell` not the button per the a11y-lint rule that caught this) with manual roving-tabindex arrow-key handling (`@nebula/primitives`' `RovingFocusGroup` is strictly 1-dimensional, a calendar grid needs both axes); all date math is plain native `Date` arithmetic in a dedicated `calendar-utils.ts` — no date library dependency, since there's no npm registry access in this sandbox to add one, but also because nothing here needs anything beyond what `Intl.DateTimeFormat` already gives free for month/weekday labels. `DatePicker`/`DateRangePicker` are both single, self-contained components (not compounds) — unlike `MultiSelect`'s consumer-composed items, a date picker's trigger and popup content are always the same shape, so there's nothing for a consumer to arrange; each mints its own ambient `Popover` plus a `Calendar` (`mode="single"`/`"range"`), closing the popover once a full selection is made — a UX decision `Calendar` itself never makes. `TimePicker` is an `Input` preset to `type="time"`, same "native input already gives it for free" reasoning `SearchField`/`PasswordField` document. `MultiSelect`/`MultiSelectTrigger`/`MultiSelectPortal`/`MultiSelectContent`/`MultiSelectItem` are also built directly in `react-ui`, composing this package's own `Popover` (positioning) with `@nebula/headless`'s `Listbox` `type="multiple"` (selection behavior) via a plain `React.createContext` (not `@nebula/primitives`' scoped context, same `Carousel`/`DataTable` reasoning) that tracks both the selected-value array and an item-label registry for `MultiSelectTrigger`'s summary text; `MultiSelectItem`'s checkbox-square indicator deliberately reuses `checkboxTokens`' box+checkmark treatment rather than `SelectItem`'s bare checkmark, since a multi-select option is toggled, not simply "the one current choice." `Timeline`/`TimelineItem`/`TimelineTitle`/`TimelineDescription` are purely presentational (an `<ol>`, since events are inherently ordered), no lower-layer counterpart. `Sheet` is a thin renamed re-export of every `Drawer` part, same `DropdownMenu`-reuses-`Menu` convention. Four new component-token entries added: `datePickerTokens` (shared by both `DatePicker`/`DateRangePicker`, since their triggers are visually identical), `calendarTokens`, `timelineTokens`, `multiSelectTokens` — all reuse already-audited semantic colors or the non-text-pairing exemption `separatorTokens`/`timelineTokens`' dot-and-line already rely on, still 23/28 on `CONTRAST_AUDIT.md`. **Also this session: `CodeBlock`, `Markdown` — and a deliberate decision not to build `QRCode`.** `CodeBlock` is a themed `<pre>`/`<code>` block with a header (language label + copy-to-clipboard button) and optional line numbers — **not a syntax highlighter**: real tokenization needs a highlighting engine (Shiki/Prism/highlight.js) as an actual dependency, unavailable in this sandbox, so this ships the honest useful subset (clean layout, copy button, line numbers) rather than a fragile hand-rolled tokenizer. `Markdown` is a deliberately-scoped subset renderer (`markdown-utils.ts`'s `parseBlocks`/`parseInline`, pure functions with no React dependency in the block-parsing half) covering headings/paragraphs/fenced-code/blockquotes/lists/inline-bold-italic-code-links — not full CommonMark, same "no parser dependency available, hand-rolling a spec-correct one isn't responsible" reasoning `CodeBlock` documents; renders through this package's own `Heading`/`Blockquote`/`List`/`CodeBlock` components rather than raw HTML tags. **Verified, not just typechecked:** since `vitest` can't run in this sandbox (see the Testing row below), `markdown-utils.ts`'s pure parsing functions were smoke-tested for real by compiling the file with a scratch `tsc` invocation and running the output through plain `node` against a representative sample (headings, a paragraph with all four inline styles, a multi-line blockquote, both list types, and a fenced code block) — block splitting and inline formatting both produced correct output; the scratch files were deleted afterward. **`QRCode` was deliberately not built** — a real scope cut, not an oversight: generating a scannable QR code needs the actual QR encoding algorithm (Reed-Solomon error correction, module placement, mask-pattern scoring), which is genuinely complex and not something to responsibly hand-roll without a reference implementation to check against, and there's no npm registry access in this sandbox to add `qrcode`/`qrcode.react`. A QR-code-*looking* image that doesn't actually decode would be actively worse than no component at all — silently failing whoever scans it — so this is left explicitly unbuilt until a real encoding library can be installed, flagged in `COMPONENT_COVERAGE_AUDIT.md` as the one deliberate scope cut in the whole document rather than just another unchecked box. One new component-token entry added: `codeBlockTokens` (`Markdown` needed none — it fully reuses `headingTokens`/`blockquoteTokens`/`listTokens`/`codeBlockTokens` via composition) — reuses already-audited semantic colors, still 23/28 on `CONTRAST_AUDIT.md`. Also added `BottomNav`/`BottomNavItem` — a mobile-only fixed tab bar (`<nav>` + `aria-current="page"` on the active item, mirroring `PaginationLink`'s convention), built react-ui-only with no `headless`/`styleless` layer since there's no distinct WAI-ARIA pattern to house there (same placement as `Card`/`Navbar`/`Header`/`Footer`), with its own `bottomNavTokens`. `Button`/`Badge`/`Tag`/`Chip`/`FAB`/`IconButton`/`Spinner`/`Progress`/`CircularProgress` were also redesigned onto a two-axis API — `variant` now means visual shape (`default`/`ghost`/`text`/`link` on `Button`/`FAB`/`IconButton`, `default`/`outline` on `Badge`, fixed to `outline` on `Tag`), `color` means the semantic hue (all eight roles: `primary`/`secondary`/`accent`/`neutral`/`info`/`success`/`warning`/`danger`); components with no shape axis (`Chip`/`Spinner`/`Progress`/`CircularProgress`) just use `color`. **Design-token refresh, Phase 1** (`DESIGN_REFERENCE.md` — Minimals-benchmark charter, color scheme locked, every other token fair game): added `motion`/`elevation`/`zIndex` groups to `tokens/primitive.ts` (wired into `tokens/generate.ts` the same way `radius`/`size`/`fontStack`/`effect` already are — `--motion-duration-fast/base/slow`, `--motion-ease-out`, `--elevation-anchored`/`-modal`, `--z-local/sticky/bottom-nav/overlay`). Closed a real gap: `DialogContent`/`AlertDialogContent`/`HoverCardContent` had **zero** open/close animation before this (unlike `PopoverContent`/`MenuContent`/`NavigationMenuContent`, already fade+scale); all three now share the same treatment. Formalized two elevation tiers (`anchored` for trigger-following panels, `modal` for surfaces that take over the interaction) across every overlay — corrected `NavigationMenuContent`, which had hardcoded `shadow-lg` despite being functionally an anchored panel like `Popover`/`Menu`. Formalized the z-index scale (`10`/`30`/`40`/`50`) that was already consistent by convention into real tokens, applied everywhere including `react-ui-blocks`' `SaasAppHeader` and `BottomNav`. Verified: full monorepo `tsc`/`eslint --max-warnings 0`/`build`/`vitest` (91 files, 545 tests) all clean, live-verified Dialog/AlertDialog/Popover's computed styles in Storybook (`--elevation-modal` vs `--elevation-anchored` genuinely distinct box-shadow values, correct durations). **Deliberately deferred to a later pass** (flagged, not forgotten): focus-ring `ring-offset`/color-target harmonization (~30-file sweep, a real but larger a11y-adjacent gap), an icon-size token scale, component-density/height-scale unification (needs per-component judgment — e.g. confirm whether `FAB`'s `h-10/14/16` is a deliberate Material-Design convention before changing it), typography tokenization (`Heading`'s scale already works, just isn't token-indirected), a documented spacing/density rationale. **`Sparkline` added** (dependency-free inline mini line/area chart, `data: number[]` → SVG `<polyline>`/`<polygon>`, `aria-hidden` by default, `preserveAspectRatio="none"` so it scales to its container — deliberately plain SVG, not `recharts`, so this package doesn't gain a charting dependency just for a decorative trend glyph; `recharts` stays scoped to `react-ui-blocks`' `ChartCard`). **Real WCAG contrast bug found and fixed** via a `react-ui-blocks` consumer (`DashboardOverview`'s new `trend` prop): `success`/`error` used as inline text on `base.100` fails in light mode (documented as a known, deliberately-unfixed gap in `CONTRAST_AUDIT.md` until something needed it) — added `successText`/`errorText` tokens (dark, low-chroma shade of the same hue in light mode, same bright fill reused in dark mode where it already passed), see `CONTRAST_AUDIT.md`. **Also fixed**: an orphaned `--card-spacing` custom property had been hand-patched directly into the *generated* `theme.css` at some point with no backing primitive, silently deleted by the next `generate.ts` run — moved into `primitive.ts`'s `size` group as `size.card` (`--size-card`), `card.tsx` updated to match, same value, no visual change. Everything else not yet built — remaining: `react-ui-blocks` (see `COMPONENT_COVERAGE_AUDIT.md`). |
| `packages/hooks` — `useVirtualizer` | Added this session (task: move `Virtualizer` here instead of a component in either `headless`/`react-ui`, since windowing math has no ARIA semantics of its own to decouple from anything — see the project owner decision in the `packages/react-ui` row above). Estimate-then-measure-and-correct strategy (same one TanStack Virtual/react-window use): `estimateSize(index)` seeds every item's size until `measureElement(index, element)` — wired to each rendered item's ref — reports its real rendered height via a per-item `ResizeObserver`, at which point that one entry in an internal `Map` size cache is corrected in place (no full re-scroll jump). Returns `{ virtualItems, totalSize, measureElement, scrollToIndex }` — a plain object, not TanStack's method-call (`getVirtualItems()`/`getTotalSize()`) API, matching every other hook in this package (`useResizeObserver` etc. also just return a plain value). `dist/use-virtualizer` was generated via the same `tsc`-based technique as the 9 new `headless` subpaths (see two rows below) since nothing else needed hand-authoring for it. |
| `packages/react-ui-blocks` | **Restructured from a flat `src/<kebab-name>/` layout into `BLOCKS_ARCHITECTURE.md`'s category/family taxonomy** (`src/<category>/<family>/<file>`, max depth 3) — neutral categories (`core`, `navigation`, `layouts`, `forms`) are re-exported from the root barrel `src/index.ts`; verticals (`marketing`, `ecommerce`, `dashboard`, `authentication`) are subpath-exports-only (`@nebula/react-ui-blocks/marketing`, etc.), never in the root barrel. 13 blocks total, all with Storybook stories + `vitest`/`vitest-axe` test coverage (this package previously had zero tests; `vitest`/`@testing-library/react`/`vitest-axe` devDependencies + `test`/`test:watch` scripts added, matching `react-ui`'s pattern — no other config changes needed, the root `vitest.config.ts` glob already covered it). **Migrated (unchanged behavior, relocated only):** `ThemeSwitcher` (`core/theme-switcher`), `AppLayout`/`AuthLayout`/`DashboardLayout`/`SettingsLayout` (`layouts/`), `TransactionForm` (`forms/transaction-form`), `LoginForm` (`authentication/sign-in`). **New this session:** `Hero` (`marketing/hero` — headline/subheadline/CTA(s), optional two-column `media` slot), `ProductCard` (`ecommerce/product-discovery` — image+name+price+badge+CTA `Card`), `SaasAppHeader` (`navigation/headers` — brand+nav+`DropdownMenu` user avatar menu; named to avoid the ambiguous `AppHeader` `BLOCKS_ARCHITECTURE.md` §7 explicitly flags), `SignupForm` (`authentication/sign-up` — mirrors `LoginForm`'s no-own-validation convention, including no password-match check), `DashboardOverview` (`dashboard/widgets` — responsive `Stat`/`Card` metrics grid, deliberately no charting), `PageSection` (`layouts/page-section` — lightweight title/description/actions content wrapper, distinct from the full-page shells, `aria-labelledby`-wired to its heading). One real a11y bug the new axe tests caught: `DashboardLayout`/`SettingsLayout` both nested `react-ui`'s `Main` (its own `<main>` landmark) inside `AppLayout`'s already-rendered `<main>` — a duplicate/non-top-level main landmark WCAG violation, distinct from the already-documented `Sidebar`-nested-in-`main` trade-off; both now use a plain `<div>` with `Main`'s own `flex-1 p-4` classes for that content region. `DOC.md`'s Deliverables 7-13 (naming/metadata/Storybook-org/docs-standards/benchmark/differentiators/roadmap) were deliberately skipped — `BLOCKS_ARCHITECTURE.md` §7-§13 already covers all of it. **`SaasAppHeader` redesigned** in the same session after user feedback flagged it as too thin — added a `logo` slot, real dropdown sub-menus via `react-ui`'s `NavigationMenu` (previously unused anywhere), a notification bell (`Popover` panel, unread pulse dot), a mobile `Sheet` slide-over nav, and a richer avatar+name+role profile widget; also wired fade/scale open-close transitions into `PopoverContent`/`MenuContent`/`NavigationMenuContent` (all three already composed `Presence` but had zero animation before). **`FilterBar`** (`forms/filter-bar` — dismissible `Chip` row + "Add filter" `DropdownMenu` + "Clear all", domain-neutral) and **`NotificationCenter`** (new `communication` vertical, `communication/notification-center` — standalone All/Unread `Tabs` inbox, the page-level counterpart to the header's compact bell panel) round out `AGENTS.md`'s prior "still to build" list; only `ChartCard` remains, intentionally deferred (no charting library exists anywhere in this repo's dependency graph — needs a deliberate library-choice decision first, not bundled into this pass). Also ran a bundle-size pass (`BLOCKS_ARCHITECTURE.md` §14): every block now imports `@nebula/react-ui` via per-component subpaths instead of its 215KB root barrel, which surfaced a real bug (fixed) — `react-ui`'s `tsup.config.ts` has `splitting: false`, so mixing root-barrel and subpath imports of the same Context-based Provider/hook pair (`ThemeProvider`/`useTheme`) resolves to two separately-bundled, non-identical context instances and breaks at runtime despite a clean typecheck; a follow-up task is queued to fix this structurally. Verified: `tsc`/`eslint`/`vitest` (88 tests, 15 files) all clean, live-verified in Storybook including the notification bell/menu/mobile-sheet interactions. **`ChartCard` built** (new `dashboard/charts` family) — the deferred item is done: added `recharts` (`^3.9.2`, the one non-`@nebula/*` runtime dependency this package has, `external` in `tsup.config.ts` like `react`/`react-dom`) and built a bar/donut chart card, discriminated on `type`, themed entirely off Nebula's `--color-*` tokens (never a hardcoded hex) including a themed custom tooltip. Also added `WelcomeBanner` (`dashboard/widgets` — a `--color-primary`-tinted greeting card) and a `Blocks/Compositions/SaaS Dashboard Home` assembled-page story (per `BLOCKS_ARCHITECTURE.md` §9's own documented pattern — no new component, just `WelcomeBanner`+`DashboardOverview`+`ChartCard` composed on one page) — both prompted by a request to match `minimals.cc/dashboard`'s general layout using this repo's own color scheme, not its literal palette. Verified: full monorepo `tsc`/`eslint --max-warnings 0`/`vitest` (91 files, 545 tests) and `pnpm -r build` all clean, live-verified in Storybook (working tooltips, correct per-series/per-segment token colors, centered donut total). Nothing left on the original "still to build" list. **This session: live-audited every page of `minimals.cc/dashboard`** (`MINIMALS_COMPONENT_INVENTORY.md` — 24-section page-by-page inventory, cross-referenced against `BLOCKS_ARCHITECTURE.md`'s taxonomy, an 8-phase build plan; Kanban/Calendar/Mail 3-pane explicitly flagged out-of-scope as mini-apps, not blocks). Following the project owner's atomic-design split (`react-ui` = atoms+molecules, `react-ui-blocks` = organisms, no duplicate components — extend via variant props), Phase 1 is done: **`DashboardOverview` extended, not duplicated** — new optional `icon`/`trend`/`sparkline` props per metric (`icon` renders as a tinted `aria-hidden` badge; `trend` replaces the plain description with an arrow + `--color-success-text`/`--color-error-text` value, the new `react-ui` tokens this surfaced; `sparkline` renders the new `react-ui` `Sparkline` atom inline, same color). **New `data-display` neutral category, first family `DataTableBlock`** (`data-display/data-table` — reachable only via the root barrel per this package's neutral-category convention, no category-level `index.ts`): a generic `DataTableBlock<TRow>` composing `Card`+`SegmentedControl` status tabs (with `Badge` counts)+`SearchField`+`react-ui`'s `DataTable` (sort/selection state passed straight through)+`Menu`-based row actions+`Pagination`+`EmptyState` — zero new state-management logic beyond page-size, everything else delegates to primitives that already had it. One naming gotcha worth flagging for future work in this family: `SegmentedControl` (built on `ToggleGroup`) renders `role="group"`/plain buttons, not `role="tab"` — don't pattern-match Minimals' own markup (which uses real ARIA tabs) when writing tests/stories against it. Verified: `tsc`/`eslint`/`vitest` all clean, live-verified in Storybook with 0 axe violations across all three `DataTableBlock` stories. **Phase 2 done: `ChartCard` extended with `line`/`area`/`radar`/`gauge` types**, added to the existing `bar`/`donut` discriminated union (not a new component) — `line`/`area`/`radar` all reuse the same `categoryKey`+`series` shape `bar` already had (`area` adds `stacked`), each series rendered through a `<linearGradient>` (area) or plain stroke/fill (line/radar) keyed off the same `ChartCardColor` → `--color-*` mapping every other chart type uses; `gauge` is a single scalar against a `max`, rendered as a semi-circle two-segment `Pie` (recharts has no dedicated gauge primitive — the standard half-donut technique) with the value centered underneath. **Real recharts 3.9.2 bug found and fixed**, caught by live Storybook verification (not just the test suite — `vitest-axe`/jsdom can't detect a visually-collapsed shape): `RadarChart`'s `Radar` polygon rendered collapsed to a single point at the chart's center on first mount — confirmed via raw SVG inspection (`recharts-radar-polygon`'s `d` attribute spanned ~1px instead of the full plot area) — and only self-corrected after an unrelated window resize forced a second layout pass, which timing-wise pointed at `Radar`'s mount-in animation getting stuck at frame 0 rather than a sizing issue (ruled out by testing `ResponsiveContainer`'s `initialDimension` prop first, which didn't help). Fixed with `isAnimationActive={false}` on `<Radar>` — confirmed by reloading the story fresh (no manual resize) both before and after. Every new chart type has its own `vitest-axe` "no violations" test alongside a rendering test, matching the existing `bar`/`donut` coverage pattern. Verified: full monorepo `tsc`/`eslint --max-warnings 0`/`vitest` all clean, and all six chart types live-verified in Storybook on a fresh page load (not just after a HMR update) with 0 accessibility violations each. Phases 3-8 of the inventory's build plan (billing widgets, ranked/activity lists, entity forms/detail pages, social/content pages, flagship compositions, chat window) remain unbuilt. |
| `apps/playground` | **Removed.** Used to be a Vite + React dev harness that smoke-tested every package in one page; deleted since it duplicated what Storybook already does per-component, and having two dev entry points was extra maintenance for no real benefit. Storybook is now the only dev harness — its Tailwind wiring (`@tailwindcss/vite` + `.storybook/tailwind.css`, importing `@nebula/react-ui/theme.css`) lives directly under `.storybook/` instead of depending on a demo app for it. If a real example app is wanted later (e.g. the `apps/budget-tracker` idea in `component-library-architecture.md`), add it fresh rather than resurrecting this. |
| Storybook | Configured at repo root (`.storybook/`), Vite builder, `addon-a11y` + `addon-essentials` + `addon-interactions`. Stories glob: `packages/*/src/**/*.stories.tsx`. Every component built so far has at least one story with a `play` interaction test. `viteFinal` in `.storybook/main.ts` registers `@tailwindcss/vite` directly (Storybook's Vite instance doesn't inherit any app's `vite.config.ts`). |
| Testing (Vitest/RTL/axe) | Wired: root `vitest.config.ts` (jsdom, single config for the whole workspace) + `vitest.setup.ts` (`@testing-library/jest-dom` + `vitest-axe` matchers). Tests exist for `primitives` (`Primitive`, `Button`, `Input`, `Label`, `FocusScope`, `DismissibleLayer`, `RovingFocusGroup`/`FocusItem`, `Presence`, `VisuallyHidden`, `Popper`'s `computePosition`), `headless` (`Tabs`, `Accordion`, `Dialog`, `Popover`), and `react-ui` (`Button`, `Input`, `Card`, `Badge`, `Avatar`, `Separator`, `Accordion`, `Dialog`, `Popover`) — each includes an axe a11y check where rendering a real DOM tree makes that meaningful. Other packages don't have a `test` script yet since they have no test files; add one (see `packages/primitives/package.json` for the pattern) the first time a package gets tests. Run `pnpm test` (whole workspace) or `pnpm --filter @nebula/primitives test` (scoped). **Unverified at runtime** — see Sandbox note in `CLAUDE.md`; run `pnpm install && pnpm test` locally before trusting this. Real `tsc`/`eslint` verification has been run against all 6 packages and both are fully clean (zero errors; one intentional `@typescript-eslint/no-explicit-any` warning in `create-context-scope.tsx`, documented inline). The `vitest-axe@0.1.0`/Vitest 2.x `toHaveNoViolations` typing gap (worked at runtime via `vitest.setup.ts` but invisible to `tsc`) is fixed via a small checked-in `src/vitest-axe-matchers.d.ts` in each of `primitives`/`headless`/`react-ui` — see that file's header for why (augments the global `jest.Matchers` interface that `@vitest/expect`'s `Assertion` already extends, rather than `vitest`/`@vitest/expect` directly). |
| Changesets / publishing | Not yet wired. |
| React version | **Upgraded to React 19** across the workspace: root + every package's `react`/`react-dom`/`@types/react`/`@types/react-dom` devDependencies bumped to `^19.2.0`, and every package's `peerDependencies` narrowed from `^18.0.0 \|\| ^19.0.0` to `^19.0.0` (React 18 is no longer supported by any `@nebula/*` package). **CJS build output was dropped in the same change** — every package's `tsup.config.ts` is now `format: ['esm']` (was `['esm', 'cjs']`), and every `require` condition was removed from each package's `package.json` `exports` map (only `types`/`import` remain per subpath); `main`/`module`/`type: "module"` were already ESM-pointing so nothing else needed to change. Two real React-19 type breaks were found and fixed by a real `tsc` run (not just inspection): `packages/hooks/src/use-previous/use-previous.ts` used the now-removed zero-arg `useRef<T>()` overload (now `useRef<T \| undefined>(undefined)`), and `packages/primitives/src/slot/slot.tsx`'s `SlotClone` `cloneElement` call needed an `any` cast on the merged props+ref object (React 19's `Attributes` type no longer includes `ref`) — documented inline, same rationale as the existing `Scope<C = any>` precedent in `create-context-scope.tsx`. All 6 packages verified clean via real `tsc -p ... --noEmit` (not hypothetical) using the actually-installed `react@19.2.7`/`@types/react@19.2.17` in `node_modules` — a real `pnpm install` had already been run (visible via `pnpm-lock.yaml` and `node_modules/react/package.json` both showing 19.2.7) by the time this upgrade landed, so this verification is against real installed types, not assumed ones. `packages/primitives/dist/dismissible-layer` (see the dismissable→dismissible rename below) was hand-patched via `mv`+`sed` in the same session rather than a real `tsup` rebuild, since `tsup`/`vitest` still can't run in this sandbox (`@rollup/rollup-linux-arm64-gnu` missing — the node_modules on disk is the user's real macOS install, not a linux-native one) — run `pnpm build` locally to get a real, non-hand-patched `dist`. |
| `dismissable` → `dismissible` rename | Fixed a real spelling bug: `DismissableLayer` (component, file, export, `DISMISSABLE_LAYER_NAME` constant, `dismissable-layer` folder/subpath-export) renamed to `DismissibleLayer`/`dismissible-layer` throughout `packages/primitives` and every consumer (`headless`'s `Dialog`/`Popover`/`Tooltip`, docs). `packages/primitives/package.json`'s `exports` map and `tsup.config.ts` entry key were updated to `./dismissible-layer`; the already-built `dist/dismissable-layer` folder was renamed and its compiled `.js`/`.cjs`/`.d.ts`/sourcemap contents were `sed`-patched to match (a real `tsup` rebuild will do this properly next time `pnpm build` runs, but this got the sandbox back to a verifiable, non-broken state without one). |
| Hand-authored `dist` output (`Menu`/`DropdownMenu`/`ContextMenu`/`Menubar`/`Progress`/`Spinner`/`Skeleton`, `primitives`' `popper` `usePopperContext` export) | Since `tsup` still can't run in this sandbox, three things were hand-written directly under `dist/` (mirroring tsup+`tsc --emitDeclarationOnly`'s real output shape: one bundled `index.js` per subpath entry, one per-source-file `.d.ts`, an `index.d.ts` barrel) rather than only existing as source: (1) `packages/primitives/dist/popper/index.js`/`index.d.ts` gained the `usePopperContext`/`PopperContextValue` re-export `ContextMenuTrigger` needs. (2) `packages/headless/dist/progress`, `dist/spinner`, `dist/skeleton` were written from scratch (brand-new subpaths, so unlike the `dismissible-layer` rename there was no prior compiled output to `sed`-patch) — needed because `packages/react-ui`'s `tsconfig.typecheck.json` clears `paths` and resolves `@nebula/headless/*` through `node_modules` → `package.json` `exports` → `dist`, same as every other cross-package import in this workspace, and `react-ui`'s new `Progress`/`Spinner`/`Skeleton` wrappers import those three subpaths directly. `Menu`/`DropdownMenu`/`ContextMenu`/`Menubar`'s `dist` was **not** hand-authored at the time this row was written — that changed once the `react-ui` Menu-family wrapper batch landed (see the `packages/react-ui` row above): those subpaths' compiled output was already present and correct from the original real `tsc` build, only `dist/menubar` needed regenerating (via the same `tsc`-based technique) to pick up the `className`/`ref` fix made to `headless`'s `Menubar` root in that session. All hand-authored files were written to match the real bundler's naming/structure conventions exactly (checked against already-real output like `dist/tooltip/`), but — like every other hand-patch this session — a real `pnpm build` should replace them the first time it's runnable. |
| Stray compiled `.d.ts`/`.d.ts.map` removed from `src/` | Found git-tracked, checked-in declaration output sitting directly inside `src/` component folders instead of only under `dist/` — leftover from some earlier out-of-band `tsc --declaration --emitDeclarationOnly` run made without `-p <tsconfig>` (which ignores the project's configured `outDir`/`rootDir` and emits next to the source instead). Scope was every folder in `packages/hooks/src/*` (plus the root `index.d.ts`/`index.d.ts.map`) and a specific subset of the *oldest* `packages/primitives/src/*` folders (`button`, `cn`, `compose-event-handlers`, `compose-refs`, `create-context-scope`, `primitive`, `roving-focus-group`, `slot`, `types`, `visually-hidden`) — 110 files total, `git rm`'d. Nothing newer (later `primitives` folders, all of `headless`/`react-ui`) had this problem, and none of the affected packages' actual `outDir: "./dist"`/`rootDir: "./src"` config is at fault — a correctly-invoked `pnpm build` would never reproduce it. Verified safe post-removal: each package's real `typecheck` script (`hooks`/`primitives` → bare `tsc -p tsconfig.json --noEmit`; `headless`/`react-ui` → `tsc -p tsconfig.typecheck.json --noEmit`, which clears workspace `paths` and resolves cross-package imports through `dist` same as a real build) all still pass clean, and a scratch-`outDir` dry run of `primitives`' `tsconfig.build.json --emitDeclarationOnly` regenerates the expected `.d.ts` set without touching `src/` at all. The one remaining `.d.ts` file under `packages/primitives/src/` (`vitest-axe-matchers.d.ts`) is the legitimate hand-authored ambient-matcher file, not generated output — left in place. `.gitignore` still doesn't have a rule that would catch a future recurrence of this (it only excludes literal `dist/`/`build/`/`storybook-static/`/`*.tsbuildinfo`), which is worth tightening as a follow-up. |

| `headless/dist` for the 9 new Navigation/Overlay/Collections subpaths (`pagination`, `stepper`, `tree`, `navigation-menu`, `drawer`, `hover-card`, `toast`, `tree-view`, `command`) | Generated via a **real `tsc` compile** rather than hand-authored — a better technique than the `dist/progress`/`dist/spinner`/`dist/skeleton` hand-writing described in the "Hand-authored `dist` output" row above. Discovery this session: unlike `tsup`/`vitest`/`tsx`/`esbuild`/`rollup` (all blocked in-sandbox by a `darwin-arm64`-vs-`linux-arm64` native-binary mismatch — `node_modules` is the user's real macOS install), plain `tsc` has **no native binary dependency** and compiles real, correct JS + `.d.ts` output in this sandbox. Ran a standalone tsconfig (extending `packages/headless/tsconfig.build.json`, `paths: {}`, absolute `include` globs scoped to the 9 new folders, `composite: false`/`incremental: false`, excluding `*.stories.tsx`/`*.test.tsx`) to a scratch `outDir`, then copied each folder's output into the real `packages/headless/dist/<component>/` (brand-new destination folders — no clash with already-real tsup-bundled output elsewhere in `dist/`). Output is genuinely correct (verified by reading compiled `.js`/`.d.ts` directly) but **per-file/unbundled** (e.g. `drawer/drawer.js` does `import { Dialog } from '../dialog/dialog'` as a real relative import) rather than tsup's one-bundled-`index.js`-per-entry shape — since real `dist/dialog`/`dist/popover` only had a bundled `index.js` (not per-file `.js`, though per-file `.d.ts` already existed from the separate `tsc --emitDeclarationOnly` build step), the missing per-file `.js` siblings (`dialog.js`, `dialog-context.js`, `popover.js`, `popover-context.js`, etc.) were additively copied in alongside the existing bundled `index.js`/`index.d.ts` (never overwritten) so `drawer`'s/`navigation-menu`'s real cross-folder relative imports resolve too. This fully unblocked `react-ui`'s typecheck (confirmed via a clean `tsc -p packages/react-ui/tsconfig.typecheck.json --noEmit`) for all 9 new styled wrappers. Like every other hand-patch this session, a real `pnpm build` locally will replace this with genuine tsup+tsc output — this scaffolding is temporary, not a shortcut anyone should build further on. |

| `packages/styleless` | **Just started — the new layer.** Per the project owner's confirmed 5-tier model and `LAYER_TAXONOMY.md` §4: unstyled but structurally/semantically complete components (`Button`, `Input`, `Card`, `Table`, `Avatar`, `Badge`, ...) that compose `primitives`/`headless` with a real, complete API but zero Tailwind/visual opinion — the layer `react-ui` should be styling *on top of*, instead of building these components' structure and styling in one step the way `react-ui` does today. Package scaffolded this session (`package.json`/`tsconfig*.json`/`tsup.config.ts` mirroring `packages/headless`'s shape; wired into `tsconfig.base.json` paths, root `tsconfig.json` references, and a new `eslint.config.mjs` layering block restricting it from importing `react-ui`/`react-ui-blocks`). Actions family (task #79) done: `Button` (`loading`/`aria-busy`/`data-loading`/forced-`disabled` moved down), `IconButton` (required-`aria-label` constraint on top of `styleless`'s own `Button`), `FAB` (same shape as `IconButton`, kept its own name), `SplitButton` (`role="group"` structural contract only, no visual-merge classes) — `react-ui`'s versions of all four now wrap their `styleless` counterpart and add only `cva`/Tailwind styling. `ToggleButton` resolved as **not a separate component** — `react-ui`'s `Toggle` already wraps `headless`'s `Toggle` directly, no unstyled middle step needed. `CloseButton` deliberately deferred (not yet needed by a third consumer, per its own taxonomy note). Inputs family (task #80) also done: `Input`/`Textarea` (thin pass-throughs of `primitives`, existing only so lower-tier presets and `react-ui` chain through `styleless` consistently), `PasswordInput` (the real `visible`/`type`-swap state machine, `renderToggle`/`toggleClassName` escape hatches for `react-ui`'s icon), `SearchInput` (bare `type="search"` preset), `EmailInput`/`UrlInput`/`TelInput` (net-new type presets) — `react-ui` gained matching `EmailField`/`UrlField`/`TelField` wrappers plus stories for `SearchField`/`PasswordField`/`TextArea` (previously missing). `MaskInput`/`CurrencyInput` deliberately deferred — need real formatting/cursor-position logic, not rushed. Selection (task #81) resolved without new `styleless` components: investigated `Select`/`MultiSelect` and confirmed `react-ui`'s existing wrappers add only classes/icons on top of `headless`'s already-complete compound components (no structural behavior worth decoupling, same call already made for `Toggle`) — documented as a resolved skip in `LAYER_TAXONOMY.md`, not silently passed over. `NativeSelect` reconfirmed correctly placed in `primitives`. Built `SegmentedControl` (new) directly in `react-ui` — a single-select `type="single"`-fixed visual variant of `headless`'s `ToggleGroup`, same "skip `styleless`" reasoning. File Upload + Form Infrastructure (task #82) done: built `ImagePreview` in `styleless` (the real behavior — `URL.createObjectURL`/`revokeObjectURL` lifecycle management for a selected `File`) and `ImageUpload` directly in `react-ui` (composes `headless`'s `FileUpload` + `styleless`'s `ImagePreview` in a themed thumbnail grid, owns its own controllable `files` state via `useControllableState` rather than reaching into `FileUpload`'s private scoped context). `Dropzone` and the whole Form Infrastructure family (`Field`/`FieldLabel`/`FieldDescription`/`FieldError`) resolved as skip-`styleless` — same "no structural behavior beyond `headless`" reasoning as `Select`/`MultiSelect`. `FileInput` deferred (likely a near-duplicate of `FileUpload`, confirm scope first). Data Display (task #83) done: built `Avatar`/`AvatarImage`/`AvatarFallback` in `styleless` (the real behavior — image load/error status tracking, `AvatarFallback`'s `delayMs` timer) and `AvatarGroup` (the `max`-truncation/overflow math, with a `renderOverflow` render-prop escape hatch — same pattern `PasswordInput`'s `renderToggle` uses — so `react-ui` can inject its own styled overflow badge); `react-ui`'s four Avatar-family components now wrap these directly, and the now-dead `react-ui/src/avatar/avatar-context.ts` was deleted. Built `CircularProgress` and `EmptyState` directly in `react-ui` (both "skip `styleless`" calls, same reasoning as `SegmentedControl`/`Card`/`Stat` — `CircularProgress` is only a different SVG geometry over `headless`'s existing `Progress`, `EmptyState` is a themed column with no behavior). `Badge`/`Chip`/`Tag` resolved skip-`styleless` (pure `cva`/Tailwind, no internal state); `Pill` resolved as **not needed** — `Chip` without `onDismiss` already covers it. `Card`/`Surface`/`Paper`/`Progress`/`Spinner`/`Skeleton`/`Timeline`/`Stat` reconfirmed already correctly placed (no behavior to decouple, `Progress`/`Spinner`/`Skeleton` already wrap `headless` directly for their ARIA roles). Added `emptyStateTokens` to `tokens/component.ts` (reuses already-audited `base-content`, still 23/28 on `CONTRAST_AUDIT.md`) and regenerated `theme.css` for real: `tsx`/esbuild can't run in this sandbox (native-binary mismatch, same as every other tsup/vitest blocker documented in this file), so `generate.ts` + its three token-file imports were copied to a scratch dir, `import.meta.url` swapped for a hardcoded absolute output path, and compiled to CommonJS with a direct `tsc` invocation (no native binary dependency, same technique this file's `headless/dist` rows use) — then run with plain `node`. Confirmed via `git diff --stat` that only the 3 new `--empty-state-*` lines were added, nothing else changed. ~25 more components remain across tasks #84-88, each tagged **[extract]**/**[new]** in `LAYER_TAXONOMY.md` §4 — same pattern each time: find the behavior/structure currently entangled with Tailwind classes in the `react-ui` version, split it down into an unstyled `styleless` version, make `react-ui`'s version a thin `cva`-only wrapper over it. Sandbox note: this package's `dist` was hand-built via a direct `tsc -p tsconfig.build.json` run (no `ERR_WORKER_OUT_OF_MEMORY`/esbuild issues hit for a package this small) — run a real `pnpm build` locally once registry access is available to confirm parity. |

Before claiming a component "already exists," check the actual `packages/*/src` tree — don't trust this table blindly if it looks stale, and update it in the same PR that adds a package.

## Non-negotiable architecture rules

1. **Layer dependency direction is one-way:** `utilities` → `hooks` → `primitives` → `headless` → `styleless` → `react-ui` → `react-ui-blocks`. `styleless` is a confirmed but not-yet-built package (see the status table above and `LAYER_TAXONOMY.md`); until it exists, `react-ui` continues building directly on `headless`/`primitives` for the components that will eventually extract into it. A lower layer must never import from a higher one. `eslint.config.mjs` enforces this per-package via `no-restricted-imports`; add a new block there for each new package rather than disabling the rule.
2. **Everything renders through `Primitive` and supports `asChild` via `Slot`** where DOM-tag flexibility makes sense. No component hardcodes its rendered tag.
3. **Uncontrolled by default, controllable when needed** — `value`/`defaultValue` + `onValueChange`, never a bare `onChange`-only API for stateful primitives.
4. **Every interactive primitive follows its WAI-ARIA APG pattern**: correct role, full keyboard support, visible `:focus-visible` ring, `aria-*` wiring, `data-state`/`data-disabled`/`data-orientation` attributes instead of JS class toggling.
5. **Style only via Tailwind utility classes + `cn()`** (`clsx` + `tailwind-merge`). Never hardcode colors — everything themable through the token layers in `packages/react-ui/src/tokens`.
6. **Nested instances of the same compound component must not collide.** Use `createContextScope` (see `packages/primitives/src/create-context-scope`) for any compound component's shared state, not a bare module-level `React.createContext`.

## File-per-component convention (non-negotiable)

One component (and its subparts) per folder — never a god-file:

```
packages/<pkg>/src/
  <component-name>/
    <component-name>.tsx
    <component-name>.types.ts       # if the prop types are non-trivial
    use-<component-name>.ts          # logic hook, kept out of the component file
    <component-name>.stories.tsx
    <component-name>.test.tsx        # once Vitest is wired
    <component-name>.a11y.test.tsx   # once Vitest is wired
    index.ts                          # re-export only, zero logic
  index.ts                            # package barrel: export * from './<component-name>'; ...
```

`index.ts` files are barrels only. Each package's `package.json` sets `"sideEffects": false` and an `exports` map with a subpath per component (see `packages/primitives/package.json` for the pattern) — bundlers tree-shake per component this way. `tsup.config.ts` in each package lists one entry per component/module, never a single bundled entry.

## Commands

```bash
pnpm install                 # from repo root — this sandbox has no npm registry access, run locally
pnpm dev                     # nx run-many -t dev, all packages in watch mode
pnpm build                   # nx run-many -t build
pnpm typecheck               # nx run-many -t typecheck
pnpm lint / pnpm lint:fix
pnpm format / pnpm format:check
pnpm test                    # vitest run, whole workspace (see root vitest.config.ts)
pnpm test:watch              # vitest, whole workspace, watch mode
pnpm storybook               # http://localhost:6006
pnpm --filter @nebula/primitives <script>   # scope any script to one package
```

## Layer placement: hooks vs primitives vs headless vs styleless vs react-ui

**Terminology note:** `@nebula/headless` (renamed from `@nebula/styleless`, which itself was renamed from the original `@nebula/headless` — see the `Current state` table's `packages/headless` row) is the layer that solves *behavior*: full ARIA/keyboard/focus/positioning, zero CSS opinion. `@nebula/styleless` is a **different, newly confirmed layer** sitting between `headless` and `react-ui` — a reusable UI component that composes `primitives`/`headless` into a complete, semantic API with *still* no visual opinion (e.g. an unstyled-but-structurally-real `Button`/`Input`/`Card`/`Table`). Don't conflate the two: `headless` answers "how does this behave," `styleless` answers "what's the reusable shape," `react-ui` answers "what does Nebula's design system make it look like." `component-library-architecture.md` predates all of this and still says `@nebula/headless` throughout — treat every mention there as today's `@nebula/headless`.

Project-owner-confirmed decision guide — ask these in order for any new (or reclassified) component:

```
1. Is it a low-level DOM abstraction?
   └─ Yes → Primitive

2. Does it primarily solve reusable interaction, state management,
   accessibility, keyboard navigation, focus management, or positioning?
   └─ Yes → Headless

3. Is it a reusable UI component that composes primitives and/or headless
   components without providing opinionated styling?
   └─ Yes → Styleless

4. Does it add the Nebula design system (Tailwind styles, variants, themes,
   animations, sizes, colors)?
   └─ Yes → React UI
```

Rules of thumb: **Primitive** = "abstracts HTML." **Headless** = "solves behavior." **Styleless** = "provides a reusable UI API." **React UI** = "provides Nebula's visual design." (`react-ui-blocks` sits above all four: it assembles multiple `react-ui` components into a full screen/module, e.g. `LoginForm`, `DashboardLayout`.)

Expanded per-question tests, with this codebase's own examples:

1. **Zero DOM/JSX of its own — just state + event wiring a consumer attaches to *any* markup shape?** → `@nebula/hooks`, not even a primitive. Test: "if every component in this repo were deleted, would this still make sense as a standalone utility a consumer calls and gets back plain values/callbacks?" Examples: `useControllableState`, `useFocusTrap`, `useDebounce`, `useVirtualizer`.
2. **Renders DOM, but is either (a) cross-cutting plumbing reused across many unrelated widgets, or (b) a single atomic element with no named WAI-ARIA widget pattern of its own?** → `@nebula/primitives`. Examples: `Slot`/`Portal`/`Presence`/`RovingFocusGroup`/`Popper`/`DismissibleLayer`/`VisuallyHidden`/`FocusScope` (shared infrastructure) and `Box`/`Text`/`Heading`/`Image`/`Button`/`Input`/`Textarea`/`Label`/`VStack`/`HStack`/`Wrap`/`Spacer` (atomic elements, no compound sub-parts, no keyboard pattern beyond the native tag).
3. **Implements a specific named WAI-ARIA authoring-practice pattern, usually via multiple coordinated sub-components (`Root`/`Trigger`/`Content`/`Item`) sharing context?** → `@nebula/headless`. Examples: `Accordion`, `Dialog`, `Popover`, `Menu`, `Select`, `Combobox`, `Tabs`, `Tree`, `Listbox`.
4. **A reusable, complete UI component built on `primitives`/`headless` but still carrying zero Tailwind/visual opinion?** → `@nebula/styleless` (not yet built — see the `Current state` table and `LAYER_TAXONOMY.md` §4 for the full ~50-component extraction list, each tagged **[extract]**). This is *not* the same test as step 3: `Button`/`Input`/`Card`/`Table` have a real, complete structural API worth reusing unstyled, but none of them is itself a named ARIA *widget* pattern the way `Dialog`/`Combobox` are — they either have no interactive state machine at all (`Card`), or their state machine already lives one layer down in `headless` (a styleless `Select` shell wraps `headless`'s `Select`/`Listbox` behavior, it doesn't reimplement it).
5. **Purely presentational structure with no interactive/keyboard behavior at all** (a page-layout region, a stat block)? → skip `headless` (nothing to decouple), but still consider `styleless` first if the shape is reusable across projects (`Card`, `Header`/`Footer`/`Sidebar`, `List`) before defaulting straight to `react-ui`.
6. **Judgment call: a genuinely new widget whose "behavior" is really just state + native semantics** (native `<table>` sort/select, native HTML5 drag-and-drop, CSS-transform slide logic) **rather than a bespoke ARIA pattern.** Built directly in `react-ui` today, explicitly as a documented project-owner decision each time, not a default — `Carousel`, `DataTable`, `DataGrid`, `Draggable`/`Droppable`/`Sortable`, `MultiSelect`, `Calendar`/`DatePicker`/`DateRangePicker`. `LAYER_TAXONOMY.md` §3 lists each with its one-paragraph justification; write the same kind if you hit a new case like this — don't silently default anywhere just because something similar landed in `react-ui` before.

Applying this test resolves every future placement question — five real buckets (hooks / primitives / headless / styleless / react-ui) cover the space; `LAYER_TAXONOMY.md` is this test already applied to the full ~200-item component inventory, so check there before re-deriving a placement from scratch.

## Adding a new component

Use the `new-component` skill (`.claude/skills/new-component/SKILL.md`) to scaffold the folder structure above with the correct boilerplate rather than hand-copying an existing component and forgetting to update its barrel/exports map.

## When in doubt

Prefer matching an existing pattern in `packages/primitives` over inventing a new one. If the architecture doc and the actual code disagree, the code plus this file win — flag the discrepancy rather than silently resolving it either way.
