# Nebula Component Inventory

One table per package — every component/primitive currently in the monorepo, what it does, and how it relates to the layer above/below it (per `component-library-architecture.md`'s layering: `primitives` → `headless` → `styleless` → `react-ui` → `react-ui-blocks`).

Generated by walking each package's `src/` tree and each component's own doc comment — regenerate with `node scripts/generate-component-inventory.mjs` after adding/moving components rather than hand-editing this file, so it doesn't drift.

**Totals**: primitives 41 · headless 41 · styleless 18 · react-ui 135 · react-ui-blocks 27 — **262 components** across 5 packages.

---

## `@nebula-lab/primitives`

The foundation layer — unstyled, polymorphic, behavior-only building blocks (focus management, positioning, portals, ref/handler composition) plus a handful of genuinely standalone atoms (`Box`, `Button`, `Input`, `Text`, ...). Everything else in the monorepo is built on top of this layer.

| SN | Component | Extended By (styleless / headless / react-ui) | Usage | Notes |
|---|---|---|---|---|
| 1 | `AspectRatio` | react-ui/aspect-ratio | Constrains its content to a fixed width/height ratio regardless of the content's own intrinsic size — for video/image/chart containers where the layout shouldn't jump as content loads. |  |
| 2 | `Boundary` | — | An error boundary — documented here as an "accessibility" primitive alongside `FocusScope`/`DismissibleLayer` because an uncaught render error otherwise leaves the page silently blank or half-rendered, which is as much a… | Internal building block, not a standalone rendered component. |
| 3 | `Box` | react-ui/box | The plainest possible polymorphic building block — a `Primitive` by another name. |  |
| 4 | `Button` | styleless, react-ui/button | Unstyled `button` — the one meaningful behavior difference from a bare `<button>` is defaulting `type="button"` instead of the native default (`"submit"`), which silently submits the nearest `<form>` and is almost never… |  |
| 5 | `Center` | react-ui/center | `Flex` with both axes centered (`align="center"`, `justify="center"`) — for centering a single child (a spinner, an empty-state illustration, a modal's content) both horizontally and vertically. |  |
| 6 | `cn` | — | Combines `clsx` (conditional class composition) with `tailwind-merge` (resolves conflicting Tailwind utility classes, last one wins) — the single class-name helper every `@nebula-lab/*` styled component should use instea… | Internal building block, not a standalone rendered component. |
| 7 | `Code` | react-ui/code | Inline code — monospace font in a subtle pill background. |  |
| 8 | `composeEventHandlers` | — | Merges a consumer-provided event handler with an internal one, so components can attach their own behavior without clobbering a user's `onClick`/`onKeyDown`/etc. | Internal building block, not a standalone rendered component. |
| 9 | `composeRefs` | — | Composes multiple refs (callback or object) into a single ref callback so a component can forward its own ref while also capturing one internally. | Internal building block, not a standalone rendered component. |
| 10 | `Container` | react-ui/container | Horizontally centers its content and caps its width at `size` — the standard "page content shouldn't stretch full-bleed on a wide monitor" wrapper. |  |
| 11 | `createContextScope` | — | `createContextScope` solves the "nested instances of the same compound component" problem — e.g. | Internal building block, not a standalone rendered component. |
| 12 | `DismissibleLayer` | — | Escape-key and outside-pointerdown dismissal for overlays, with correct layering: only the topmost mounted `DismissibleLayer` responds, so closing a nested popover doesn't also close the dialog underneath it. | Internal building block, not a standalone rendered component. |
| 13 | `Flex` | react-ui/flex | A `display: flex` container with small, literal-CSS convenience props (`direction`, `align`, `justify`, `wrap`, `gap`) instead of hand-written Tailwind flex utility classes — see `component-library-architecture.md` §6 ("… |  |
| 14 | `FocusScope` | — | Manages focus for a subtree: on mount, moves focus into itself (first focusable descendant, or the container itself if none); on unmount, restores focus to whatever was focused beforehand; while mounted, can optionally t… | Internal building block, not a standalone rendered component. |
| 15 | `Form` | react-ui/form | Unstyled `form`. |  |
| 16 | `Grid` | react-ui/grid | A `display: grid` container. |  |
| 17 | `Heading` | react-ui/heading | A heading with size/weight derived from `level` (1-6), independent of which tag actually renders — so document outline (`as`/tag) and visual hierarchy (`level`/size) can be set independently when they need to diverge, wh… |  |
| 18 | `HStack` | react-ui/hstack | `Flex` pinned to `direction="row"`, `align="center"`, and no wrapping — a horizontal row of children that stays on one line and overflows rather than wrapping. |  |
| 19 | `Image` | react-ui/image | An unstyled `img` — exists so `image` has the same polymorphic `as`/ `asChild` escape hatches (framework image components, `next/image`, a `<picture>` wrapper, ...) as every other Nebula element primitive, instead of con… |  |
| 20 | `Inline` | react-ui/inline | `Flex` pinned to `direction="row"` with `wrap` on by default and `align="center"` — a horizontal run of children (tag lists, button groups, breadcrumbs) that wraps onto multiple lines instead of overflowing when it runs… |  |
| 21 | `Input` | styleless, react-ui/input | Unstyled `input` — forwards a ref to the underlying DOM node (needed for imperative focus management, e.g. |  |
| 22 | `Label` | react-ui/label | Unstyled `label`. |  |
| 23 | `Link` | react-ui/link | An `a` with underline-on-hover + focus-visible ring by default, and an `external` convenience prop. |  |
| 24 | `NativeSelect` | react-ui/native-select | Unstyled native `<select>` — no popup/listbox behavior to add on top of what the browser already provides (unlike `@nebula-lab/headless`'s `Select`, which builds a fully custom popup listbox for cases that need one), so… |  |
| 25 | `Overlay` | — | A full-viewport `fixed inset-0` layer — the dimmed backdrop behind a modal `Dialog`. | Internal building block, not a standalone rendered component. |
| 26 | `Paragraph` | react-ui/paragraph | A `p` with comfortable reading line-height by default — for body copy, as distinct from `Text` (no line-height opinion, meant for inline runs). |  |
| 27 | `Popper` | — | Root of the Popper compound primitive — the shared anchor-positioning building block `Popover` and `Tooltip` (once built) are both meant to be built on, the same way Radix's `Popover`/`Tooltip`/`DropdownMenu` all share o… | Internal building block, not a standalone rendered component. |
| 28 | `Portal` | — | Renders its children into `container` (default `document.body`) instead of in place in the React tree — the backbone every overlay (`Dialog`/`Popover`/`Tooltip`/`Toast`, once built) is portaled through, so `overflow: hid… | Internal building block, not a standalone rendered component. |
| 29 | `Pre` | react-ui/pre | A multi-line code block container — monospace, padded, horizontally scrollable instead of wrapping/overflowing. |  |
| 30 | `Presence` | — | Keeps `children` mounted through an exit animation instead of unmounting the instant `present` becomes `false` — the difference between a `Dialog`'s content snapping out of existence vs. | Internal building block, not a standalone rendered component. |
| 31 | `Primitive` | — | Implementation note (applies to every polymorphic component in this package — `Box`, `Flex`, `Text`, etc. | Internal building block, not a standalone rendered component. |
| 32 | `RovingFocusGroup` | — | Implements the "roving tabindex" keyboard pattern shared by `Tabs`, `RadioGroup`, `Menu`, and toolbars: only one item in the group is ever a Tab stop (`tabIndex={0}`) at a time — everyone else is `tabIndex={-1}` — and ar… | Internal building block, not a standalone rendered component. |
| 33 | `Slot` | — | `Slot` merges the props (and ref) it receives onto its single child element instead of rendering a DOM node of its own. | Internal building block, not a standalone rendered component. |
| 34 | `Spacer` | react-ui/spacer | An invisible, `flex-grow`ing filler — drop it between two children of a `Flex`/`HStack`/`Inline` to push them to opposite ends without hand- writing `justify="between"` (useful when there are more than two children and o… |  |
| 35 | `Stack` | react-ui/stack | `Flex` pinned to `direction="column"` — a vertical stack of children with consistent spacing. |  |
| 36 | `Text` | react-ui/text | Generic inline/block text — no font-size or weight opinion by default (that's for `className`, or reach for `Heading` for headings). |  |
| 37 | `Textarea` | styleless, react-ui/textarea | Unstyled `textarea`, optionally auto-resizing to fit its content. |  |
| 38 | `Types` | — | The two props every Nebula polymorphic component accepts on top of its own props: `as` (swap the rendered tag/component) and `asChild` (merge props onto a single child via `Slot` instead of rendering an element of your o… | Internal building block, not a standalone rendered component. |
| 39 | `VisuallyHidden` | — | Hides content visually while keeping it in the accessibility tree — for text that a sighted user doesn't need (a redundant label, an icon button's accessible name) but a screen reader user does. | Internal building block, not a standalone rendered component. |
| 40 | `VStack` | react-ui/vstack | Alias of `Stack` under the name some consumers reach for first coming from other component libraries (Chakra UI's `VStack`, notably) — a thin pass-through wrapper rather than `const VStack = Stack` directly, so it gets i… |  |
| 41 | `Wrap` | react-ui/wrap | Alias of `Inline` under the name some consumers reach for first (Chakra UI's `Wrap`, notably) — `Inline` already *is* "a wrapping row of children," so this is a thin pass-through wrapper (own `displayName`, same behavior… |  |

## `@nebula-lab/headless`

Behavior + ARIA-correct state machines with zero styling — the "logic" half of interactive components (`Dialog`, `Select`, `Accordion`, ...). `react-ui` wraps almost all of these directly with Tailwind classes.

| SN | Component | Extended By (react-ui) | Usage | Notes |
|---|---|---|---|---|
| 1 | `Accordion` | react-ui/accordion | Root of the Accordion compound component. |  |
| 2 | `AlertDialog` | react-ui/alert-dialog | Root of the AlertDialog compound component — a thin wrapper around `Dialog`'s root (reusing its context/scope directly, see `alert-dialog-content.tsx`) that forces `modal={true}` unconditionally: an alert dialog that did… |  |
| 3 | `Autocomplete` | react-ui/autocomplete | Thin renamed re-export of {@link Combobox} — `Combobox` was already built around free-text entry that doesn't require matching any option (typing `inputValue` and the eventually-`onValueChange`d selection are two separat… |  |
| 4 | `Breadcrumb` | react-ui/breadcrumb | `<nav aria-label="breadcrumb">` — the WAI-ARIA breadcrumb pattern's landmark wrapper. |  |
| 5 | `Checkbox` | react-ui/checkbox | `role="checkbox"` — a `button`, not a native `<input type="checkbox">`, so `@nebula-lab/react-ui` can fully style the check mark instead of fighting browser-native checkbox rendering. |  |
| 6 | `CheckboxGroup` | react-ui/checkbox-group | `role="group"` over a set of {@link CheckboxGroupItem}s — unlike `RadioGroup`/`ToggleGroup`, there's no roving-tabindex here: native checkboxes in a group are each independently `Tab`-reachable (there's no "arrow keys mo… |  |
| 7 | `Collapsible` | react-ui/collapsible | Root of the Collapsible compound component — a single expand/collapse panel, independent of any group semantics (unlike `Accordion`, which is effectively several of these sharing an "at most one/many open" rule via its o… |  |
| 8 | `ColorPicker` | react-ui/color-picker | Root of the ColorPicker compound component — a swatch button (`ColorPickerTrigger`) that opens a `Popover` (reused directly, the same "mint one ambient instance" reuse `MenubarMenu` makes of `Menu`) containing whatever c… |  |
| 9 | `Combobox` | react-ui/combobox | Root of the Combobox compound component — the WAI-ARIA [editable combobox with list autocomplete pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-autocomplete-list/): a text `ComboboxInput` (`… |  |
| 10 | `Command` | react-ui/command | Root of the Command compound component — the WAI-ARIA [combobox-with-list pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/), but always "open": unlike `Combobox`, there's no popup to show/hide — `CommandInput`… |  |
| 11 | `ContextMenu` | react-ui/context-menu | `ContextMenu`'s open/close contract (controlled/uncontrolled `open`, `Popper` + scoped context underneath) is identical to `Menu`'s — the only real behavioral difference is how it's triggered (right-click at a point vs… |  |
| 12 | `Dialog` | react-ui/dialog | Root of the Dialog compound component — renders no DOM of its own (like `DialogTrigger` and `DialogPortal`'s children are meant to be siblings, not nested inside an extra wrapper element), just provides shared state via… |  |
| 13 | `Drawer` | react-ui/drawer | Root of the Drawer compound component — a thin re-export of `Dialog`'s root (reusing its context/scope directly, same technique `AlertDialog` uses): a drawer/"sheet" is behaviorally identical to a dialog (modal by defaul… |  |
| 14 | `DropdownMenu` | react-ui/dropdown-menu | `DropdownMenu` is `Menu` under the name most consumers reach for first — a menu triggered by clicking a visible button is the overwhelmingly common case `Menu` was designed around (see `menu.tsx`'s header comment), so th… |  |
| 15 | `Field` | react-ui/field | Root of the Field compound component — wires a control (`FieldControl`) to its `FieldLabel`/`FieldDescription`/`FieldError` via generated, shared ids, the same `label`-`for`/`aria-describedby`/`aria-invalid` plumbing eve… |  |
| 16 | `FileUpload` | react-ui/file-upload | Root of the FileUpload compound component — a drag-and-drop zone (`FileUploadDropzone`) wrapping a real native `<input type="file">` (`FileUploadInput`), so click-to-open and full keyboard/screen-reader operability come… |  |
| 17 | `HoverCard` | react-ui/hover-card | Root of the HoverCard compound component. |  |
| 18 | `Listbox` | — | Root of the Listbox compound component — `role="listbox"` over a set of {@link ListboxOption}s, the WAI-ARIA [Listbox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/). | Internal engine for Select/Combobox/Autocomplete — not meant to be styled standalone. |
| 19 | `Menu` | react-ui/menu | Root of the Menu compound component — the shared foundation `DropdownMenu`, `ContextMenu`, and `Menubar` are all built on (same relationship `Popper` has to `Popover`/`Tooltip`: one implementation, several triggers/conte… |  |
| 20 | `Menubar` | react-ui/menubar | `role="menubar"` — a horizontal row of `MenubarMenu`s (each its own `Menu`) that share one rule: at most one open at a time. |  |
| 21 | `NavigationMenu` | react-ui/navigation-menu | Root of the NavigationMenu compound component — a `<nav>` wrapping `NavigationMenuList`/`NavigationMenuItem`s. |  |
| 22 | `NumberInput` | react-ui/number-input | Root of the NumberInput compound component. |  |
| 23 | `OTPInput` | react-ui/otp-input | Root of the OTPInput compound component — a segmented one-time-passcode field, one real `<input maxLength={1}>` per {@link OTPInputSlot}. |  |
| 24 | `Pagination` | react-ui/pagination | Root of the Pagination compound component — a `<nav>` (per the WAI-ARIA Authoring Practices' guidance that pagination is just a labeled navigation landmark over a list of links, not a distinct ARIA pattern of its own) wr… |  |
| 25 | `Popover` | react-ui/popover | Root of the Popover compound component. |  |
| 26 | `Progress` | react-ui/progress | `role="progressbar"` — the WAI-ARIA Progress Meter pattern. |  |
| 27 | `RadioGroup` | react-ui/radio-group | `role="radiogroup"` — a single-select group of {@link RadioGroupItem}s. |  |
| 28 | `Rating` | react-ui/rating | `role="radiogroup"` over a set of {@link RatingItem}s (typically stars) — single-select, close to `RadioGroup`'s own shape, plus pointer-hover preview: moving the pointer over an item visually fills every item up to it (… |  |
| 29 | `Select` | react-ui/select | Root of the Select compound component — the WAI-ARIA [collapsible listbox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/examples/listbox-collapsible/): a button (`SelectTrigger`, `aria-haspopup="listbox"`) th… |  |
| 30 | `Skeleton` | react-ui/skeleton | A loading placeholder shape — `aria-hidden` by default, since a skeleton bone is a decorative stand-in for content that hasn't arrived yet, not something a screen reader user benefits from having announced individually (… |  |
| 31 | `Slider` | react-ui/slider | Root of the Slider compound component — the WAI-ARIA [Slider pattern](https://www.w3.org/WAI/ARIA/apg/patterns/slider/) (and, with two thumbs, its [Slider (Multi-Thumb)](https://www.w3.org/WAI/ARIA/apg/patterns/slider-mu… |  |
| 32 | `Spinner` | react-ui/spinner | `role="status"` — announces the loading state to assistive tech via a visually-hidden label, without requiring visible text. |  |
| 33 | `Stepper` | react-ui/stepper | Root of the Stepper compound component — a `<nav>` (a stepper is, at its core, navigation over an ordered sequence of steps, same reasoning as `Pagination`) wrapping `StepperList`/`StepperItem`/`StepperTrigger`. |  |
| 34 | `Switch` | react-ui/switch | `role="switch"` — like {@link Checkbox} (a `button` + a visually-hidden native `<input type="checkbox">` for form participation), but semantically an on/off toggle rather than a selectable option: screen readers announce… |  |
| 35 | `Tabs` | react-ui/tabs | Root of the Tabs compound component — provides shared state to `TabList`, `Tab`, and `TabPanel` via scoped context, following the WAI-ARIA Tabs Authoring Practice. |  |
| 36 | `Toast` | react-ui/toast | `role="status"` + `aria-live="polite"` + `aria-atomic="true"` — announces itself to assistive tech without stealing focus or interrupting whatever the user is doing (the WAI-ARIA "status message" pattern; a toast is fund… |  |
| 37 | `Toggle` | react-ui/toggle | A two-state toggle button — `aria-pressed`, not `Switch`'s `role="switch"`/`aria-checked`, per the WAI-ARIA [Button pattern's pressed state](https://www.w3.org/WAI/ARIA/apg/patterns/button/): a toggle button IS a button… |  |
| 38 | `ToggleGroup` | react-ui/toggle-group | Root of the ToggleGroup compound component — a set of {@link Toggle} buttons (`role="group"`, not `radiogroup`: this is a group of toggle buttons per the WAI-ARIA Button pattern, not the Radio Group pattern, even in `typ… |  |
| 39 | `Tooltip` | react-ui/tooltip | Root of the Tooltip compound component. |  |
| 40 | `Tree` | react-ui/tree | Root of the Tree compound component — `role="tree"` over a set of nested {@link TreeItem}s, the WAI-ARIA [Tree View pattern](https://www.w3.org/WAI/ARIA/apg/patterns/treeview/). |  |
| 41 | `TreeView` | react-ui/tree-view | Thin renamed re-export — see `tree-view/index.ts`. |  |

## `@nebula-lab/styleless`

A thinner, in-between layer: real behavior extracted out of specific `react-ui` components (so the logic isn't locked inside one styled implementation), still with no Tailwind classes of its own — every part takes a `classNames` prop that `react-ui`'s version supplies.

| SN | Component | Extended By (react-ui) | Usage | Notes |
|---|---|---|---|---|
| 1 | `Avatar` | react-ui/avatar | Root — tracks whether `AvatarImage` has loaded, errored, or is still loading, so `AvatarFallback` knows when to render. |  |
| 2 | `AvatarGroup` | react-ui/avatar-group | A row of `Avatar`s with overflow truncation — unstyled (no overlap/ring treatment; `@nebula-lab/react-ui`'s `AvatarGroup` adds that). |  |
| 3 | `Button` | react-ui/button | `styleless`-tier `Button` — the reusable, complete API a moment before any visual design gets applied: real `loading` semantics (`aria-busy`, `data-loading`, forced `disabled`) on top of `@nebula-lab/primitives`' bare `B… |  |
| 4 | `CodeBlock` | react-ui/code-block | `styleless`-tier `CodeBlock` — the real behavior extracted from `@nebula-lab/react-ui`'s `CodeBlock`: `copied` state (reset after 1.5s via `window.setTimeout`), the `navigator.clipboard.writeText` copy action (silently n… |  |
| 5 | `DataGrid` | react-ui/data-grid | `styleless`-tier `DataGrid` — the real behavior extracted from `@nebula-lab/react-ui`'s `DataGrid`: composes `@nebula-lab/hooks`' `useVirtualizer` directly (not `DataTable`, whose real un-virtualized `<table>` row model… |  |
| 6 | `DataTable` | react-ui/data-table | `styleless`-tier root of the DataTable compound component — the real behavior extracted from `@nebula-lab/react-ui`'s `DataTable`: sort state (`sortKey`/`sortDirection`, both controllable) and row-selection state (`selec… |  |
| 7 | `EmailInput` | react-ui/email-field | `styleless`-tier `EmailInput` — an `Input` preset to `type="email"`, which gets the browser's own format validation and mobile `@`-key keyboard layout for free. | Exposed in react-ui as `email-field`. |
| 8 | `FAB` | react-ui/fab | `styleless`-tier Floating Action Button — structurally identical to `IconButton` (icon-only, requires `aria-label`, inherits `Button`'s `loading` semantics) with its own name because it's a distinct semantic role (a page… |  |
| 9 | `IconButton` | react-ui/icon-button | `styleless`-tier `IconButton` — wraps this package's own `Button` (so `loading`/`aria-busy`/`data-loading` behavior comes along for free) and adds exactly one structural rule on top: `children` and `aria-label` are both… |  |
| 10 | `ImagePreview` | react-ui/image-preview | `styleless`-tier `ImagePreview` — the real behavior an "image upload" preset needs on top of `@nebula-lab/headless`'s `FileUpload`: turning a selected `File` into a renderable image without leaking memory. |  |
| 11 | `Input` | react-ui/input | `styleless`-tier `Input` — a thin pass-through of `@nebula-lab/primitives`' own `Input` (which already covers the one real behavior an input needs pre-styling: `invalid` -> `aria-invalid` wiring). |  |
| 12 | `PasswordInput` | react-ui/password-field | `styleless`-tier `PasswordInput` — the real behavior extracted from `@nebula-lab/react-ui`'s `PasswordField`: a password `Input` with a show/hide toggle button. | Exposed in react-ui as `password-field`. |
| 13 | `PasswordStrengthIndicator` | react-ui/password-strength-indicator | `styleless`-tier password strength meter — `role="meter"` (the WAI-ARIA Meter pattern: a value within a known range, not task-completion progress, which is what `role="progressbar"` implies instead). |  |
| 14 | `SearchInput` | react-ui/search-field | `styleless`-tier `SearchInput` — an `Input` preset to `type="search"`. | Exposed in react-ui as `search-field`. |
| 15 | `SplitButton` | react-ui/split-button | `styleless`-tier `SplitButton` — the structural contract only: a `role="group"` wrapper around a primary action button and a second, smaller trigger (typically a `DropdownMenu`-opening `IconButton` chevron). |  |
| 16 | `TelInput` | react-ui/tel-field | `styleless`-tier `TelInput` — an `Input` preset to `type="tel"`, which gets the mobile numeric-phone-pad keyboard layout for free (unlike `type="number"`, it doesn't reject non-digit characters like `+`/`(`/`)`, which re… | Exposed in react-ui as `tel-field`. |
| 17 | `Textarea` | react-ui/textarea | `styleless`-tier `Textarea` — a thin pass-through of `@nebula-lab/primitives`' own `Textarea` (already gives `invalid` -> `aria-invalid` wiring and `autoResize`), same reasoning as this package's `Input`: exists so `@neb… |  |
| 18 | `UrlInput` | react-ui/url-field | `styleless`-tier `UrlInput` — an `Input` preset to `type="url"`, which gets the browser's own format validation and mobile `.`/`/`-key keyboard layout for free. | Exposed in react-ui as `url-field`. |

## `@nebula-lab/react-ui`

The styled, themeable component library — Tailwind classes + this package's own design tokens (`theme.css`) applied over `headless`/`styleless`/`primitives`. Organized into category subfolders (`actions`, `data-display`, `forms`, `layout`, `media`, `navigation`, `overlays`, `theming`, `typography`, `feedback`, `drag-and-drop`) — every component still ships a flat `@nebula-lab/react-ui/<name>` import path regardless of its `src/` folder depth.

| SN | Component | Built On | Usage | Notes |
|---|---|---|---|---|
| 1 | `Button` | primitives/cn; styleless/button | Styled `Button` — the `react-ui` layer's job is purely visual, so this wraps `@nebula-lab/styleless`'s `Button` (which already gives `asChild` support, the `type="button"` default, and real `loading` semantics — `aria-bu… |  |
| 2 | `ButtonGroup` | primitives/cn,primitive | Visually merges a row (or column) of adjacent `Button`/`IconButton`s into one segmented control — purely a CSS trick via child-combinator Tailwind classes (`[&>*:not(:first-child)]:...`), no shared state or roving tabind… |  |
| 3 | `FAB` | primitives/cn; styleless/fab | Floating Action Button — wraps `@nebula-lab/styleless`'s `FAB` (required `aria-label`, `Button`'s `loading` semantics) and adds the circular, elevated styling. |  |
| 4 | `IconButton` | primitives/cn; styleless/icon-button | A square, icon-only `Button` — wraps `@nebula-lab/styleless`'s `IconButton` (which already enforces the required-`aria-label` contract and carries `Button`'s `loading` semantics), then reuses this package's own `buttonVa… |  |
| 5 | `SplitButton` | primitives/cn; styleless/split-button | A primary action button attached to a second, smaller trigger button (typically an `IconButton` chevron opening a `DropdownMenu` of related actions). |  |
| 6 | `Accordion` | primitives/cn; headless/accordion | Styled wrapper around `@nebula-lab/headless`'s `Accordion` — all ARIA/keyboard behavior (single/multiple, roving-tabindex, `collapsible`) comes from there unchanged; this layer only adds visual styling, per `ui`'s job de… |  |
| 7 | `Avatar` | primitives/cn; styleless/avatar | Styled wrapper around `@nebula-lab/styleless`'s `Avatar` — the image load/error tracking that lets `AvatarFallback` know when to render comes from there unchanged (moved down this session, since it's reusable non-visual… |  |
| 8 | `AvatarGroup` | primitives/cn; styleless/avatar-group | Styled wrapper around `@nebula-lab/styleless`'s `AvatarGroup` — the `max` truncation math comes from there unchanged. |  |
| 9 | `Badge` | primitives/cn,primitive | A small status/count pill. |  |
| 10 | `Card` | primitives/cn,primitive | A bordered container — `CardHeader`/`CardTitle`/`CardDescription`/ `CardContent`/`CardFooter` are purely presentational structure, no behavior, so (unlike `Accordion`/`Dialog`/`Popover`) there's no matching `@nebula-lab/… |  |
| 11 | `CardListItem` | primitives/cn | One row of a table's data rendered as a self-contained card instead of a `<tr>` — the small-screen fallback `DataTableBlock` switches to below its `cardBreakpoint` (a `<table>`'s fixed columns don't reflow onto a narrow… |  |
| 12 | `Carousel` | primitives/cn,primitive | Root of the Carousel compound component. |  |
| 13 | `Chip` | primitives/cn,primitive | The interactive/removable pill `Badge`'s own doc comment points at building separately rather than growing an `onDismiss` prop onto itself — this is that component. |  |
| 14 | `Collapsible` | headless/collapsible | No visual chrome of its own (just a `div` carrying `data-state`/ `data-disabled` for its `CollapsibleTrigger`/`CollapsibleContent` to key off of) — re-exported as-is from `@nebula-lab/headless`. |  |
| 15 | `DataGrid` | primitives/cn; styleless/data-grid | Themed wrapper over `@nebula-lab/styleless`'s `DataGrid` — a virtualized, WAI-ARIA Grid-pattern row/column grid (see that package's own doc comment for why this is a separate component from `DataTable`, not a variant of… |  |
| 16 | `DataTable` | primitives/cn; styleless/data-table | Wraps `@nebula-lab/styleless`'s `DataTable` (which owns the real behavior: controllable sort state, controllable row-selection state, both threaded to every descendant via context) and adds only the table's own Tailwind… |  |
| 17 | `DescriptionList` | primitives/cn,primitive | A `<dl>` of key/value pairs — `DescriptionTerm`/`DescriptionDetails` are purely presentational structure, same "no matching lower-layer compound" treatment `Stat`/`Card` document. |  |
| 18 | `EmptyState` | primitives/cn,primitive | A placeholder for an empty list/search-result/inbox — `EmptyStateIcon`/ `EmptyStateTitle`/`EmptyStateDescription`/`EmptyStateAction` are purely presentational structure, same "no matching `@nebula-lab/headless` or `@nebu… |  |
| 19 | `FilterBar` | primitives/cn | A row of active filter chips (`Chip`'s own built-in `onDismiss`, no custom dismiss-button logic needed here) plus an optional "Add filter" dropdown and a "Clear all" action. |  |
| 20 | `List` | primitives/cn,primitive | A plain list of `ListItem`s — purely presentational structure, no `@nebula-lab/headless` compound underneath (unlike `Menu`/`Tree`, this has no interactive selection/focus behavior at all; for that, reach for `Listbox`/`… |  |
| 21 | `PaymentMethodList` | primitives/cn | A saved payment method list — Minimals' Account → Billing "Payment Method Manager" (§3.10 SaaS/Billing). |  |
| 22 | `Sparkline` | primitives/cn,primitive | A dependency-free inline mini line/area chart — no matching `@nebula-lab/headless` compound, same "purely presentational, no ARIA behavior of its own" treatment `Stat`/`Timeline`/`EmptyState` already document. |  |
| 23 | `Stat` | primitives/cn,primitive | A single metric display — `StatLabel`/`StatValue`/`StatDescription` are purely presentational structure, same "no matching `@nebula-lab/headless` compound" treatment `Card` documents (single-file compound, not a folder-p… |  |
| 24 | `StaticRating` | primitives/cn,primitive | A read-only star rating display — `n`/5 filled stars plus an `aria-label` announcing the score as a sentence. |  |
| 25 | `Tag` | primitives/cn,primitive | A non-interactive category/keyword label — reuses `Badge`'s own `badgeVariants` recipe pinned to `variant: 'outline'` (see `../badge/badge.tsx`) rather than a separate `tagVariants`, since `Tag` is exactly `Badge`'s outl… |  |
| 26 | `Timeline` | primitives/cn,primitive | A vertical sequence of dated/ordered events — purely presentational structure, no matching `@nebula-lab/headless` compound, same treatment `List`/`Stat`/`Card` document (nothing here is a widget with its own ARIA behavio… |  |
| 27 | `Tree` | primitives/cn; headless/tree | Root `role="tree"` list — sets the base text size/color every nested `TreeItem` inherits. |  |
| 28 | `TreeTable` | primitives/cn | A hierarchical table — composes react-ui's own `DataTable` parts (`DataTableHeader`/`DataTableBody`/`DataTableRow`/`DataTableHead`/ `DataTableCell`) rather than reinventing table chrome, the same way `AlertDialog`/`Menub… |  |
| 29 | `TreeView` | (composed from other react-ui atoms) | Thin renamed re-export of this package's own `Tree` — mirrors `@nebula-lab/headless/tree-view`'s relationship to `@nebula-lab/headless/tree`. |  |
| 30 | `VirtualList` | primitives/cn | A virtualized single-column list — composes `@nebula-lab/hooks`' `useVirtualizer` directly, the same "pure windowing math, no ARIA behavior split worth decoupling" project-owner call `DataGrid` documents (no `@nebula-lab… |  |
| 31 | `Draggable` | primitives/cn,primitive | Makes its content draggable via the native HTML5 Drag and Drop API (`draggable`, `dragstart`/`dragend`) — built directly in `react-ui`, no `@nebula-lab/headless` layer underneath (project owner decision, see `AGENTS.md`'… |  |
| 32 | `Droppable` | primitives/cn,primitive | Accepts a `Draggable`'s drop via the native HTML5 Drag and Drop API. |  |
| 33 | `Sortable` | primitives/cn,primitive | Root of the Sortable compound component — reorders a list of `SortableItem`s by combining `Draggable`'s drag-start and `Droppable`'s drop-target behavior directly into each item (rather than composing separate `Draggable… |  |
| 34 | `SwipeableCards` | primitives/cn | A stacked deck of cards you swipe through — no `CarouselPrevious`/`Next` buttons, unlike `Carousel`: the interaction model here (think a mobile banking app's debit/credit card switcher) is "swipe the front card away," no… |  |
| 35 | `Alert` | primitives/cn,primitive | A status message box — `role="alert"` for assertive, auto-announced severities (`warning`/`danger`); `info`/`success` get `role="status"` instead, since a polite, non-interrupting announcement is more appropriate for goo… |  |
| 36 | `AlertDialog` | (composed from other react-ui atoms) | Thin renamed re-export of this package's own `Dialog` root — which itself renders no DOM of its own — mirrors `@nebula-lab/headless`'s `AlertDialog`-wraps-`Dialog` relationship. |  |
| 37 | `AlertPopup` | primitives/cn | A ready-to-use confirmation/status popup — a ["SweetAlert"](https://sweetalert2.github.io/)-style centered icon, title, message, and action button(s) — versus `AlertDialog`, which is the same underlying primitive but req… |  |
| 38 | `CircularProgress` | primitives/cn; headless/progress | An SVG-ring variant of `Progress` — same `@nebula-lab/headless` `Progress` underneath (unchanged `role="progressbar"` + `aria-valuenow`/`valuemin`/ `valuemax` semantics), just a different visual: a ring built from two st… |  |
| 39 | `Progress` | primitives/cn; headless/progress | Styled wrapper around `@nebula-lab/headless`'s `Progress`/`ProgressIndicator` pair — the `role="progressbar"` semantics, `aria-valuenow`/`valuemin`/ `valuemax`/`valuetext`, and indeterminate handling all come from there… |  |
| 40 | `Skeleton` | primitives/cn; headless/skeleton | Styled wrapper around `@nebula-lab/headless`'s `Skeleton` — the `aria-hidden` behavior comes from there unchanged. |  |
| 41 | `Spinner` | primitives/cn; headless/spinner | Styled wrapper around `@nebula-lab/headless`'s `Spinner` — the `role="status"` + visually-hidden label announcement come from there unchanged. |  |
| 42 | `Toast` | primitives/cn; headless/toast | A single notification card — reads `--toast-bg`/`-text`/`-border` (`neutral`/`neutral-content`, same "pop off the page" reasoning `tooltipTokens` uses — see `../tokens/component.ts`). |  |
| 43 | `Autocomplete` | (composed from other react-ui atoms) | Thin renamed re-export of this package's own `Combobox` — mirrors `@nebula-lab/headless/autocomplete`'s relationship to `@nebula-lab/headless/combobox`. |  |
| 44 | `Calendar` | primitives/cn | A month-grid date picker core — the WAI-ARIA [Date Picker Dialog](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/datepicker-dialog/) grid pattern: `role="grid"` over day `role="gridcell"`s, each a real `<… |  |
| 45 | `Checkbox` | primitives/cn; headless/checkbox | Styled wrapper around `@nebula-lab/headless`'s `Checkbox` — tri-state behavior (`checked`/`false`/`'indeterminate'`), keyboard handling (Space), and hidden-native-input form participation all come from there unchanged… |  |
| 46 | `CheckboxGroup` | primitives/cn; headless/checkbox-group | (no doc comment found) |  |
| 47 | `ColorPicker` | headless/color-picker | Thin re-export straight from `@nebula-lab/headless`: mints its own ambient `Popover` internally and renders no visible DOM of its own — nothing here for this package to style, same as this package's own `Popover` root… |  |
| 48 | `Combobox` | headless/combobox | The root renders no visible DOM of its own — nothing to style. |  |
| 49 | `DatePicker` | primitives/cn | A single self-contained component — not a compound (`DatePickerTrigger`/ `DatePickerContent`/etc.) — because unlike `MultiSelect`'s items or `AlertDialog`'s title/actions, there's no arbitrary consumer content to compose… |  |
| 50 | `DateRangePicker` | primitives/cn | `DatePicker`'s two-endpoint sibling — same self-contained (not compound) shape and same reasoning for why: the trigger and popup content are both fixed, nothing here is consumer-composed. |  |
| 51 | `EmailField` | primitives/cn; styleless/email-input | Styled `type="email"` `Input` preset — wraps `@nebula-lab/styleless`'s `EmailInput` and reuses `Input`'s exact class recipe (`inputVariants`), same "preset over the one real `Input`" shape `SearchField` established. |  |
| 52 | `Field` | primitives/cn; headless/field | Vertical stack layout for a label/control/description/error group — all the `id`/`aria-*` wiring between those sub-parts comes unchanged from the headless source. |  |
| 53 | `FileUpload` | headless/file-upload | The root renders no DOM of its own (just context + state) — nothing to style, re-exported as-is from `@nebula-lab/headless`, same as this package's own `Dialog` root. |  |
| 54 | `Form` | primitives/cn,form | Styled `Form` — wraps `@nebula-lab/primitives`' unstyled `Form` (which already defaults `noValidate` and calls `event.preventDefault()` before your `onSubmit` runs) with the vertical-stack field layout most forms in this… |  |
| 55 | `ImageUpload` | primitives/cn; headless/file-upload; styleless/image-preview | A ready-to-use image uploader — composes `@nebula-lab/headless`'s `FileUpload` (defaulting `accept="image/*"`) with `@nebula-lab/styleless`'s `ImagePreview` (which owns the real object-URL-lifecycle behavior) inside a th… |  |
| 56 | `Input` | primitives/cn; styleless/input | Styled `input` — wraps `@nebula-lab/styleless`'s `Input` (itself a thin pass- through of `@nebula-lab/primitives`' `Input`, which gives `invalid` -> `aria-invalid` wiring and ref forwarding) rather than reaching past it… |  |
| 57 | `MultiSelect` | (composed from other react-ui atoms) | Root of the MultiSelect compound component — built directly in `react-ui` with no `@nebula-lab/headless` layer underneath, the same project-owner call `Carousel`/`DataTable`/`DataGrid` make: this composes two already-bui… |  |
| 58 | `NativeSelect` | primitives/cn,native-select | Styled native `<select>` — wraps `@nebula-lab/primitives`' unstyled `NativeSelect` (which already gives `invalid` -> `aria-invalid` wiring) with `Input`'s own `inputVariants` recipe, so a native select reads as pixel-ide… |  |
| 59 | `NumberInput` | headless/number-input | The root renders no DOM of its own (just context + state) — nothing to style, re-exported as-is from `@nebula-lab/headless`. |  |
| 60 | `OTPInput` | primitives/cn; headless/otp-input | The headless root has no `className` of its own — it renders no DOM at all (just context/state, see the headless source), so the layout div this wrapper adds is a genuinely new element, not a passthrough onto an existing… |  |
| 61 | `PasswordField` | primitives/cn; styleless/password-input | Wraps `@nebula-lab/styleless`'s `PasswordInput` (which owns the real behavior: `visible` state, `type="password"\|"text"` swapping, `aria-pressed` wiring) and supplies the eye/eye-off icon pair via `renderToggle` plus the… |  |
| 62 | `PasswordStrengthIndicator` | primitives/cn; styleless/password-strength-indicator | Wraps `@nebula-lab/styleless`'s `PasswordStrengthIndicator` (which owns the real behavior: the length/character-class scoring heuristic and the `role="meter"` semantics) and supplies every part's Tailwind classes via its… |  |
| 63 | `RadioGroup` | primitives/cn; headless/radio-group | Styled wrapper around `@nebula-lab/headless`'s `RadioGroup` — roving-tabindex arrow-key navigation and single-select state come from there unchanged. |  |
| 64 | `Rating` | primitives/cn; headless/rating | (no doc comment found) |  |
| 65 | `SearchField` | primitives/cn; styleless/search-input | Wraps `@nebula-lab/styleless`'s `SearchInput` (the `type="search"` preset) and adds a leading search icon — like `PasswordField`, a `react-ui`- layer-only visual addition (no `@nebula-lab/headless` counterpart), since a… |  |
| 66 | `SegmentedControl` | primitives/cn; headless/toggle-group | A single-select visual variant of `ToggleGroup` — same headless behavior (`@nebula-lab/headless`'s `ToggleGroup` with `type="single"` fixed, since a segmented control by definition allows exactly one active segment), jus… |  |
| 67 | `Select` | headless/select | The root renders no visible DOM of its own (a `Popper` anchor scope plus context/state, same as `Popover`'s root) — nothing to style, re-exported as-is. |  |
| 68 | `Slider` | headless/slider | The root renders no DOM of its own (just context + state) — nothing to style, re-exported as-is from `@nebula-lab/headless`. |  |
| 69 | `Switch` | primitives/cn; headless/switch | Styled wrapper around `@nebula-lab/headless`'s `Switch` — on/off behavior, `role="switch"`, and hidden-native-input form participation all come from there unchanged. |  |
| 70 | `TelField` | primitives/cn; styleless/tel-input | Styled `type="tel"` `Input` preset — wraps `@nebula-lab/styleless`'s `TelInput` and reuses `Input`'s exact class recipe (`inputVariants`). |  |
| 71 | `TextArea` | primitives/cn; styleless/textarea | Styled `textarea` — wraps `@nebula-lab/styleless`'s `Textarea` (itself a thin pass-through of `@nebula-lab/primitives`' `Textarea`, which gives `invalid` -> `aria-invalid` wiring, ref forwarding, and the optional `autoRe… |  |
| 72 | `TimePicker` | primitives/cn | An `Input` preset to `type="time"` — same "`react-ui`-layer-only convenience, no `@nebula-lab/headless` counterpart" reasoning `SearchField`/ `PasswordField` document: a native `type="time"` input already gives a fully k… |  |
| 73 | `Toggle` | primitives/cn; headless/toggle | `data-state="on"\|"off"` (set by the headless source) fills the button when pressed — same `base-200`/`primary`-adjacent treatment other selectable-row components in this package use. |  |
| 74 | `ToggleGroup` | primitives/cn; headless/toggle-group | `data-orientation` (set by the headless source) switches the row between horizontal and vertical layout. |  |
| 75 | `UrlField` | primitives/cn; styleless/url-input | Styled `type="url"` `Input` preset — wraps `@nebula-lab/styleless`'s `UrlInput` and reuses `Input`'s exact class recipe (`inputVariants`). |  |
| 76 | `AspectRatio` | primitives/aspect-ratio | Styled `AspectRatio` — thin re-export of `@nebula-lab/primitives`' `AspectRatio`, constraining content to a fixed width/height ratio regardless of its own intrinsic size (video/image/chart containers). |  |
| 77 | `Box` | primitives/box | Styled `Box` — thin re-export of `@nebula-lab/primitives`' `Box`, the plainest possible polymorphic building block (a `Primitive` by another name, zero injected classes or structural opinion). |  |
| 78 | `Center` | primitives/center,types | Styled `Center` — thin re-export of `@nebula-lab/primitives`' `Flex` with both axes centered, for centering a single child (a spinner, an empty-state illustration) both horizontally and vertically. |  |
| 79 | `Container` | primitives/container | Styled `Container` — thin re-export of `@nebula-lab/primitives`' `Container` (horizontally centers content, caps width at `size`, adds responsive horizontal padding). |  |
| 80 | `Flex` | primitives/flex | Styled `Flex` — a thin re-export of `@nebula-lab/primitives`' unstyled `Flex` (`direction`/`align`/`justify`/`wrap`/`gap` convenience props instead of hand-written Tailwind flex classes). |  |
| 81 | `Footer` | primitives/cn,primitive | A page/app-level `<footer>` — same purely-presentational treatment as `Header`, just `border-t` instead of `border-b`. |  |
| 82 | `Grid` | primitives/grid | Styled `Grid` — thin re-export of `@nebula-lab/primitives`' `Grid`. |  |
| 83 | `Header` | primitives/cn,primitive | A page/app-level `<header>` — purely presentational structure, no behavior, so (like `Card`) there's no matching `@nebula-lab/headless` compound underneath this one; a thin `cn()` wrapper around `Primitive` directly. |  |
| 84 | `HStack` | primitives/hstack,types | Styled `HStack` — thin re-export of `@nebula-lab/primitives`' `Flex` pinned to a single-line horizontal row (no wrap — overflow scrolls/clips rather than wrapping; reach for `Inline`/`Wrap` when it should wrap). |  |
| 85 | `Inline` | primitives/inline | Styled `Inline` — thin re-export of `@nebula-lab/primitives`' `Flex` pinned to a horizontal row that wraps (tag lists, button groups, breadcrumbs) instead of overflowing. |  |
| 86 | `Main` | primitives/cn,primitive | The page's `<main>` landmark — `flex-1` so it fills remaining space alongside a `Sidebar`/`Header`/`Footer` in a flex/grid page shell. |  |
| 87 | `Paper` | primitives/cn,primitive | A bordered or elevated surface for arbitrary content — like `Card`, but without `Card`'s compound header/title/content/footer sub-parts: reach for `Paper` for a generic raised panel (a floating toolbar, a custom layout t… |  |
| 88 | `Section` | primitives/cn,primitive | A `<section>` landmark for one distinct region of page content — vertical spacing between its own children only (`space-y-4`), no background/border of its own, same "neutral content region" treatment `Main` uses. |  |
| 89 | `Separator` | primitives/cn,primitive | A thin rule, horizontal or vertical, styled off `--separator-bg` (see `../tokens/component.ts`), which currently points at `--color-base-300` — the same token flagged as a known WCAG 1.4.11 failure in `CONTRAST_AUDIT.md`… |  |
| 90 | `Spacer` | primitives/spacer,types | Styled `Spacer` — thin re-export of `@nebula-lab/primitives`' `Spacer`, an invisible `flex-grow`ing filler for pushing flex children to opposite ends without hand-writing `justify="between"`. |  |
| 91 | `Stack` | primitives/stack | Styled `Stack` — thin re-export of `@nebula-lab/primitives`' `Flex` pinned to `direction="column"`. |  |
| 92 | `Surface` | primitives/cn,primitive | The plainest possible themed background — just `--surface-bg`/`-text`, no border/shadow/radius at all (unlike `Paper`/`Card`). |  |
| 93 | `VStack` | primitives/vstack | Styled `VStack` — alias of `Stack` under the name consumers coming from Chakra UI reach for first. |  |
| 94 | `Wrap` | primitives/wrap | Styled `Wrap` — alias of `Inline` under the name consumers coming from Chakra UI reach for first. |  |
| 95 | `Audio` | primitives/cn,primitive | A themed `<audio>` — same "just adds rounding, native controls stay in charge" treatment as `Video`. |  |
| 96 | `Image` | primitives/cn,image,types | Styled `Image` — wraps `@nebula-lab/primitives`' unstyled `Image` and adds `display: block` (an `<img>` is inline by default, which leaves a few px of baseline gap under it inside a flex/block container — a common, easy-… |  |
| 97 | `ImagePreview` | primitives/cn; styleless/image-preview | Styled `ImagePreview` — wraps `@nebula-lab/styleless`'s `ImagePreview` (which owns the real behavior: turning a selected `File` into a renderable image via `URL.createObjectURL`, revoked on unmount/change) with the same… |  |
| 98 | `Logo` | (composed from other react-ui atoms) | The nebula wordmark's icon — an original four-point sparkle/star mark (not a literal "N" letterform), picked to evoke the project's own name rather than trace a specific external reference. |  |
| 99 | `Video` | primitives/cn,primitive | A themed `<video>` — purely presentational (rounded, clipped corners), no custom player chrome of its own; native browser `controls` are shown by default (pass `controls={false}` to drive playback entirely yourself). |  |
| 100 | `BottomNav` | primitives/cn,primitive | A fixed, mobile-only tab bar — `<nav>` landmark pinned to the viewport bottom, hidden at `md:` and above (this is a phone-width navigation pattern; wider viewports should use `Sidebar`/`Navbar` instead). |  |
| 101 | `Breadcrumb` | headless/breadcrumb | A `<nav>` landmark with no visual chrome of its own — `BreadcrumbList` carries the actual layout styling, same "wrapper vs. |  |
| 102 | `Navbar` | primitives/cn,primitive | A horizontal `<nav>` row — distinct from `Header` in tag (`nav` vs. |  |
| 103 | `NavigationMenu` | primitives/cn; headless/navigation-menu | Root `<nav>` — relative positioning so a consumer's own `NavigationMenuContent` can be absolutely placed against it if not portalled. |  |
| 104 | `Pagination` | headless/pagination | The root renders a plain `<nav>` with no visual chrome of its own (see `@nebula-lab/headless`'s `Pagination`) — layout/styling lives on `PaginationList`/`PaginationLink`/etc., so there's nothing for this layer to add her… |  |
| 105 | `SideNav` | primitives/cn,primitive | The vertical nav landmark meant to render inside `Sidebar`'s children — `Sidebar` itself is a bare `<aside>` with no opinion on content (see its own doc comment), so `SideNav` is the `<nav>` landmark + vertical-stack lay… |  |
| 106 | `Sidebar` | primitives/cn,primitive | A fixed-width `<aside>` for app navigation/filters — `side` only flips which edge gets the border (`border-r` for a left sidebar, `border-l` for a right one); actual page placement is the consumer's own layout (flex/ gri… |  |
| 107 | `Stepper` | headless/stepper | Root has no visual chrome of its own — it's a `<nav>` landmark, same as `Pagination`'s root. |  |
| 108 | `Tabs` | headless/tabs | The root renders a plain `div` with no visual chrome of its own (see `@nebula-lab/headless`'s `Tabs`) — layout/styling lives on `TabList`/`Tab`/ `TabPanel`, so there's nothing for this layer to add here. |  |
| 109 | `Backdrop` | primitives/cn,overlay | The styled counterpart of `@nebula-lab/primitives`' bare `Overlay` (`fixed inset-0`, zero color/blur of its own by design) — a standalone, theme-aware backdrop usable anywhere a full-viewport scrim is needed, not just be… |  |
| 110 | `Command` | primitives/cn; headless/command | The palette's outer card — same `base-100`/`base-300`/`base-content` surface treatment as `DialogContent`/`PopoverContent`, since a command palette is usually rendered inside a consumer-supplied `Dialog`. |  |
| 111 | `ContextMenu` | (composed from other react-ui atoms) | Thin renamed re-export of this package's own `Menu` — mirrors `@nebula-lab/headless/context-menu`'s relationship to `@nebula-lab/headless/menu`. |  |
| 112 | `Dialog` | headless/dialog | The root renders no DOM of its own (just context + state) — nothing to style, re-exported as-is from `@nebula-lab/headless`. |  |
| 113 | `Drawer` | headless/drawer | The root renders no DOM of its own (just context + state, reusing `Dialog`'s directly — see the headless source's header comment) — nothing to style, re-exported as-is. |  |
| 114 | `DropdownMenu` | (composed from other react-ui atoms) | Thin renamed re-export of this package's own `Menu` — mirrors `@nebula-lab/headless/dropdown-menu`'s relationship to `@nebula-lab/headless/menu`. |  |
| 115 | `HoverCard` | headless/hover-card | The root renders no DOM of its own (just context + state) — nothing to style, re-exported as-is from `@nebula-lab/headless`. |  |
| 116 | `Menu` | headless/menu | The root renders no visible DOM of its own (a `Popper` anchor scope plus context/state, same as `Popover`'s root) — nothing to style. |  |
| 117 | `Menubar` | primitives/cn; headless/menubar | Unlike this package's own `Menu` (whose root renders no visible DOM of its own), `Menubar`'s root *is* the horizontal bar itself — it renders `RovingFocusGroup` directly rather than through a portal, so it's the one root… |  |
| 118 | `Popover` | headless/popover | The root renders no DOM of its own (just context + anchor state) — nothing to style, re-exported as-is from `@nebula-lab/headless`. |  |
| 119 | `Sheet` | (composed from other react-ui atoms) | Thin renamed re-export of this package's own `Drawer` parts — a "Sheet" (the shadcn/Radix naming) and a "Drawer" (the Chakra/MUI naming) are the same edge-anchored overlay pattern under two different vocabularies; no pro… |  |
| 120 | `Tooltip` | headless/tooltip | The root renders no DOM of its own (just scoped context + `Popper`) — nothing to style, re-exported as-is from `@nebula-lab/headless`. |  |
| 121 | `ThemeProvider` | primitives/create-context-scope | Wraps the app, persists the user's light/dark/system preference, and reflects it on `<html>` two ways: a `data-theme` attribute (`"light"` / `"dark"` / `"system"` — the original mechanism) and a `.dark` class, toggled to… |  |
| 122 | `ThemeSwitcher` | (composed from other react-ui atoms) | A three-way light/dark/system switcher — built purely from this package's own `IconButton`/`DropdownMenu` and `ThemeProvider`, no new primitives. |  |
| 123 | `Tokens` | (composed from other react-ui atoms) | Focus-ring offset convention (also documented, not tokenized — same reasoning as the icon-size convention above: `ring-offset-1`/`-2` are already Tailwind's own systematic scale, nothing new to add). |  |
| 124 | `Blockquote` | primitives/cn,primitive | A quoted passage — purely presentational, no `@nebula-lab/primitives`/ `@nebula-lab/headless` counterpart underneath (unlike `Text`/`Heading`/ `Code`, which wrap an existing unstyled primitive), same "thin `cn()` wrapper… |  |
| 125 | `Code` | primitives/cn,code,types | Styled inline `Code` — overrides the unstyled primitive's hardcoded `bg-gray-100` (a pre-token-system leftover, not yet themed at that layer) with `--code-bg`/`-text` (see `../tokens/component.ts`); `cn()`'s `tailwind-me… |  |
| 126 | `CodeBlock` | primitives/cn; styleless/code-block | Wraps `@nebula-lab/styleless`'s `CodeBlock` (which owns the real behavior: `copied` state, the clipboard write, splitting `code` into lines) and supplies every part's Tailwind classes via its `classNames` prop — this fil… |  |
| 127 | `Heading` | primitives/cn,heading,types | Styled `Heading` — wraps `@nebula-lab/primitives`' unstyled, polymorphic `Heading` (which already derives size/weight from `level`, independent of the rendered tag) and adds this theme's heading font stack (`--font-headi… |  |
| 128 | `Kbd` | primitives/cn,primitive | A keyboard-shortcut label (e.g. |  |
| 129 | `KbdShortcut` | primitives/cn,primitive | A `Kbd` sequence renderer — same "no lower-layer counterpart" treatment `Kbd` itself already documents (purely presentational, no ARIA pattern, no state). |  |
| 130 | `Label` | primitives/cn,label | Styled `Label` — wraps `@nebula-lab/primitives`' unstyled `Label` (which already gives the `required` asterisk + screen-reader announcement) and adds the same `text-sm font-medium text-[var(--field-label-text)]` treatmen… |  |
| 131 | `Link` | primitives/cn,link | Styled `Link` — wraps `@nebula-lab/primitives`' unstyled `Link` (which already gives underline-on-hover, a focus-visible ring, and the `external` convenience prop) and adds this theme's primary color. |  |
| 132 | `Markdown` | primitives/cn | Renders a **practical subset** of Markdown as themed `react-ui` components — not a full CommonMark implementation (see `markdown-utils.ts`'s header comment for why: no npm registry access in this sandbox to add a real pa… |  |
| 133 | `Paragraph` | primitives/paragraph | Styled `Paragraph` — thin re-export of `@nebula-lab/primitives`' `Paragraph`, a `p` with comfortable reading line-height by default — for body copy, as distinct from `Text` (no line-height opinion, meant for inline runs)… |  |
| 134 | `Pre` | primitives/cn,pre | Styled `Pre` — wraps `@nebula-lab/primitives`' `Pre` (multi-line code block container, monospace, horizontally scrollable). |  |
| 135 | `Text` | primitives/cn,text,types | Styled `Text` — wraps `@nebula-lab/primitives`' unstyled, polymorphic `Text` (which already gives `as`/`truncate`) and only adds this theme's default body color (`--text-color`, see `../tokens/component.ts`) — no font-si… |  |

## `@nebula-lab/react-ui-blocks`

Organisms — full page sections/flows (`LoginForm`, `SaasAppHeader`, `ChatWindow`, ...) composed from `react-ui` components, each shaped by one specific flow's copy/logic rather than being domain-neutral. Exported per domain category (`authentication`, `dashboard`, `ecommerce`, ...), not per-component.

| SN | Component | Built On (react-ui) | Usage | Notes |
|---|---|---|---|---|
| 1 | `AuthSplitLayout` | react-ui/heading, react-ui/text | A two-panel auth shell: a left marketing panel (logo, headline, an illustration slot) beside a right panel that centers `children` (the actual form) — visible only at `lg` and above. |  |
| 2 | `LoginForm` | react-ui/button, react-ui/card, react-ui/checkbox, react-ui/field, react-ui/input, react-ui/password-field | A email/password sign-in card composed entirely from `@nebula-lab/react-ui` components (`Card`, `Field`, `Input`, `PasswordField`, `Checkbox`, `Button`) — no new primitives, matching this package's own README rule that e… |  |
| 3 | `SignupForm` | react-ui/button, react-ui/card, react-ui/checkbox, react-ui/field, react-ui/input, react-ui/password-field | A name/email/password sign-up card — deliberately mirrors `LoginForm`'s structure and conventions (see `../sign-in/login-form.tsx`): no validation of its own (including **no password-match check** between `password` and… |  |
| 4 | `ChatWindow` | react-ui/avatar, react-ui/badge, react-ui/button, react-ui/card, react-ui/empty-state, react-ui/input, react-ui/text | A single-conversation chat window — contact list + message thread + composer, Minimals' Chat page (§3.19 Communication → *Chat Window*). |  |
| 5 | `NotificationCenter` | react-ui/badge, react-ui/button, react-ui/card, react-ui/tabs | A standalone notifications inbox — All/Unread tabs (`Tabs`, animated underline via its own `data-[state=active]` treatment), a scrollable list, per-row hover-revealed "mark as read", and a header-level "mark all as read"… |  |
| 6 | `BalanceCard` | react-ui/button, react-ui/card, react-ui/text | A balance summary card with a row of quick-action buttons underneath — the "current balance + Send/Top-up" panel that leads Minimals' Banking home page. |  |
| 7 | `BillingSummaryCard` | react-ui/card, react-ui/progress, react-ui/text | A progress-list breakdown card — one labeled `Progress` bar per category, the "storage used by file type" / "budget spent by category" pattern from Minimals' File and Banking dashboard homes. |  |
| 8 | `PlanCards` | react-ui/badge, react-ui/list, react-ui/radio-group, react-ui/text | Plan comparison cards — the pricing-tier selector from Minimals' Account → Billing settings tab (§3.10 SaaS/Billing → "Plan Comparison Cards"). |  |
| 9 | `ChartCard` | react-ui/card, react-ui/text | A dashboard chart card — bar, line, area, radar, donut, or gauge, all themed entirely off Nebula's own `--color-*` semantic tokens (never a hardcoded hex), so a chart re-themes for free the same way every other component… |  |
| 10 | `TeamMemberCard` | react-ui/avatar, react-ui/card, react-ui/icon-button, react-ui/text | A team-member grid card — cover placeholder + overlapping avatar + name/role + social-platform icon row + stat triple, Minimals' User Cards page (§3.10 Team → *Team Member List*, card variant). |  |
| 11 | `DashboardOverview` | react-ui/card, react-ui/heading, react-ui/sparkline, react-ui/stat | A responsive grid of metric cards — the metrics-grid half of a dashboard "overview" screen. |  |
| 12 | `RankedList` | react-ui/card, react-ui/list, react-ui/text | A numbered ranking list — "Top products," "Top countries by revenue," the mini-table pattern from Minimals' Ecommerce/Booking dashboard homes (§3.5 Data Display → *Comparison Table* / §3.8 Dashboard → *Activity*, no sing… |  |
| 13 | `ReviewsList` | react-ui/avatar, react-ui/card, react-ui/list, react-ui/static-rating, react-ui/text | An avatar + star rating + text reviews list — Minimals' Course/Product review section (§3.12 Blog → *Comment Section* is the closest taxonomy analog, though this is read-only with no reply/compose affordance a comment se… |  |
| 14 | `ThumbnailList` | react-ui/card, react-ui/list, react-ui/text | A thumbnail-led list with a per-item trend indicator — "Best-selling products this week" with a small change badge, the pattern from Minimals' Ecommerce/Course dashboard homes. |  |
| 15 | `WelcomeBanner` | react-ui/button, react-ui/card, react-ui/heading, react-ui/text | A dashboard-home greeting banner — the "welcome back" card that typically sits above the metrics/chart grid on a dashboard's landing screen (the counterpart `Hero`'s marketing-page role plays one vertical over). |  |
| 16 | `DataTableBlock` | react-ui/badge, react-ui/card, react-ui/data-table, react-ui/empty-state, react-ui/icon-button, react-ui/menu, react-ui/pagination, react-ui/search-field, react-ui/segmented-control | The `BLOCKS_ARCHITECTURE.md` §3.5 "Generic Data Table Block" — status tabs + filter toolbar + sortable/selectable table + pagination footer, composed entirely from `@nebula-lab/react-ui`'s own `DataTable` compound (sort/… |  |
| 17 | `ListingCard` | react-ui/card, react-ui/icon-button, react-ui/menu, react-ui/text | A generalized listing/browse card — company-logo-tile Job listings and photo-mosaic Tour listings from Minimals both reduce to the same shape (a media slot, a title, meta rows, an optional overflow menu), just with very… |  |
| 18 | `ProductCard` | react-ui/badge, react-ui/button, react-ui/card, react-ui/heading, react-ui/icon-button, react-ui/static-rating, react-ui/text | A single product tile — image with a hover zoom, a wishlist heart toggle and status badge overlaid on opposite corners, name, price (with an optional struck-through original price for a discount), an optional star-rating… |  |
| 19 | `ProductGallery` | react-ui/carousel, react-ui/text | Product Detail's image gallery (§2.16/§3.7 Product Detail → *Gallery*) — a main `Carousel` view + slide counter + prev/next, plus a thumbnail strip below for direct navigation. |  |
| 20 | `ProductInfoPanel` | react-ui/badge, react-ui/button, react-ui/heading, react-ui/icon-button, react-ui/number-input, react-ui/radio-group, react-ui/segmented-control, react-ui/static-rating, react-ui/text | Product Detail's info panel (§2.16/§3.7 Product Detail → *Info Panel*, *Size/Variant Selector*) — status badge, title, rating, price, colors, sizes, quantity, availability, and an action row. |  |
| 21 | `EntityFormLayout` | react-ui/avatar, react-ui/button, react-ui/card, react-ui/switch, react-ui/text | The avatar-card + field-grid entity form shell — Minimals' User Create/Edit page, generalized to any entity (§3.14 *Account Profile Form*, the same shell this session's inventory also flags as reused unchanged for the Ac… |  |
| 22 | `TransactionForm` | react-ui/button, react-ui/card, react-ui/date-picker, react-ui/field, react-ui/input, react-ui/select, react-ui/textarea, react-ui/toggle-group | A generic income/expense entry form — named "Transaction" rather than anything ecommerce-specific (no "Order"/"Payment"/"Checkout") since this package is deliberately domain-neutral (see its own README): the same shape s… |  |
| 23 | `Hero` | react-ui/button, react-ui/heading, react-ui/text | A marketing landing-page hero — headline/subheadline/CTA(s), with an optional media slot that switches the layout from a single centered column to a two-column split once supplied. |  |
| 24 | `PromoBanner` | react-ui/heading, react-ui/text | A single promo/ad-style banner card — headline/subheadline over a light surface, a curved color-block shape sweeping in from the trailing edge (an SVG path with `preserveAspectRatio="none"`, so it stretches to fill any c… |  |
| 25 | `PromoBannerCarousel` | react-ui/carousel | One promo banner on screen at a time, navigated by swipe (touch/pointer drag) or trackpad/wheel scroll — no `CarouselPrevious`/`CarouselNext` buttons, matching the reference "story-style" ad slider this was built from: j… |  |
| 26 | `SaasAppHeader` | react-ui/avatar, react-ui/dropdown-menu, react-ui/heading, react-ui/icon-button, react-ui/navigation-menu, react-ui/popover, react-ui/sheet, react-ui/text | A modern SaaS application header: logo + brand + primary nav (desktop: `NavigationMenu` with optional dropdown sub-menus; mobile: a `Sheet` slide- over, `md:hidden`/`hidden md:block` split, same breakpoint convention `Bo… |  |
| 27 | `ProfileHeader` | react-ui/avatar, react-ui/heading, react-ui/tabs, react-ui/text | A social profile header — cover banner + overlapping avatar + name/role + stat pair + section tabs, Minimals' User Profile page (§3.13 Social → *Profile Header*, *Profile Tabs*). |  |

---

## Known gaps (computed from this table, not hand-listed)

_None right now — every non-internal-utility component in `primitives`/`headless`/`styleless` has a `react-ui` wrapper._
- **`headless/listbox`**: deliberately has no `react-ui` wrapper — it's `Select`/`Combobox`/`Autocomplete`'s shared internal engine, not meant to be styled standalone.
- **Renames across layers**: `styleless`'s `email-input`/`password-input`/`search-input`/`tel-input`/`url-input` are exposed in `react-ui` as `email-field`/`password-field`/`search-field`/`tel-field`/`url-field` — same component, different public name.

## Keeping this doc current

This file is generated, not hand-maintained — each component's **Usage** cell is the first sentence of that component's own doc comment (JSDoc directly above its `function X`/`const X` declaration), and **Built On**/**Extended By** are computed from real `import` statements and folder-name matches, not guessed. Re-run `node scripts/generate-component-inventory.mjs` after any component is added, renamed, or moved between packages so the totals/gaps above stay accurate — don't hand-edit rows here, fix the source doc comment instead and regenerate.
