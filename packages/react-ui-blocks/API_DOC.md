# `@nebula-lab/react-ui-blocks` — API Reference

Generated from the actual TypeScript source (real exported prop/parameter names and types, not hand-transcribed) — regenerate with `node scripts/generate-api-docs.mjs` after adding/changing a component so this doesn't drift. See `ARCHITECTURE.md` at the repo root for how this package fits into the overall layering, and this package's own `README.md` for install/usage.

Cross-package prop types (e.g. a `react-ui` component's Props extending a `styleless` one) are shown as `extends `Type` (from `@nebula-lab/other-package`)` rather than flattened in place — look up that type in the other package's own API_DOC.md. Inherited native DOM attributes (from `PrimitivePropsWithRef`/`PolymorphicComponentPropsWithRef`) are summarized as a single note rather than enumerated, since they're just the target element's standard attributes.

---

## `@nebula-lab/react-ui-blocks`

Full page sections/flows composed from `react-ui` components — each block is its own file; a leaf folder may hold several sibling blocks.

| SN | Component | Props | Usage notes | Comments |
|---|---|---|---|---|
| 1 | `AuthSplitLayout` | `children: React.ReactNode` — The auth form itself — e.g. `<LoginForm />`/`<SignupForm />` — rendered centered in the right panel (the only panel shown below `lg`).<br>`logo?: React.ReactNode` — Rendered top-left of the left panel.<br>`illustration?: React.ReactNode` — The left panel's illustration/artwork — deliberately a consumer-supplied slot, not a bundled image, since real marketing artwork is…<br>`title?: React.ReactNode` — Left panel headline, e.g. `"Hi, welcome back"`.<br>`description?: React.ReactNode` — Left panel supporting text under `title`.<br>`footerSlot?: React.ReactNode` — Rendered at the bottom of the left panel — e.g. a row of partner/trust logos.<br>`topRightSlot?: React.ReactNode` — Rendered top-right of the right panel — e.g. a "Need help?" link or a language switcher.<br>`className?: string` | A two-panel auth shell: a left marketing panel (logo, headline, an illustration slot) beside a right panel that centers `children` (the actual form) — visible only at `lg` and above. | `authentication/auth-split-layout` |
| 2 | `LoginForm` | `onSubmit?: (values: LoginFormValues) => void` — Called with the current field values on submit — this component never calls an API itself, same "block owns the UI, consumer owns the…<br>`isSubmitting?: boolean` — Shows the submit button's busy state and disables the form.<br>`error?: string` — A server-side/auth error to display above the fields (e.g. "Invalid email or password") — distinct from per-field validation, which this…<br>`title?: React.ReactNode`<br>`description?: React.ReactNode`<br>`submitLabel?: React.ReactNode`<br>`footer?: React.ReactNode` — Rendered below the submit button — e.g. a "Forgot password?" link or a "Don't have an account? Sign up" line. Left to the consumer since…<br>`card?: boolean` — `true` (default) wraps the form in `Card` — a self-contained sign-in widget droppable anywhere. `false` renders the same…<br>`className?: string` | A email/password sign-in card composed entirely from `@nebula-lab/react-ui` components (`Card`, `Field`, `Input`, `PasswordField`, `Checkbox`, `Button`) — no new primitives, matching this package's own README rule that everything here builds purely on the layer below. | `authentication/sign-in` |
| 3 | `SignupForm` | `onSubmit?: (values: SignupFormValues) => void` — Called with the current field values on submit — same "block owns the UI, consumer owns the request" split `LoginForm` follows.<br>`isSubmitting?: boolean`<br>`error?: string`<br>`title?: React.ReactNode`<br>`description?: React.ReactNode`<br>`submitLabel?: React.ReactNode`<br>`termsLabel?: React.ReactNode` — Rendered inline with the terms checkbox — a node (not a string) since it typically wraps `<a>` links to Terms/Privacy pages.<br>`footer?: React.ReactNode` — Rendered below the submit button — e.g. "Already have an account? Sign in".<br>`card?: boolean` — `true` (default) wraps the form in `Card`. `false` renders the same title/description/fields with no card border/shadow — same reasoning…<br>`className?: string` | A name/email/password sign-up card — deliberately mirrors `LoginForm`'s structure and conventions (see `../sign-in/login-form.tsx`): no validation of its own (including **no password-match check** between `password` and `confirmPassword` — both raw values are handed to `onSubmit`, and comparing them is left to the… | `authentication/sign-up` |
| 4 | `ChatWindow` | `contacts: ChatContact[]`<br>`activeContactId?: string`<br>`onActiveContactChange?: (id: string) => void`<br>`messages: ChatMessage[]` — Messages for the currently active conversation only — this component has no notion of "all conversations," the consumer already filtered by…<br>`currentUserId: string`<br>`onSendMessage?: (content: string) => void` — Omit to render the composer `disabled` (e.g. a read-only transcript view).<br>`emptyStateTitle?: React.ReactNode`<br>`emptyStateDescription?: React.ReactNode`<br>`className?: string` | A single-conversation chat window — contact list + message thread + composer, Minimals' Chat page (§3.19 Communication → *Chat Window*). | `communication/chat-window` |
| 5 | `NotificationCenter` | `notifications: NotificationCenterItem[]` — No sensible default — every notification center has its own feed.<br>`onNotificationClick?: (id: string) => void`<br>`onMarkRead?: (id: string) => void`<br>`onMarkAllRead?: () => void`<br>`title?: React.ReactNode`<br>`className?: string` | A standalone notifications inbox — All/Unread tabs (`Tabs`, animated underline via its own `data-[state=active]` treatment), a scrollable list, per-row hover-revealed "mark as read", and a header-level "mark all as read". | `communication/notification-center` |
| 6 | `BalanceCard` | `label: React.ReactNode` — e.g. "Total balance".<br>`amount: React.ReactNode` — Pre-formatted by the consumer (e.g. `"$24,500.00"`) — same convention `DashboardOverview`'s `Stat` value uses, since currency/locale…<br>`description?: React.ReactNode` — e.g. "+2.5% from last month".<br>`actions?: BalanceCardAction[]` — 1-3 quick actions rendered as a button row below the amount (e.g. "Send", "Top up"). The first action renders filled (`color="primary"`),…<br>`className?: string` | A balance summary card with a row of quick-action buttons underneath — the "current balance + Send/Top-up" panel that leads Minimals' Banking home page. | `dashboard/billing` |
| 7 | `BillingSummaryCard` | `title: React.ReactNode`<br>`description?: React.ReactNode`<br>`items: BillingSummaryItem[]`<br>`className?: string` | A progress-list breakdown card — one labeled `Progress` bar per category, the "storage used by file type" / "budget spent by category" pattern from Minimals' File and Banking dashboard homes. | `dashboard/billing` |
| 8 | `PlanCards` | `plans: PlanCardsPlan[]`<br>`value?: string`<br>`defaultValue?: string`<br>`onValueChange?: (value: string) => void`<br>`aria-label: string` — `RadioGroup`'s accessible name — required, since there's no visible group label otherwise.<br>`className?: string` | Plan comparison cards — the pricing-tier selector from Minimals' Account → Billing settings tab (§3.10 SaaS/Billing → "Plan Comparison Cards"). | `dashboard/billing` |
| 9 | `ChartCard` | `title: React.ReactNode`<br>`description?: React.ReactNode`<br>`data: ChartCardDataPoint[]`<br>`className?: string`<br>`height?: number` — Pixel height of the plotting area.<br>extends `\| ({ type: 'bar' } & ChartCardCategorySeriesProps) \| { type: 'donut'; /** The field in each `data` item holding the numeric value to plot. */ valueKey: string; /** The field in each `data` item holding the segment's display name. */ nameKey: string; /** One color per data point, in order — cycles if there are more points than colors. @default ['primary', 'secondary', 'accent', 'info', 'success', 'warning'] */ colors?: ChartCardColor[]; } \| ({ type: 'line' } & ChartCardCategorySeriesProps) \| ({ type: 'area'; /** Stack series on top of each other instead of overlaying them. @default false */ stacked?: boolean; } & ChartCardCategorySeriesProps) \| ({ type: 'radar' } & ChartCardCategorySeriesProps) \| { type: 'gauge'; /** The field in `data`'s first item holding the current value. */ valueKey: string; /** The value that represents a full gauge. @default 100 */ max?: number; /** @default 'primary' */ color?: ChartCardColor; /** Small label under the value, e.g. "of monthly goal". */ valueLabel?: string; }` | A dashboard chart card — bar, line, area, radar, donut, or gauge, all themed entirely off Nebula's own `--color-*` semantic tokens (never a hardcoded hex), so a chart re-themes for free the same way every other component in this repo does. | `dashboard/charts` |
| 10 | `TeamMemberCard` | `avatarSrc?: string`<br>`avatarFallback: string`<br>`name: string`<br>`jobTitle?: React.ReactNode` — e.g. "UI Designer" — named `jobTitle`, not `role`, since `role` as a bare JSX attribute reads as (and gets linted as) the ARIA `role`…<br>`socialLinks?: TeamMemberSocialLink[]`<br>`stats?: TeamMemberStat[]` — e.g. Followers/Following/Posts.<br>`className?: string` | A team-member grid card — cover placeholder + overlapping avatar + name/role + social-platform icon row + stat triple, Minimals' User Cards page (§3.10 Team → *Team Member List*, card variant). | `dashboard/team` |
| 11 | `DashboardOverview` | `metrics: DashboardMetric[]` — No sensible default — every dashboard has its own metrics.<br>`title?: React.ReactNode`<br>`className?: string` | A responsive grid of metric cards — the metrics-grid half of a dashboard "overview" screen. | `dashboard/widgets` |
| 12 | `RankedList` | `title?: React.ReactNode`<br>`items: RankedListItem[]`<br>`className?: string` | A numbered ranking list — "Top products," "Top countries by revenue," the mini-table pattern from Minimals' Ecommerce/Booking dashboard homes (§3.5 Data Display → *Comparison Table* / §3.8 Dashboard → *Activity*, no single exact taxonomy name). | `dashboard/widgets` |
| 13 | `ReviewsList` | `title?: React.ReactNode`<br>`reviews: Review[]`<br>`className?: string` | An avatar + star rating + text reviews list — Minimals' Course/Product review section (§3.12 Blog → *Comment Section* is the closest taxonomy analog, though this is read-only with no reply/compose affordance a comment section would have). | `dashboard/widgets` |
| 14 | `ThumbnailList` | `title?: React.ReactNode`<br>`items: ThumbnailListItem[]`<br>`className?: string` | A thumbnail-led list with a per-item trend indicator — "Best-selling products this week" with a small change badge, the pattern from Minimals' Ecommerce/Course dashboard homes. | `dashboard/widgets` |
| 15 | `WelcomeBanner` | `title: React.ReactNode` — e.g. "Welcome back, Jane 👋" — no sensible default, every app's greeting differs.<br>`description?: React.ReactNode`<br>`action?: WelcomeBannerAction`<br>`illustration?: React.ReactNode` — A decorative graphic rendered on the right — hidden below `md` to keep the banner compact on narrow screens. `aria-hidden` is applied…<br>`className?: string` | A dashboard-home greeting banner — the "welcome back" card that typically sits above the metrics/chart grid on a dashboard's landing screen (the counterpart `Hero`'s marketing-page role plays one vertical over). | `dashboard/widgets` |
| 16 | `DataTableBlock` | `columns: DataTableBlockColumn<TRow>[]`<br>`rows: TRow[]`<br>`getRowId: (row: TRow) => string`<br>`tabs?: DataTableBlockTab[]` — Status filter row (e.g. All/Active/Pending/Banned) — omit entirely for a table with no status concept. Filtering `rows` to the active tab…<br>`activeTab?: string`<br>`defaultActiveTab?: string`<br>`onActiveTabChange?: (value: string) => void`<br>`searchPlaceholder?: string`<br>`searchValue?: string`<br>`onSearchChange?: (value: string) => void`<br>`filters?: React.ReactNode` — Extra domain-specific filter controls (a role `Select`, a date-range picker, ...) rendered after the search field — this block has no…<br>`toolbarActions?: React.ReactNode` — Trailing toolbar controls (Product's "Columns/Filters/Export/Settings" buttons, or a plain overflow menu) — rendered at the toolbar's far…<br>`selectable?: boolean`<br>`selected?: string[]`<br>`defaultSelected?: string[]`<br>`onSelectedChange?: (ids: string[]) => void`<br>`sortKey?: string`<br>`defaultSortKey?: string`<br>`sortDirection?: SortDirection`<br>`defaultSortDirection?: SortDirection`<br>`onSortChange?: (key: string, direction: SortDirection) => void`<br>`rowActions?: (row: TRow) => React.ReactNode` — Returns this row's "⋮" menu content (`MenuItem`s) — omit to skip the actions column entirely.<br>`rowActionsLabel?: (row: TRow) => string` — Accessible label for each row's actions trigger — `row` is passed so it can include the row's own identity, e.g. `(row) => \`Actions for…<br>`renderCard?: (row: TRow) => React.ReactNode` — Renders one row as a card (typically a `CardListItem`) instead of a `<tr>` — when given, the block switches from the table to a stacked…<br>`cardBreakpoint?: string` — The media query at and above which the table renders instead of `renderCard`'s card list — matches Tailwind's `md` breakpoint by default…<br>`page?: number` — Omit `page`/`totalCount` entirely for a table with no pagination footer.<br>`defaultPage?: number`<br>`onPageChange?: (page: number) => void`<br>`pageSize?: number`<br>`defaultPageSize?: number`<br>`pageSizeOptions?: number[]`<br>`onPageSizeChange?: (size: number) => void`<br>`totalCount?: number` — Total row count across every page — required to compute the page count and the "1–10 of 30" range text.<br>`emptyState?: React.ReactNode`<br>`className?: string` | The `BLOCKS_ARCHITECTURE.md` §3.5 "Generic Data Table Block" — status tabs + filter toolbar + sortable/selectable table + pagination footer, composed entirely from `@nebula-lab/react-ui`'s own `DataTable` compound (sort/selection state already lives there) rather than re-implementing any of that. | `data-display/data-table` |
| 17 | `ListingCard` | `media: React.ReactNode` — The leading visual — a logo tile, single image, or a custom mosaic. Left entirely to the consumer since the shape varies a lot by domain (a…<br>`title: React.ReactNode`<br>`subtitle?: React.ReactNode` — e.g. "Posted 2 days ago", or an author avatar + date composed together — accepts any node, not just text.<br>`description?: React.ReactNode` — A short excerpt/summary below the subtitle (e.g. a blog post excerpt). Omit for listings that don't need one (Job, Tour).<br>`meta?: ListingCardMetaItem[]` — Icon+label meta rows below the description (experience level, salary, location, date range, comment/view counts, ...).<br>`mediaBadge?: React.ReactNode` — A badge overlaid on the top-right corner of the media slot (e.g. a price badge).<br>`actions?: React.ReactNode` — `MenuItem`s for the overflow menu — same convention `DataTableBlock`/`PaymentMethodList` use for row actions. Omit to render no overflow…<br>`actionsLabel?: string`<br>`href?: string` — Wraps `title` in an `<a>` when given.<br>`className?: string` | A generalized listing/browse card — company-logo-tile Job listings and photo-mosaic Tour listings from Minimals both reduce to the same shape (a media slot, a title, meta rows, an optional overflow menu), just with very different `media`/`meta` content (§3.23 Real Estate → *Listing Card*, generalized per the… | `data-display/listing-card` |
| 18 | `ProductCard` | `imageSrc: string`<br>`imageAlt: string`<br>`name: React.ReactNode`<br>`price: React.ReactNode` — Pre-formatted, e.g. `"$49.00"` — this block has no currency-formatting opinion.<br>`originalPrice?: React.ReactNode` — Pre-formatted pre-discount price, rendered struck through beside `price` — omit for a product with no discount.<br>`description?: React.ReactNode`<br>`rating?: number` — 0-5 — omit to hide the rating row entirely (e.g. a brand-new product with no reviews yet).<br>`reviewCount?: number`<br>`badge?: { label: React.ReactNode; color?: BadgeProps['color'] }`<br>`favorited?: boolean` — Omit to hide the heart toggle entirely — a read-only product tile with no wishlist concept.<br>`onFavorite?: () => void`<br>`action?: { label?: React.ReactNode; onClick?: () => void }`<br>`className?: string` | A single product tile — image with a hover zoom, a wishlist heart toggle and status badge overlaid on opposite corners, name, price (with an optional struck-through original price for a discount), an optional star-rating row, and a cart CTA. | `ecommerce/product-discovery` |
| 19 | `ProductGallery` | `images: ProductGalleryImage[]`<br>`className?: string` | Product Detail's image gallery (§2.16/§3.7 Product Detail → *Gallery*) — a main `Carousel` view + slide counter + prev/next, plus a thumbnail strip below for direct navigation. | `ecommerce/product-discovery` |
| 20 | `ProductInfoPanel` | `badge?: { label: React.ReactNode; color?: BadgeProps['color'] }`<br>`name: React.ReactNode`<br>`rating?: number` — 0-5. Omit along with `reviewCount` if this product has no reviews yet.<br>`reviewCount?: number`<br>`price: React.ReactNode` — Pre-formatted, e.g. `"$89.00"` — same no-currency-opinion convention `ProductCard.price` uses.<br>`originalPrice?: React.ReactNode` — Pre-formatted strikethrough original price, e.g. for a sale.<br>`description?: React.ReactNode`<br>`colors?: ProductColorOption[]`<br>`color?: string`<br>`defaultColor?: string`<br>`onColorChange?: (value: string) => void`<br>`sizes?: ProductSizeOption[]`<br>`size?: string`<br>`defaultSize?: string`<br>`onSizeChange?: (value: string \| undefined) => void` — `undefined` is possible because `SegmentedControl` is built on a `type="single"` `ToggleGroup`, which allows deselecting the pressed item —…<br>`onSizeChartClick?: () => void`<br>`defaultQuantity?: number`<br>`maxQuantity?: number`<br>`onQuantityChange?: (quantity: number \| undefined) => void`<br>`availability?: React.ReactNode` — e.g. "In stock" / "Only 3 left" — this block has no stock-level opinion, just renders whatever's passed.<br>`primaryAction?: ProductInfoPanelAction` — e.g. "Add to cart" — omit to hide the whole action row (a read-only product view).<br>`secondaryAction?: ProductInfoPanelAction` — e.g. "Buy now", rendered as a secondary outline action beside `primaryAction`.<br>`onCompare?: () => void`<br>`onFavorite?: () => void`<br>`favorited?: boolean`<br>`onShare?: () => void`<br>`className?: string` | Product Detail's info panel (§2.16/§3.7 Product Detail → *Info Panel*, *Size/Variant Selector*) — status badge, title, rating, price, colors, sizes, quantity, availability, and an action row. | `ecommerce/product-discovery` |
| 21 | `EntityFormLayout` | `avatarSrc?: string`<br>`avatarFallback: string` — Required — `AvatarFallback` always needs something to render when there's no image, or while one is loading.<br>`onAvatarChange?: (file: File) => void` — Called with the selected file when the avatar is clicked and a new image is chosen. Omit to make the avatar non-interactive (e.g. a…<br>`helperText?: React.ReactNode` — e.g. "Allowed *.jpeg, *.jpg, *.png max 3.1 MB".<br>`toggles?: EntityFormLayoutToggle[]` — e.g. "Email verified". Rendered as a `Switch` + label row below the avatar.<br>`dangerActions?: EntityFormLayoutDangerAction[]` — e.g. "Ban this user" / "Delete user" — rendered as `danger`-colored ghost buttons at the bottom of the left card.<br>`children: React.ReactNode` — The right card's field grid — entirely left to the consumer, this component only owns the two-column shell and the…<br>`onSubmit?: (event: React.FormEvent<HTMLFormElement>) => void`<br>`submitLabel?: React.ReactNode`<br>`className?: string` | The avatar-card + field-grid entity form shell — Minimals' User Create/Edit page, generalized to any entity (§3.14 *Account Profile Form*, the same shell this session's inventory also flags as reused unchanged for the Account → General settings tab). | `forms/entity-form-layout` |
| 22 | `TransactionForm` | `onSubmit?: (values: TransactionFormValues) => void`<br>`isSubmitting?: boolean`<br>`error?: string`<br>`categories: string[]` — Category options — domain-neutral on purpose (see this component's doc comment): pass whatever categories your app uses (expense-tracker…<br>`title?: React.ReactNode`<br>`submitLabel?: React.ReactNode`<br>`className?: string` | A generic income/expense entry form — named "Transaction" rather than anything ecommerce-specific (no "Order"/"Payment"/"Checkout") since this package is deliberately domain-neutral (see its own README): the same shape serves an expense-tracker PWA, a personal-budget app, or a simple internal ledger tool equally well,… | `forms/transaction-form` |
| 23 | `Hero` | `eyebrow?: React.ReactNode`<br>`headline: React.ReactNode` — No sensible default copy — this is the one thing every hero needs.<br>`subheadline?: React.ReactNode`<br>`primaryAction?: HeroAction`<br>`secondaryAction?: HeroAction` — Rendered with `variant="text"` to visually subordinate it to `primaryAction`.<br>`media?: React.ReactNode` — Optional right-column visual (an `Image`, illustration, or any node) — the layout collapses to a single centered column when omitted.<br>`className?: string` | A marketing landing-page hero — headline/subheadline/CTA(s), with an optional media slot that switches the layout from a single centered column to a two-column split once supplied. | `marketing/hero` |
| 24 | `PromoBanner` | `logo?: React.ReactNode` — Small brand mark above the headline — a wordmark `Text`, an `Image`, or any node.<br>`headline: React.ReactNode` — No sensible default copy — this is the one thing every banner needs.<br>`subheadline?: React.ReactNode`<br>`media?: React.ReactNode` — A decorative image/illustration anchored to the trailing edge, over the color curve — `aria-hidden` applied automatically.<br>`badge?: React.ReactNode` — Small corner label, e.g. `"Ad"` — omit for a purely editorial promo.<br>`color?: PromoBannerColor` — Tints the curved background shape.<br>`className?: string` | A single promo/ad-style banner card — headline/subheadline over a light surface, a curved color-block shape sweeping in from the trailing edge (an SVG path with `preserveAspectRatio="none"`, so it stretches to fill any card size/aspect ratio rather than needing per-breakpoint tuning), and an optional trailing media… | `marketing/promo-banner` |
| 25 | `PromoBannerCarousel` | `banners: PromoBannerProps[]`<br>`index?: number` — Controlled: the current (0-indexed) banner.<br>`defaultIndex?: number` — Uncontrolled: the initial banner.<br>`onIndexChange?: (index: number) => void`<br>`loop?: boolean` — Whether swiping/scrolling past the last banner wraps to the first.<br>`className?: string`<br>`aria-label?: string` | One promo banner on screen at a time, navigated by swipe (touch/pointer drag) or trackpad/wheel scroll — no `CarouselPrevious`/`CarouselNext` buttons, matching the reference "story-style" ad slider this was built from: just the card, a dot trail, and a gesture. | `marketing/promo-banner` |
| 26 | `SaasAppHeader` | `logo?: React.ReactNode` — An icon/image mark rendered left of `brand` — decorative, `aria-hidden`; the accessible name comes from `brand`.<br>`brand: React.ReactNode` — A string is wrapped in a `Heading`; any other node (e.g. a custom brand mark) passes through untouched.<br>`brandHref?: string`<br>`navLinks?: AppHeaderNavLink[]`<br>`notifications?: AppHeaderNotification[]` — Omit entirely to hide the notification bell; pass `[]` to show the bell with an empty-state panel.<br>`onNotificationClick?: (id: string) => void`<br>`onMarkAllNotificationsRead?: () => void`<br>`user?: { name: string; role?: React.ReactNode; avatarSrc?: string }` — Omitting `avatarSrc` falls back to `AvatarFallback` showing `name`'s first letter.<br>`userMenuItems?: AppHeaderUserMenuItem[]`<br>`glass?: boolean` — Starts fully transparent (no border, no background) and gains a frosted `backdrop-blur` + semi-transparent surface once the page scrolls…<br>`className?: string` | A modern SaaS application header: logo + brand + primary nav (desktop: `NavigationMenu` with optional dropdown sub-menus; mobile: a `Sheet` slide- over, `md:hidden`/`hidden md:block` split, same breakpoint convention `BottomNav` uses) on the left, a notification bell (`Popover` panel, unread-count pulse dot) and a… | `navigation/headers` |
| 27 | `ProfileHeader` | `coverSrc?: string`<br>`avatarSrc?: string`<br>`avatarFallback: string`<br>`name: string`<br>`jobTitle?: React.ReactNode` — e.g. "UI Designer" — named `jobTitle`, not `role`, since `role` as a bare JSX attribute reads as (and gets linted as) the ARIA `role`…<br>`stats?: ProfileHeaderStat[]` — e.g. Followers/Following counts.<br>`tabs?: ProfileHeaderTab[]` — Each tab's `content` renders in a real `TabPanel` — a `role="tab"` always needs one WAI-ARIA-correct `aria-controls` target, so this never…<br>`activeTab?: string`<br>`defaultActiveTab?: string`<br>`onActiveTabChange?: (value: string) => void`<br>`className?: string` | A social profile header — cover banner + overlapping avatar + name/role + stat pair + section tabs, Minimals' User Profile page (§3.13 Social → *Profile Header*, *Profile Tabs*). | `social/profile-header` |

