# `@nebula-lab/headless` — API Reference

Generated from the actual TypeScript source (real exported prop/parameter names and types, not hand-transcribed) — regenerate with `node scripts/generate-api-docs.mjs` after adding/changing a component so this doesn't drift. See `ARCHITECTURE.md` at the repo root for how this package fits into the overall layering, and this package's own `README.md` for install/usage.

Cross-package prop types (e.g. a `react-ui` component's Props extending a `styleless` one) are shown as `extends `Type` (from `@nebula-lab/other-package`)` rather than flattened in place — look up that type in the other package's own API_DOC.md. Inherited native DOM attributes (from `PrimitivePropsWithRef`/`PolymorphicComponentPropsWithRef`) are summarized as a single note rather than enumerated, since they're just the target element's standard attributes.

---

## `@nebula-lab/headless`

Behavior-only, ARIA-complete compound components — no styling opinions.

| SN | Component | Props | Usage notes | Comments |
|---|---|---|---|---|
| 1 | `Accordion` | extends `AccordionSingleProps \| AccordionMultipleProps` | Root of the Accordion compound component. |  |
| 2 | `AccordionItem` | `value: string` — Identifies this item within its `Accordion` — must be unique within the group.<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'div'>` | Wraps one header+content pair. |  |
| 3 | `AccordionHeader` | Inherits all standard `'h3'` attributes. | Wraps `AccordionTrigger` in a heading, per the WAI-ARIA Accordion pattern ("the title of each accordion header is contained in an element with role `heading`"). |  |
| 4 | `AccordionTrigger` | Inherits all standard `'button'` attributes. | `role="button"` (native, via the `button` tag) with `aria-expanded`/ `aria-controls` wired to this item's panel. |  |
| 5 | `AccordionContent` | `forceMount?: boolean` — Keep the panel mounted (but `hidden`) while collapsed instead of unmounting it — same escape hatch as `TabPanel`'s `forceMount`, useful to…<br>extends `PrimitivePropsWithRef<'div'>` | `role="region"`, labelled by its `AccordionTrigger`. |  |
| 6 | `Breadcrumb` | Inherits all standard `'nav'` attributes. | `<nav aria-label="breadcrumb">` — the WAI-ARIA breadcrumb pattern's landmark wrapper. |  |
| 7 | `BreadcrumbList` | Inherits all standard `'ol'` attributes. | `<ol>` — an ordered list, since a breadcrumb trail has a meaningful sequence (the WAI-ARIA breadcrumb pattern specifically recommends a list, ordered over unordered, for that reason). |  |
| 8 | `BreadcrumbItem` | Inherits all standard `'li'` attributes. | `<li>` — one step in the trail, wrapping either a `BreadcrumbLink` (a past/ancestor page) or a `BreadcrumbPage` (the current one). |  |
| 9 | `BreadcrumbLink` | Inherits all standard `'a'` attributes. | `<a>` — a past/ancestor page in the trail, a real link (not the current page — that's `BreadcrumbPage`, which is deliberately not a link at all). |  |
| 10 | `BreadcrumbPage` | Inherits all standard `'span'` attributes. | `<span role="link" aria-disabled="true" aria-current="page">` — the current page in the trail. |  |
| 11 | `BreadcrumbSeparator` | Inherits all standard `'li'` attributes. | `<li role="presentation" aria-hidden="true">` — the visual divider between two `BreadcrumbItem`s (a "/" or a chevron icon, supplied via `children`; this component renders none by default). |  |
| 12 | `Checkbox` | `checked?: CheckedState` — Controlled: `true`/`false`/`'indeterminate'` (a third, non-interactive visual state).<br>`defaultChecked?: CheckedState` — Uncontrolled initial state.<br>`onCheckedChange?: (checked: CheckedState) => void`<br>`disabled?: boolean`<br>`name?: string` — Forwarded to a hidden native `<input type="checkbox">` so this participates in native `<form>` submission.<br>`value?: string`<br>`required?: boolean`<br>extends `Omit<PrimitivePropsWithRef<'button'>, 'checked' \| 'defaultChecked'>` | `role="checkbox"` — a `button`, not a native `<input type="checkbox">`, so `@nebula-lab/react-ui` can fully style the check mark instead of fighting browser-native checkbox rendering. |  |
| 13 | `CheckboxGroup` | `value?: string[]` — Controlled: the set of currently checked items' values.<br>`defaultValue?: string[]`<br>`onValueChange?: (value: string[]) => void`<br>`disabled?: boolean`<br>`name?: string` — Forwarded to every item's hidden native checkbox input for `<form>` submission.<br>extends `PrimitivePropsWithRef<'div'>` | `role="group"` over a set of {@link CheckboxGroupItem}s — unlike `RadioGroup`/`ToggleGroup`, there's no roving-tabindex here: native checkboxes in a group are each independently `Tab`-reachable (there's no "arrow keys move between them" convention the way radio buttons have), so this doesn't wrap… |  |
| 14 | `CheckboxGroupItem` | `value: string` — Identifies this item within its `CheckboxGroup` — must be unique within the group.<br>extends `Omit<CheckboxProps, 'checked' \| 'defaultChecked' \| 'onCheckedChange' \| 'name'>` | A {@link Checkbox} whose checked state comes entirely from `CheckboxGroup`'s context (whether this item's `value` is in the group's checked set) rather than local state — same relationship `ToggleGroupItem` has to `Toggle`. |  |
| 15 | `Collapsible` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'div'>` | Root of the Collapsible compound component — a single expand/collapse panel, independent of any group semantics (unlike `Accordion`, which is effectively several of these sharing an "at most one/many open" rule via its own root). |  |
| 16 | `CollapsibleTrigger` | Inherits all standard `'button'` attributes. | `aria-expanded`/`aria-controls` wired to `CollapsibleContent`'s `id`, same disclosure pattern `AccordionTrigger` follows — native `<button>` gives correct keyboard/click activation for free, so unlike `AccordionTrigger` this doesn't need `FocusItem`/roving-tabindex at all: a standalone `Collapsible` isn't one of… |  |
| 17 | `CollapsibleContent` | `forceMount?: boolean` — Keep mounted while collapsed instead of unmounting — same escape hatch as `AccordionContent`/`TabPanel`'s `forceMount`.<br>extends `PrimitivePropsWithRef<'div'>` | Wrapped in `@nebula-lab/primitives`' `Presence` (not a plain conditional unmount) so consumers can animate expand/collapse via CSS gated on `data-state="open"\|"closed"` — same reasoning `AccordionContent` documents, since this is effectively the single-panel version of it. |  |
| 18 | `ColorPicker` | `value?: string` — A `#rgb`/`#rrggbb` hex string.<br>`defaultValue?: string`<br>`onValueChange?: (value: string) => void`<br>`open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`disabled?: boolean`<br>`children?: React.ReactNode` | Root of the ColorPicker compound component — a swatch button (`ColorPickerTrigger`) that opens a `Popover` (reused directly, the same "mint one ambient instance" reuse `MenubarMenu` makes of `Menu`) containing whatever channel controls the consumer composes, plus a built-in `ColorPickerHexInput` for direct hex entry. |  |
| 19 | `ColorPickerTrigger` | Inherits all standard `'button'` attributes. | A swatch button (background color set to the current `value`) that opens `ColorPickerContent` — built on `PopoverTrigger`, so it inherits `aria-haspopup`/`aria-expanded`/`aria-controls` and toggle-on-click for free. |  |
| 20 | `ColorPickerPortal` | — | (no doc comment found) |  |
| 21 | `ColorPickerContent` | extends `PopoverContentProps` | Thin wrapper over `PopoverContent` — no ColorPicker-specific behavior on top, just a renamed entry point so consumers reach for `ColorPickerContent` rather than an unrelated-looking `PopoverContent` import. |  |
| 22 | `ColorPickerHexInput` | extends `Omit<InputProps, 'value' \| 'defaultValue' \| 'type'>` | A real, validated text `<input>` for typing a hex color directly — deliberately kept as a genuine text field rather than a custom canvas-based picker, so it's fully keyboard/screen-reader accessible on its own even before any visual channel controls are added. |  |
| 23 | `Autocomplete` | — | Thin renamed re-export of {@link Combobox} — `Combobox` was already built around free-text entry that doesn't require matching any option (typing `inputValue` and the eventually-`onValueChange`d selection are two separate, decoupled pieces of state), which is exactly what "Autocomplete" means in most component-library… | `combobox` |
| 24 | `AutocompleteInput` | — | (no doc comment found) | `combobox` |
| 25 | `AutocompletePortal` | — | (no doc comment found) | `combobox` |
| 26 | `AutocompleteContent` | — | (no doc comment found) | `combobox` |
| 27 | `AutocompleteItem` | — | (no doc comment found) | `combobox` |
| 28 | `Combobox` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`value?: string`<br>`defaultValue?: string`<br>`onValueChange?: (value: string) => void`<br>`inputValue?: string`<br>`defaultInputValue?: string`<br>`onInputValueChange?: (value: string) => void`<br>`disabled?: boolean`<br>`children?: React.ReactNode` | Root of the Combobox compound component — the WAI-ARIA [editable combobox with list autocomplete pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/examples/combobox-autocomplete-list/): a text `ComboboxInput` (`role="combobox"`, `aria-autocomplete="list"`) whose typed text filters an anchor-positioned… | `combobox` |
| 29 | `ComboboxInput` | extends `Omit<InputProps, 'value' \| 'defaultValue' \| 'role'>` | `role="combobox"` on the real text input — per the WAI-ARIA combobox pattern, this is where `aria-expanded`/`aria-controls`/`aria-activedescendant` all live (not on some other trigger element). | `combobox` |
| 30 | `ComboboxPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `SelectPortal`/`PopoverPortal`. | `combobox` |
| 31 | `ComboboxContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean`<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="listbox"`, anchor-positioned against `ComboboxInput` via `@nebula-lab/primitives`' `PopperContent`. | `combobox` |
| 32 | `ComboboxItem` | `value: string`<br>`disabled?: boolean`<br>`textValue?: string` — Overrides the label used for `ComboboxInput`'s display text on selection — only needed when `children` isn't plain text.<br>extends `PrimitivePropsWithRef<'div'>` | `role="option"` — highlighted state (`aria-selected`, `data-highlighted`) comes from `Combobox`'s `highlightedValue`, not local state or real DOM focus (real focus always stays on `ComboboxInput`; see that file's doc comment). | `combobox` |
| 33 | `Command` | `inputValue?: string`<br>`defaultInputValue?: string`<br>`onInputValueChange?: (value: string) => void`<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'div'>` | Root of the Command compound component — the WAI-ARIA [combobox-with-list pattern](https://www.w3.org/WAI/ARIA/apg/patterns/combobox/), but always "open": unlike `Combobox`, there's no popup to show/hide — `CommandInput` and `CommandList` are always both visible, which is the whole "command palette" shape (usually… |  |
| 34 | `CommandInput` | extends `Omit<InputProps, 'value' \| 'defaultValue' \| 'role'>` | `role="combobox"`, always `aria-expanded="true"` — `CommandList` is never hidden the way `ComboboxContent` is, so unlike `ComboboxInput` there's no open/close state to manage here at all. |  |
| 35 | `CommandList` | Inherits all standard `'div'` attributes. | `role="listbox"` — always mounted and visible (unlike `ComboboxContent`, there's no `Presence`/open-state gating here, since `Command` has no concept of "closed"). |  |
| 36 | `CommandItem` | `value: string`<br>`disabled?: boolean`<br>`onSelect?: () => void` — Runs when this item is chosen — by click, or by Enter while it's `highlightedValue` (see `CommandInput`). Unlike `ComboboxItem`'s…<br>extends `PrimitivePropsWithRef<'div'>` | `role="option"` — highlighted state comes entirely from `Command`'s `highlightedValue`, not local state or real DOM focus (focus always stays on `CommandInput`, same as `ComboboxItem`). |  |
| 37 | `CommandGroup` | `heading?: React.ReactNode` — Rendered as the group's visible label, and wired to `aria-labelledby` — takes a plain heading prop rather than a separate…<br>extends `PrimitivePropsWithRef<'div'>` | `role="group"` wrapping a labeled cluster of `CommandItem`s, e.g. all the "Actions" or all the "Recent files" together — purely organizational, no effect on `CommandInput`'s arrow-key navigation (which always just walks every visible `[role="option"]` regardless of which group it's in). |  |
| 38 | `CommandEmpty` | Inherits all standard `'div'` attributes. | "No results" state — rendered by the consumer when their own filtering left zero `CommandItem`s, same "consumer controls it, nothing automatic" convention `Combobox` leaves filtering to. |  |
| 39 | `CommandSeparator` | Inherits all standard `'div'` attributes. | A decorative divider between `CommandGroup`s — `aria-hidden`, same treatment `BreadcrumbSeparator`/`PaginationEllipsis` get, since group order alone already conveys the structure to assistive tech. |  |
| 40 | `AlertDialog` | extends `Omit<DialogProps, 'modal'>` | Root of the AlertDialog compound component — a thin wrapper around `Dialog`'s root (reusing its context/scope directly, see `alert-dialog-content.tsx`) that forces `modal={true}` unconditionally: an alert dialog that didn't trap focus would let a user tab past an unanswered prompt, defeating the whole point of… | `dialog` |
| 41 | `AlertDialogTrigger` | — | (no doc comment found) | `dialog` |
| 42 | `AlertDialogPortal` | — | (no doc comment found) | `dialog` |
| 43 | `AlertDialogOverlay` | — | (no doc comment found) | `dialog` |
| 44 | `AlertDialogContent` | `forceMount?: boolean` — Keep mounted while closed instead of unmounting — same escape hatch as `DialogContent`'s `forceMount`.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop Escape from closing the dialog.<br>extends `PrimitivePropsWithRef<'div'>` | `role="alertdialog"` — the WAI-ARIA Alert Dialog pattern, a `Dialog` variant for interruptions that demand an explicit response (confirming a destructive action, a blocking error) rather than being casually dismissed. | `dialog` |
| 45 | `AlertDialogTitle` | — | (no doc comment found) | `dialog` |
| 46 | `AlertDialogDescription` | — | (no doc comment found) | `dialog` |
| 47 | `AlertDialogCancel` | — | (no doc comment found) | `dialog` |
| 48 | `AlertDialogAction` | — | (no doc comment found) | `dialog` |
| 49 | `Dialog` | `open?: boolean` — Controlled open state.<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`modal?: boolean` — Modal dialogs trap Tab/Shift+Tab within `DialogContent` (via `FocusScope` `trapped`) and the WAI-ARIA pattern's `aria-modal="true"`.…<br>`children?: React.ReactNode` | Root of the Dialog compound component — renders no DOM of its own (like `DialogTrigger` and `DialogPortal`'s children are meant to be siblings, not nested inside an extra wrapper element), just provides shared state via scoped context. | `dialog` |
| 50 | `DialogTrigger` | Inherits all standard `'button'` attributes. | Opens the dialog on click. | `dialog` |
| 51 | `DialogPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal`, so consumers of `@nebula-lab/headless/dialog` don't need a second import for it. | `dialog` |
| 52 | `DialogOverlay` | `forceMount?: boolean` — Keep mounted while closed instead of unmounting — same escape hatch as `AccordionContent`/`TabPanel`'s `forceMount`.<br>extends `OverlayProps` | The dimmed backdrop behind `DialogContent`, rendered inside `DialogPortal`. | `dialog` |
| 53 | `DialogContent` | `forceMount?: boolean` — Keep mounted while closed instead of unmounting — same escape hatch as `DialogOverlay`'s `forceMount`.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop Escape from closing the dialog.<br>`onPointerDownOutside?: (event: PointerEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop an outside click (including on `DialogOverlay`) from closing the…<br>extends `PrimitivePropsWithRef<'div'>` | `role="dialog"`, rendered inside `DialogPortal`. | `dialog` |
| 54 | `DialogTitle` | Inherits all standard `'h2'` attributes. | The dialog's accessible name — its `id` is what `DialogContent`'s `aria-labelledby` points to. | `dialog` |
| 55 | `DialogDescription` | Inherits all standard `'p'` attributes. | Optional supporting text — its `id` is what `DialogContent`'s `aria-describedby` points to. | `dialog` |
| 56 | `DialogClose` | Inherits all standard `'button'` attributes. | Closes the dialog on click. | `dialog` |
| 57 | `Drawer` | extends `DialogProps` | Root of the Drawer compound component — a thin re-export of `Dialog`'s root (reusing its context/scope directly, same technique `AlertDialog` uses): a drawer/"sheet" is behaviorally identical to a dialog (modal by default, traps focus, Escape/outside-click dismisses) — the only difference is *where* the panel sits and… | `dialog` |
| 58 | `DrawerTrigger` | — | (no doc comment found) | `dialog` |
| 59 | `DrawerPortal` | — | (no doc comment found) | `dialog` |
| 60 | `DrawerOverlay` | — | (no doc comment found) | `dialog` |
| 61 | `DrawerContent` | `side?: DrawerSide` — Which edge of the viewport the panel is anchored to — exposed as `data-side` for the consumer's own CSS to position/animate from (a…<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting — same escape hatch as `DialogContent`'s `forceMount`.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop Escape from closing the drawer.<br>`onPointerDownOutside?: (event: PointerEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop an outside click from closing the drawer.<br>extends `PrimitivePropsWithRef<'div'>` | `role="dialog"`, rendered inside `DrawerPortal` — identical composition to `DialogContent` (`Presence` > `DismissibleLayer` > `FocusScope` via `asChild` chaining, `trapped` following the root's `modal`), reusing `Dialog`'s context directly (same cross-folder relative-import pattern `AlertDialogContent` uses). | `dialog` |
| 62 | `DrawerTitle` | — | (no doc comment found) | `dialog` |
| 63 | `DrawerDescription` | — | (no doc comment found) | `dialog` |
| 64 | `DrawerClose` | — | (no doc comment found) | `dialog` |
| 65 | `Field` | `invalid?: boolean`<br>`disabled?: boolean`<br>`required?: boolean`<br>`id?: string` — Overrides the auto-generated id shared by every sub-part (`FieldLabel`'s `htmlFor`, `FieldControl`'s `id`,…<br>extends `PrimitivePropsWithRef<'div'>` | Root of the Field compound component — wires a control (`FieldControl`) to its `FieldLabel`/`FieldDescription`/`FieldError` via generated, shared ids, the same `label`-`for`/`aria-describedby`/`aria-invalid` plumbing every accessible form field needs, factored out once instead of hand-wired per field. |  |
| 66 | `FieldLabel` | — | `Field`'s label — `htmlFor` and `required` both default from `Field`'s context, so they never need to be repeated here in the common case (pass either explicitly to override just this label). |  |
| 67 | `FieldControl` | `asChild?: boolean`<br>extends `PrimitivePropsWithRef<'input'>` | Wraps the actual form control (an `Input`/`Textarea`/custom widget passed via `asChild`, since a control's tag varies far more than e.g. a `DialogTrigger`'s always-a-button shape) and injects the `id`/`aria-*` wiring `Field`'s context computed: `id`, `aria-invalid`, `aria-required`, `aria-describedby`, and `disabled`… |  |
| 68 | `FieldDescription` | — | Helper text for a `Field` — rendered at `Field`'s `descriptionId` and registers that id with `Field` (via `useLayoutEffect`) so `FieldControl`'s `aria-describedby` includes it only while this is actually mounted — see `Field`'s own doc comment for why this can't be a static, unconditional id. |  |
| 69 | `FieldError` | — | Validation message for a `Field` — `role="alert"` so assistive tech announces it the moment it mounts (e.g. right after a failed submit), rendered at `Field`'s `errorId` and registers that id with `Field` (via `useLayoutEffect`) so `FieldControl`'s `aria-describedby` includes it only while this is actually mounted —… |  |
| 70 | `FileUpload` | `files?: File[]`<br>`defaultFiles?: File[]`<br>`onFilesChange?: (files: File[]) => void`<br>`accept?: string` — Forwarded to the underlying native `<input type="file" accept>`.<br>`multiple?: boolean`<br>`disabled?: boolean`<br>`children?: React.ReactNode` | Root of the FileUpload compound component — a drag-and-drop zone (`FileUploadDropzone`) wrapping a real native `<input type="file">` (`FileUploadInput`), so click-to-open and full keyboard/screen-reader operability come from the browser's own `<label>`-wraps-`<input>` behavior rather than a hand-rolled `role="button"`… |  |
| 71 | `FileUploadDropzone` | Inherits all standard `'label'` attributes. | Renders a `<label>` (not a `div`/`button`) wrapping `FileUploadInput` — clicking anywhere in it opens the native file picker via the browser's own label-activates-its-control behavior, no click handler or `role="button"` needed for that part. |  |
| 72 | `FileUploadInput` | extends `Omit<InputProps, 'type' \| 'accept' \| 'multiple' \| 'value'>` | The real native `<input type="file">` — visually hidden (via `VisuallyHidden`, not `display: none`, so it stays focusable/operable for keyboard and screen-reader users), nested inside `FileUploadDropzone`'s `<label>`. |  |
| 73 | `FileUploadFileList` | Inherits all standard `'ul'` attributes. | `role="list"` (via the native `<ul>` it renders as) wrapping {@link FileUploadFileItem}s — purely structural, no state; map over `FileUpload`'s `files` yourself and render one `FileUploadFileItem` per entry. |  |
| 74 | `FileUploadFileItem` | `file: File` — The file this row represents — not read internally, just typed here so consumers reliably pass one (e.g. for a future thumbnail/size…<br>extends `PrimitivePropsWithRef<'li'>` | `role="listitem"` (via the native `<li>` it renders as) for one selected file — purely structural; render whatever you want inside (name, size, a thumbnail, a `FileUploadRemoveTrigger`). |  |
| 75 | `FileUploadRemoveTrigger` | `index: number` — The index (within `FileUpload`'s `files`) to remove on click.<br>extends `PrimitivePropsWithRef<'button'>` | Removes one file from `FileUpload`'s list by index. |  |
| 76 | `HoverCard` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`openDelay?: number` — Hover/focus delay (ms) before opening.<br>`closeDelay?: number` — Delay (ms) before closing once the pointer leaves *both* the trigger and the content — unlike `Tooltip`, closing isn't instant, since the…<br>`children?: React.ReactNode` | Root of the HoverCard compound component. |  |
| 77 | `HoverCardTrigger` | Inherits all standard `'a'` attributes. | Renders as an `<a>` by default — the classic hover-card use case is previewing what a link (a username, a linked reference) points to before committing to navigate. |  |
| 78 | `HoverCardPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `PopoverPortal`. |  |
| 79 | `HoverCardContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void`<br>`onPointerDownOutside?: (event: PointerEvent) => void`<br>`onPointerEnter?: React.PointerEventHandler<HTMLDivElement>`<br>`onPointerLeave?: React.PointerEventHandler<HTMLDivElement>`<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="dialog"`, non-modal — same `Presence` > `DismissibleLayer` > `FocusScope` (`trapped={false}`) > `PopperContent` collapsing `PopoverContent` uses. |  |
| 80 | `Listbox` | extends `ListboxSingleProps \| ListboxMultipleProps` | Root of the Listbox compound component — `role="listbox"` over a set of {@link ListboxOption}s, the WAI-ARIA [Listbox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/). |  |
| 81 | `ListboxOption` | `value: string` — Identifies this option within its `Listbox` — must be unique within the list.<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'div'>` | `role="option"`. |  |
| 82 | `Menubar` | `value?: string`<br>`defaultValue?: string`<br>`onValueChange?: (value: string \| undefined) => void`<br>`className?: string` — Forwarded (along with the ref) to the root `RovingFocusGroup` element — added so a consumer (e.g. `react-ui`'s styled wrapper) can lay the…<br>`children?: React.ReactNode` | `role="menubar"` — a horizontal row of `MenubarMenu`s (each its own `Menu`) that share one rule: at most one open at a time. |  |
| 83 | `MenubarMenu` | `value: string` — Identifies this menu among its `Menubar` siblings — matched against `Menubar`'s `value` to decide whether this one is open.<br>`children?: React.ReactNode` | One menu within a `Menubar` — mints an ordinary (unscoped) `Menu` whose `open` state is derived from whether `Menubar`'s `value` matches this menu's own `value`, rather than managing its own independent open state. |  |
| 84 | `MenubarTrigger` | Inherits all standard `'button'` attributes. | One menu's trigger button within a `Menubar`. |  |
| 85 | `MenubarPortal` | — | (no doc comment found) |  |
| 86 | `MenubarContent` | — | (no doc comment found) |  |
| 87 | `MenubarItem` | — | (no doc comment found) |  |
| 88 | `MenubarCheckboxItem` | — | (no doc comment found) |  |
| 89 | `MenubarRadioGroup` | — | (no doc comment found) |  |
| 90 | `MenubarRadioItem` | — | (no doc comment found) |  |
| 91 | `MenubarSeparator` | — | (no doc comment found) |  |
| 92 | `MenubarLabel` | — | (no doc comment found) |  |
| 93 | `ContextMenu` | — | (no doc comment found) | `menu` |
| 94 | `ContextMenuTrigger` | Inherits all standard `'div'` attributes. | Wraps arbitrary content (a table row, a canvas, a card — anything the user right-clicks on) and opens the menu positioned at the pointer, instead of `MenuTrigger`'s click-a-visible-button model. | `menu` |
| 95 | `ContextMenuPortal` | — | (no doc comment found) | `menu` |
| 96 | `ContextMenuContent` | — | (no doc comment found) | `menu` |
| 97 | `ContextMenuItem` | — | (no doc comment found) | `menu` |
| 98 | `ContextMenuCheckboxItem` | — | (no doc comment found) | `menu` |
| 99 | `ContextMenuRadioGroup` | — | (no doc comment found) | `menu` |
| 100 | `ContextMenuRadioItem` | — | (no doc comment found) | `menu` |
| 101 | `ContextMenuSeparator` | — | (no doc comment found) | `menu` |
| 102 | `ContextMenuLabel` | — | (no doc comment found) | `menu` |
| 103 | `DropdownMenu` | — | (no doc comment found) | `menu` |
| 104 | `DropdownMenuTrigger` | — | (no doc comment found) | `menu` |
| 105 | `DropdownMenuPortal` | — | (no doc comment found) | `menu` |
| 106 | `DropdownMenuContent` | — | (no doc comment found) | `menu` |
| 107 | `DropdownMenuItem` | — | (no doc comment found) | `menu` |
| 108 | `DropdownMenuCheckboxItem` | — | (no doc comment found) | `menu` |
| 109 | `DropdownMenuRadioGroup` | — | (no doc comment found) | `menu` |
| 110 | `DropdownMenuRadioItem` | — | (no doc comment found) | `menu` |
| 111 | `DropdownMenuSeparator` | — | (no doc comment found) | `menu` |
| 112 | `DropdownMenuLabel` | — | (no doc comment found) | `menu` |
| 113 | `Menu` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`children?: React.ReactNode` | Root of the Menu compound component — the shared foundation `DropdownMenu`, `ContextMenu`, and `Menubar` are all built on (same relationship `Popper` has to `Popover`/`Tooltip`: one implementation, several triggers/contexts on top of it). | `menu` |
| 114 | `MenuTrigger` | Inherits all standard `'button'` attributes. | Toggles the menu open/closed on click; `ArrowDown`/`ArrowUp` open it too (both focus the first item on open — see `menu.tsx`'s header comment on why `ArrowUp` doesn't focus the last item here). | `menu` |
| 115 | `MenuPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `PopoverPortal`/`TooltipPortal`. | `menu` |
| 116 | `MenuContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop Escape from closing the menu.<br>`onPointerDownOutside?: (event: PointerEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop an outside click from closing the menu.<br>`onKeyDown?: React.KeyboardEventHandler<HTMLDivElement>`<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="menu"`, positioned against `MenuTrigger`'s anchor via `@nebula-lab/primitives`' `PopperContent`. | `menu` |
| 117 | `MenuItem` | `disabled?: boolean`<br>`onSelect?: (event: Event) => void` — Called on activation (click, or Enter/Space while focused). Call `event.preventDefault()` to stop the menu from closing afterward — the…<br>extends `Omit<PrimitivePropsWithRef<'div'>, 'onSelect'>` | `role="menuitem"`. | `menu` |
| 118 | `MenuCheckboxItem` | `checked?: boolean`<br>`onCheckedChange?: (checked: boolean) => void`<br>extends `MenuItemProps` | `role="menuitemcheckbox"` — a `MenuItem` that also toggles a boolean on selection. | `menu` |
| 119 | `MenuRadioGroup` | `value?: string`<br>`defaultValue?: string`<br>`onValueChange?: (value: string) => void`<br>`children?: React.ReactNode` | Groups `MenuRadioItem`s into a single-select set (`role="group"`, no additional ARIA role of its own beyond that — the individual items carry `role="menuitemradio"`). | `menu` |
| 120 | `MenuRadioItem` | `value: string` — Identifies this item within its `MenuRadioGroup` — must be unique within the group.<br>extends `MenuItemProps` | `role="menuitemradio"` — composes `MenuItem` the same way `MenuCheckboxItem` does. | `menu` |
| 121 | `MenuSeparator` | Inherits all standard `'div'` attributes. | A non-interactive divider between groups of `MenuItem`s. | `menu` |
| 122 | `MenuLabel` | Inherits all standard `'div'` attributes. | A non-interactive heading above a group of related `MenuItem`s (e.g. "Actions"). | `menu` |
| 123 | `NavigationMenu` | `value?: string` — Controlled: which `NavigationMenuItem`'s `value` currently has its content open.<br>`defaultValue?: string`<br>`onValueChange?: (value: string \| undefined) => void`<br>`openDelay?: number`<br>`closeDelay?: number`<br>extends `PrimitivePropsWithRef<'nav'>` | Root of the NavigationMenu compound component — a `<nav>` wrapping `NavigationMenuList`/`NavigationMenuItem`s. |  |
| 124 | `NavigationMenuList` | Inherits all standard `'ul'` attributes. | `<ul>` wrapper for `NavigationMenuItem`s, wrapped in `@nebula-lab/primitives`' `RovingFocusGroup` (horizontal, looping) so Left/Right arrow keys move focus between sibling triggers/links — same technique `Menubar` uses for its top-level triggers. |  |
| 125 | `NavigationMenuItem` | `value?: string` — Identifies this item among its `NavigationMenuList` siblings — matched against `NavigationMenu`'s `value` to decide whether this one's…<br>`children?: React.ReactNode` | One item within a `NavigationMenuList`. |  |
| 126 | `NavigationMenuTrigger` | Inherits all standard `'button'` attributes. | Opens this item's `NavigationMenuContent` on hover (after `openDelay`) — or immediately, with no delay, if some other item's content in this same `NavigationMenu` is already open (matching a very common mega-menu expectation: once you're "in" the menu, moving across the bar shouldn't feel laggy). |  |
| 127 | `NavigationMenuPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `PopoverPortal`/`DialogPortal`. |  |
| 128 | `NavigationMenuContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void`<br>`onPointerDownOutside?: (event: PointerEvent) => void`<br>`onPointerEnter?: React.PointerEventHandler<HTMLDivElement>`<br>`onPointerLeave?: React.PointerEventHandler<HTMLDivElement>`<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="dialog"`, non-modal — same `Presence` > `DismissibleLayer` > `FocusScope` > `PopperContent` collapsing `PopoverContent` uses. |  |
| 129 | `NavigationMenuLink` | `active?: boolean` — Whether this link represents the current page — sets `aria-current="page"`, per the same convention `BreadcrumbPage`/`PaginationLink` use.<br>extends `PrimitivePropsWithRef<'a'>` | A plain top-level nav link with no dropdown content — the counterpart to a `NavigationMenuTrigger` + `NavigationMenuContent` pair, for items like `NavigationMenuItem value="docs"`'s single "Docs" link that just navigates directly. |  |
| 130 | `NumberInput` | `value?: number`<br>`defaultValue?: number`<br>`onValueChange?: (value: number \| undefined) => void`<br>`min?: number`<br>`max?: number`<br>`step?: number`<br>`disabled?: boolean`<br>`children?: React.ReactNode` | Root of the NumberInput compound component. |  |
| 131 | `NumberInputField` | extends `Omit<InputProps, 'type' \| 'value' \| 'defaultValue'>` | The real `<input type="number">` — `min`/`max`/`step`/`disabled`/`value` all come from `NumberInput`'s context, so they never need to be repeated here. |  |
| 132 | `NumberInputIncrement` | Inherits all standard `'button'` attributes. | Increments `NumberInput`'s value by `step`, clamped to `max` — disabled automatically once the current value has reached `max`. |  |
| 133 | `NumberInputDecrement` | Inherits all standard `'button'` attributes. | Decrements `NumberInput`'s value by `step`, clamped to `min` — disabled automatically once the current value has reached `min`. |  |
| 134 | `OTPInput` | `value?: string`<br>`defaultValue?: string`<br>`onValueChange?: (value: string) => void`<br>`length: number` — Total number of slots/characters, e.g. `6` for a 6-digit code.<br>`disabled?: boolean`<br>`name?: string` — Forwarded to a visually-hidden native input mirroring the full code, for `<form>` submission.<br>`children?: React.ReactNode` | Root of the OTPInput compound component — a segmented one-time-passcode field, one real `<input maxLength={1}>` per {@link OTPInputSlot}. |  |
| 135 | `OTPInputSlot` | `index: number` — This slot's position, `0`-based.<br>extends `Omit<InputProps, 'value' \| 'defaultValue' \| 'maxLength'>` | One character of `OTPInput`'s code — a real `<input maxLength={1}>` (not a `<div>` painted to look like an input, unlike some OTP widgets) so autofill, password managers' one-time-code suggestions, and mobile "insert code from SMS" affordances all keep working. |  |
| 136 | `Pagination` | `page?: number`<br>`defaultPage?: number`<br>`onPageChange?: (page: number) => void`<br>`pageCount: number` — Total number of pages, 1-indexed.<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'nav'>` | Root of the Pagination compound component — a `<nav>` (per the WAI-ARIA Authoring Practices' guidance that pagination is just a labeled navigation landmark over a list of links, not a distinct ARIA pattern of its own) wrapping {@link PaginationList}/{@link PaginationItem}/ {@link PaginationLink}, plus {@link… |  |
| 137 | `PaginationList` | Inherits all standard `'ul'` attributes. | `<ul>` wrapper for {@link PaginationItem}s. |  |
| 138 | `PaginationItem` | Inherits all standard `'li'` attributes. | `<li>` wrapper for one {@link PaginationLink}/{@link PaginationEllipsis}/ {@link PaginationPrevious}/{@link PaginationNext}. |  |
| 139 | `PaginationLink` | `page: number` — The page number this link navigates to.<br>extends `PrimitivePropsWithRef<'button'>` | One page number — a `<button>` by default (`asChild` a real `<a href>` if pages are separate URLs, e.g. `?page=3`, which is the more common real-app shape for pagination; a `<button>` alone doesn't get you shareable/ bookmarkable URLs). |  |
| 140 | `PaginationPrevious` | Inherits all standard `'button'` attributes. | Moves to `page - 1` — disabled automatically on the first page. |  |
| 141 | `PaginationNext` | Inherits all standard `'button'` attributes. | Moves to `page + 1` — disabled automatically on the last page. |  |
| 142 | `PaginationEllipsis` | Inherits all standard `'span'` attributes. | A decorative "…" marker for a run of collapsed page numbers — `aria-hidden` since it's not itself interactive, with a visually-hidden "More pages" label so assistive tech still gets some indication a gap exists. |  |
| 143 | `ELLIPSIS` | extends `number \| typeof ELLIPSIS` | Computes which page numbers (plus `'ellipsis'` markers) to render for a given current `page`/`pageCount`, so consumers don't have to hand-roll the usual "show first/last `boundaryCount` pages, `siblingCount` pages either side of the current one, collapse the rest into an ellipsis" truncation math themselves — exported… |  |
| 144 | `Popover` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`children?: React.ReactNode` | Root of the Popover compound component. |  |
| 145 | `PopoverTrigger` | Inherits all standard `'button'` attributes. | Toggles the popover open/closed on click (unlike `DialogTrigger`, which only opens — a popover trigger commonly doubles as its own close button). |  |
| 146 | `PopoverPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `DialogPortal`. |  |
| 147 | `PopoverContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting.<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop Escape from closing the popover.<br>`onPointerDownOutside?: (event: PointerEvent) => void` — Forwarded to `DismissibleLayer`. Call `event.preventDefault()` to stop an outside click from closing the popover.<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="dialog"` (no `aria-modal` — this is the non-modal counterpart to `DialogContent`), positioned against `PopoverTrigger`'s anchor via `@nebula-lab/primitives`' `PopperContent`. |  |
| 148 | `PopoverClose` | Inherits all standard `'button'` attributes. | Closes the popover on click. |  |
| 149 | `Progress` | `value?: number \| null` — Current progress, `0`–`max`. Pass `null` for an indeterminate progress bar — a busy/loading state whose completion can't be measured (e.g.…<br>`max?: number`<br>`getValueLabel?: (value: number, max: number) => string` — Formats `aria-valuetext` for screen readers, e.g. `(value, max) => \`${value} of ${max} steps\``. Omit to fall back to the browser's…<br>extends `PrimitivePropsWithRef<'div'>` | `role="progressbar"` — the WAI-ARIA Progress Meter pattern. |  |
| 150 | `ProgressIndicator` | Inherits all standard `'div'` attributes. | The actual filled portion of the progress bar — a plain `div` carrying `data-state`/`data-value`/`data-max` (mirroring `Progress`'s own attributes) for `react-ui` to size/animate off of via CSS: a percentage `width` when `data-state="loading"`/`"complete"`, a looping animation when `data-state="indeterminate"` (where… |  |
| 151 | `RadioGroup` | `value?: string` — Controlled: the currently selected item's value.<br>`defaultValue?: string` — Uncontrolled: the initially selected item's value.<br>`onValueChange?: (value: string) => void`<br>`disabled?: boolean`<br>`required?: boolean`<br>`name?: string` — Forwarded to every item's hidden native radio input for `<form>` submission.<br>`orientation?: RovingFocusGroupOrientation`<br>`loop?: boolean` — Arrow keys wrap from the last item back to the first.<br>extends `PrimitivePropsWithRef<'div'>` | `role="radiogroup"` — a single-select group of {@link RadioGroupItem}s. |  |
| 152 | `RadioGroupItem` | `value: string` — Identifies this item within its `RadioGroup` — must be unique within the group.<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'button'>` | `role="radio"`. |  |
| 153 | `Rating` | `value?: number`<br>`defaultValue?: number`<br>`onValueChange?: (value: number) => void`<br>`disabled?: boolean`<br>`required?: boolean`<br>`name?: string` — Forwarded to every item's hidden native radio input for `<form>` submission.<br>extends `PrimitivePropsWithRef<'div'>` | `role="radiogroup"` over a set of {@link RatingItem}s (typically stars) — single-select, close to `RadioGroup`'s own shape, plus pointer-hover preview: moving the pointer over an item visually fills every item up to it (via each item's own `data-state`, computed from `hoveredValue` when present), without committing… |  |
| 154 | `RatingItem` | `value: number` — This item's numeric rating value, e.g. `3` for the third star.<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'button'>` | `role="radio"`. |  |
| 155 | `Select` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`value?: string` — Controlled: the currently selected item's value.<br>`defaultValue?: string` — Uncontrolled: the initially selected item's value.<br>`onValueChange?: (value: string) => void`<br>`disabled?: boolean`<br>`required?: boolean`<br>`name?: string` — Mirrored onto a visually-hidden native `<select>` so this participates in native `<form>` submission.<br>`children?: React.ReactNode` | Root of the Select compound component — the WAI-ARIA [collapsible listbox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/examples/listbox-collapsible/): a button (`SelectTrigger`, `aria-haspopup="listbox"`) that opens an anchor-positioned `role="listbox"` popup (`SelectContent`), built on… |  |
| 156 | `SelectTrigger` | Inherits all standard `'button'` attributes. | `aria-haspopup="listbox"` per the WAI-ARIA [collapsible listbox pattern](https://www.w3.org/WAI/ARIA/apg/patterns/listbox/examples/listbox-collapsible/) — a plain button, not `role="combobox"` (that role is for the *editable* combobox pattern `Combobox` builds separately; a `Select` trigger never accepts typed text). |  |
| 157 | `SelectValue` | `placeholder?: React.ReactNode` — Rendered when nothing is selected yet. | Displays the currently selected `SelectItem`'s label (its `textValue`, or its plain-string `children` — see `SelectItem`), or `placeholder` when nothing is selected. |  |
| 158 | `SelectPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `PopoverPortal`/`DialogPortal`. |  |
| 159 | `SelectContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean`<br>`onEscapeKeyDown?: (event: KeyboardEvent) => void`<br>`onPointerDownOutside?: (event: PointerEvent) => void`<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="listbox"`, anchor-positioned against `SelectTrigger` via `@nebula-lab/primitives`' `PopperContent` — same `Presence` > `DismissibleLayer` > `FocusScope` > (positioning) `asChild` chain `PopoverContent` uses, with one more link added: `Listbox` (`type="single"`, no explicit scope passed — it mints its own… |  |
| 160 | `SelectItem` | `value: string`<br>`textValue?: string` — Overrides the label `SelectValue` displays and registers for this item — only needed when `children` isn't plain text (e.g. an icon +…<br>extends `Omit<ListboxOptionProps, 'value'>` | A thin wrapper over `Listbox`'s `ListboxOption` (`role="option"`, roving tabindex, click/Enter/Space to select — all unchanged) that adds one real behavior on top: registering this item's display label against `Select`'s own context so `SelectValue` can look it up, and unregistering it on unmount. |  |
| 161 | `Skeleton` | Inherits all standard `'div'` attributes. | A loading placeholder shape — `aria-hidden` by default, since a skeleton bone is a decorative stand-in for content that hasn't arrived yet, not something a screen reader user benefits from having announced individually (a page with a dozen skeleton bones shouldn't announce a dozen "loading" statuses). |  |
| 162 | `Slider` | `value?: number[]` — One value per thumb. A plain slider has one value; a range slider has two.<br>`defaultValue?: number[]`<br>`onValueChange?: (value: number[]) => void`<br>`min?: number`<br>`max?: number`<br>`step?: number`<br>`disabled?: boolean`<br>`orientation?: SliderOrientation`<br>`name?: string` — Forwarded to a visually-hidden native input per thumb for `<form>` submission (see `SliderThumb`).<br>`children?: React.ReactNode` | Root of the Slider compound component — the WAI-ARIA [Slider pattern](https://www.w3.org/WAI/ARIA/apg/patterns/slider/) (and, with two thumbs, its [Slider (Multi-Thumb)](https://www.w3.org/WAI/ARIA/apg/patterns/slider-multithumb/) variant). |  |
| 163 | `SliderTrack` | Inherits all standard `'div'` attributes. | The slider's visual track — also handles clicking directly on the track (not on a thumb) by jumping whichever thumb is currently closest to the clicked position, then continuing to drag that thumb for the rest of the pointer gesture (uses `setPointerCapture` on the track itself so `pointermove` keeps firing here even… |  |
| 164 | `SliderRange` | Inherits all standard `'div'` attributes. | The filled portion of the track — spans from the track's start to the single thumb's value (a plain slider), or between the two thumbs' values (a range slider), computed generically off `Math.min`/`Math.max` of `values` either way. |  |
| 165 | `SliderThumb` | `index: number` — Which entry of `Slider`'s `values` array this thumb represents. `0` for a plain slider; `0`/`1` for a range slider's low/high thumb.<br>extends `PrimitivePropsWithRef<'div'>` | `role="slider"` — draggable via `setPointerCapture` on itself (so `pointermove` keeps firing here even once the pointer leaves the thumb's small hit area, without document-level listeners), and fully keyboard-operable per the WAI-ARIA Slider pattern: Arrow Left/Right (or Up/Down, whichever matches `orientation`) by… |  |
| 166 | `Spinner` | `label?: string` — Accessible label announced to screen readers while the spinner is visible. Unlike most components, a bare spinner icon has no visible text…<br>extends `PrimitivePropsWithRef<'div'>` | `role="status"` — announces the loading state to assistive tech via a visually-hidden label, without requiring visible text. |  |
| 167 | `Stepper` | `step?: number` — Controlled: the current (0-indexed) step.<br>`defaultStep?: number` — Uncontrolled: the initial (0-indexed) step.<br>`onStepChange?: (step: number) => void`<br>`orientation?: StepperOrientation`<br>`allowSkipAhead?: boolean` — See `StepperContextValue.allowSkipAhead`.<br>extends `PrimitivePropsWithRef<'nav'>` | Root of the Stepper compound component — a `<nav>` (a stepper is, at its core, navigation over an ordered sequence of steps, same reasoning as `Pagination`) wrapping `StepperList`/`StepperItem`/`StepperTrigger`. |  |
| 168 | `StepperList` | Inherits all standard `'ol'` attributes. | `<ol>` wrapper for `StepperItem`s — ordered since a stepper's sequence is meaningful (step 2 genuinely comes after step 1), unlike `PaginationList`'s `<ul>` where page links have no inherent order relationship beyond number. |  |
| 169 | `StepperItem` | `index: number` — This step's position in the sequence (0-indexed).<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'li'>` | Wraps one step's `StepperTrigger`. |  |
| 170 | `StepperTrigger` | Inherits all standard `'button'` attributes. | A `<button>` that jumps to this step. |  |
| 171 | `StepperIndicator` | Inherits all standard `'span'` attributes. | Decorative slot for a step's number/checkmark/icon — `aria-hidden` since `StepperTitle`'s visible text (rendered alongside it inside `StepperTrigger`) already provides the accessible name (mirrors `CardHeader`'s `icon` slot). |  |
| 172 | `StepperTitle` | Inherits all standard `'span'` attributes. | A step's visible label, e.g. `"Account"`. |  |
| 173 | `StepperDescription` | Inherits all standard `'span'` attributes. | A step's secondary text, e.g. `"Personal details"` under a `StepperTitle` of `"Account"`. |  |
| 174 | `StepperSeparator` | Inherits all standard `'span'` attributes. | A decorative connecting line between two `StepperItem`s — `aria-hidden` since it carries no information beyond what the items' order already conveys (mirrors `BreadcrumbSeparator`/`PaginationEllipsis`). |  |
| 175 | `Switch` | `checked?: boolean` — Controlled on/off state.<br>`defaultChecked?: boolean` — Uncontrolled initial state.<br>`onCheckedChange?: (checked: boolean) => void`<br>`disabled?: boolean`<br>`name?: string` — Forwarded to a hidden native `<input type="checkbox">` so this participates in native `<form>` submission.<br>`value?: string`<br>`required?: boolean`<br>extends `Omit<PrimitivePropsWithRef<'button'>, 'checked' \| 'defaultChecked'>` | `role="switch"` — like {@link Checkbox} (a `button` + a visually-hidden native `<input type="checkbox">` for form participation), but semantically an on/off toggle rather than a selectable option: screen readers announce "on"/"off" instead of "checked"/"unchecked", per the WAI-ARIA [Switch… |  |
| 176 | `Tabs` | `value?: string` — Controlled: the currently selected tab's value.<br>`defaultValue?: string` — Uncontrolled: the initially selected tab's value.<br>`onValueChange?: (value: string) => void`<br>`orientation?: Orientation`<br>`activationMode?: ActivationMode` — `'automatic'` selects a tab as soon as it receives focus (arrow-key navigation moves selection); `'manual'` requires Enter/Space to select…<br>extends `PrimitivePropsWithRef<'div'>` | Root of the Tabs compound component — provides shared state to `TabList`, `Tab`, and `TabPanel` via scoped context, following the WAI-ARIA Tabs Authoring Practice. |  |
| 177 | `TabList` | Inherits all standard `'div'` attributes. | `role="tablist"` — owns arrow-key roving-tabindex navigation between `Tab`s (Left/Right for horizontal, Up/Down for vertical, Home/End to jump to the first/last tab), per the WAI-ARIA Tabs pattern. |  |
| 178 | `Tab` | `value: string` — Identifies this tab and the `TabPanel` it controls — must be unique within the `Tabs`.<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'button'>` | `role="tab"`. |  |
| 179 | `TabPanel` | `value: string` — Must match the `value` of the `Tab` that controls this panel.<br>`forceMount?: boolean` — Keep the panel mounted (but `hidden`) while inactive instead of unmounting it — useful to preserve panel-local state (form input, video…<br>extends `PrimitivePropsWithRef<'div'>` | `role="tabpanel"`. |  |
| 180 | `Toast` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`duration?: number` — How long (ms) before the toast auto-dismisses itself. `Infinity` (or any non-positive number) disables auto-dismiss entirely — useful for a…<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting.<br>extends `PrimitivePropsWithRef<'div'>` | `role="status"` + `aria-live="polite"` + `aria-atomic="true"` — announces itself to assistive tech without stealing focus or interrupting whatever the user is doing (the WAI-ARIA "status message" pattern; a toast is fundamentally a live region, not a dialog). |  |
| 181 | `ToastPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — wraps `ToastViewport` (once, near the root of an app), not each individual `Toast`, unlike `DialogPortal`/`PopoverPortal` which wrap one piece of overlay content per open/close cycle. |  |
| 182 | `ToastViewport` | Inherits all standard `'div'` attributes. | `role="region"` container for `Toast`s, typically rendered once near the root of an app (inside a `Portal` so it renders above everything else) and defaulting `aria-label` to `"Notifications"`. |  |
| 183 | `ToastTitle` | Inherits all standard `'div'` attributes. | A toast's heading text. |  |
| 184 | `ToastDescription` | Inherits all standard `'div'` attributes. | A toast's supporting text, e.g. `"report.pdf was uploaded successfully."` under a `ToastTitle` of `"Upload complete"`. |  |
| 185 | `ToastAction` | `altText: string` — Required, separately from this button's visible text/children. A toast's live-region announcement can be interrupted or missed entirely…<br>extends `PrimitivePropsWithRef<'button'>` | A toast's action button, e.g. "Undo". |  |
| 186 | `ToastClose` | Inherits all standard `'button'` attributes. | Dismisses the toast on click. |  |
| 187 | `Toggle` | `pressed?: boolean` — Controlled pressed state.<br>`defaultPressed?: boolean` — Uncontrolled initial state.<br>`onPressedChange?: (pressed: boolean) => void`<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'button'>` | A two-state toggle button — `aria-pressed`, not `Switch`'s `role="switch"`/`aria-checked`, per the WAI-ARIA [Button pattern's pressed state](https://www.w3.org/WAI/ARIA/apg/patterns/button/): a toggle button IS a button, just one that stays visually/semantically "pressed" after activation (a bold/italic formatting… |  |
| 188 | `ToggleGroup` | extends `ToggleGroupSingleProps \| ToggleGroupMultipleProps` | Root of the ToggleGroup compound component — a set of {@link Toggle} buttons (`role="group"`, not `radiogroup`: this is a group of toggle buttons per the WAI-ARIA Button pattern, not the Radio Group pattern, even in `type="single"` mode — matching how a real text-alignment toolbar is exposed). |  |
| 189 | `ToggleGroupItem` | `value: string` — Identifies this item within its `ToggleGroup` — must be unique within the group.<br>extends `Omit<ToggleProps, 'pressed' \| 'defaultPressed' \| 'onPressedChange'>` | A {@link Toggle} whose pressed state comes entirely from `ToggleGroup`'s context (comparing this item's `value` against the group's pressed value(s)) rather than local state — this component holds none itself, same relationship `RadioGroupItem` has to `RadioGroup`. |  |
| 190 | `Tooltip` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`delayDuration?: number` — Hover/focus delay (ms) before the tooltip opens. Closing is always immediate — unlike `Popover`/`Dialog`, a tooltip should never linger…<br>`children?: React.ReactNode` | Root of the Tooltip compound component. |  |
| 191 | `TooltipTrigger` | Inherits all standard `'button'` attributes. | Opens the tooltip (after `delayDuration`) on pointer-enter or focus, closes it immediately on pointer-leave or blur. |  |
| 192 | `TooltipPortal` | extends `PortalProps` (from `@nebula-lab/primitives/portal`) | Thin re-export of `@nebula-lab/primitives`' `Portal` — same rationale as `PopoverPortal`/`DialogPortal`. |  |
| 193 | `TooltipContent` | `className?: string`<br>`children?: React.ReactNode`<br>`forceMount?: boolean` — Keep mounted while closed instead of unmounting.<br>extends `Pick< PopperContentProps, 'side' \| 'sideOffset' \| 'align' \| 'alignOffset' \| 'avoidCollisions' \| 'collisionPadding' >` | `role="tooltip"`, positioned against `TooltipTrigger`'s anchor via `@nebula-lab/primitives`' `PopperContent`. |  |
| 194 | `Tree` | extends `TreeSingleProps \| TreeMultipleProps` | Root of the Tree compound component — `role="tree"` over a set of nested {@link TreeItem}s, the WAI-ARIA [Tree View pattern](https://www.w3.org/WAI/ARIA/apg/patterns/treeview/). | `tree` |
| 195 | `TreeItem` | `value: string` — Identifies this item within its `Tree` — must be unique within the whole tree.<br>`expandable?: boolean` — Whether this item can hold a nested `TreeGroup` at all. Leaf items (files, not folders) should omit this — they never get `aria-expanded`,…<br>`disabled?: boolean`<br>extends `PrimitivePropsWithRef<'li'>` | `role="treeitem"` — and, per the WAI-ARIA Tree View pattern, the element that's actually focusable and clickable itself (not a nested button; a tree row's whole line is the interactive unit). | `tree` |
| 196 | `TreeItemToggle` | Inherits all standard `'button'` attributes. | Expand/collapse icon button for one `TreeItem`, e.g. a chevron. | `tree` |
| 197 | `TreeGroup` | `forceMount?: boolean` — Keep the group mounted (but `hidden`) while collapsed instead of unmounting it — same escape hatch as `AccordionContent`'s `forceMount`.…<br>extends `PrimitivePropsWithRef<'ul'>` | `role="group"` — the nested `<ul>` holding one `TreeItem`'s children. | `tree` |
| 198 | `TreeView` | — | (no doc comment found) | `tree` |
| 199 | `TreeViewItem` | — | (no doc comment found) | `tree` |
| 200 | `TreeViewItemToggle` | — | (no doc comment found) | `tree` |
| 201 | `TreeViewGroup` | — | (no doc comment found) | `tree` |

