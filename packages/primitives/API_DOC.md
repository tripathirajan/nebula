# `@nebula-lab/primitives` — API Reference

Generated from the actual TypeScript source (real exported prop/parameter names and types, not hand-transcribed) — regenerate with `node scripts/generate-api-docs.mjs` after adding/changing a component so this doesn't drift. See `ARCHITECTURE.md` at the repo root for how this package fits into the overall layering, and this package's own `README.md` for install/usage.

Cross-package prop types (e.g. a `react-ui` component's Props extending a `styleless` one) are shown as `extends `Type` (from `@nebula-lab/other-package`)` rather than flattened in place — look up that type in the other package's own API_DOC.md. Inherited native DOM attributes (from `PrimitivePropsWithRef`/`PolymorphicComponentPropsWithRef`) are summarized as a single note rather than enumerated, since they're just the target element's standard attributes.

---

## `@nebula-lab/primitives`

Unstyled, polymorphic low-level building blocks. Zero in-workspace dependencies.

| SN | Component | Props | Usage notes | Comments |
|---|---|---|---|---|
| 1 | `AspectRatio` | `ratio?: number` — `width / height`, e.g. `16 / 9` or `1` for a square. Applied via the CSS `aspect-ratio` property (broadly supported; no padding-percentage…<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | Constrains its content to a fixed width/height ratio regardless of the content's own intrinsic size — for video/image/chart containers where the layout shouldn't jump as content loads. |  |
| 2 | `Boundary` | `children: React.ReactNode`<br>`fallback: React.ReactNode \| ((error: Error, reset: () => void) => React.ReactNode)` — Rendered instead of `children` after an error is caught. Receives the error and a `reset` function that re-attempts rendering `children`…<br>`onError?: (error: Error, info: { componentStack: string \| null }) => void` — Called with the caught error (and React's componentStack) for logging/reporting. | An error boundary — documented here as an "accessibility" primitive alongside `FocusScope`/`DismissibleLayer` because an uncaught render error otherwise leaves the page silently blank or half-rendered, which is as much a disaster for screen-reader/keyboard users as it is for anyone else (there's often no visual cue… |  |
| 3 | `Box` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | The plainest possible polymorphic building block — a `Primitive` by another name. |  |
| 4 | `Button` | Inherits all standard `'button'` attributes. | Unstyled `button` — the one meaningful behavior difference from a bare `<button>` is defaulting `type="button"` instead of the native default (`"submit"`), which silently submits the nearest `<form>` and is almost never what you want for a button that isn't literally a submit button. |  |
| 5 | `Center` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | `Flex` with both axes centered (`align="center"`, `justify="center"`) — for centering a single child (a spinner, an empty-state illustration, a modal's content) both horizontally and vertically. |  |
| 6 | `Cn` | — | Combines `clsx` (conditional class composition) with `tailwind-merge` (resolves conflicting Tailwind utility classes, last one wins) — the single class-name helper every `@nebula-lab/*` styled component should use instead of hand-rolled string concatenation. |  |
| 7 | `Code` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | Inline code — a `code` by another name, no classes of its own (unstyled primitives own behavior, not visuals — `@nebula-lab/react-ui`'s `Code` supplies the monospace/background/pill styling). |  |
| 8 | `ComposeEventHandlers` | — | Merges a consumer-provided event handler with an internal one, so components can attach their own behavior without clobbering a user's `onClick`/`onKeyDown`/etc. If the consumer calls `event.preventDefault()`, the internal handler is skipped by default (matches native DOM semantics). |  |
| 9 | `ComposeRefs` | — | Composes multiple refs (callback or object) into a single ref callback so a component can forward its own ref while also capturing one internally. |  |
| 10 | `Container` | `size?: ContainerSize`<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | Horizontally centers its content and caps its width at `size` — the standard "page content shouldn't stretch full-bleed on a wide monitor" wrapper. |  |
| 11 | `CreateContextScope` | — | `createContextScope` solves the "nested instances of the same compound component" problem — e.g. a `Tabs` inside another `Tabs` — by letting each root instance mint its own set of contexts instead of every instance sharing one module-level context. |  |
| 12 | `DismissibleLayer` | `onEscapeKeyDown?: (event: KeyboardEvent) => void` — Called on Escape keydown, only when this is the topmost open layer. Call `event.preventDefault()` to stop `onDismiss` from also firing.<br>`onPointerDownOutside?: (event: PointerEvent) => void` — Called on a pointerdown outside this layer's subtree, only when this is the topmost open layer. Call `event.preventDefault()` to stop…<br>`onDismiss?: () => void` — Fires after `onEscapeKeyDown`/`onPointerDownOutside` unless that handler called `preventDefault()` — the common case, wire this to close…<br>extends `PrimitivePropsWithRef<'div'>` | Escape-key and outside-pointerdown dismissal for overlays, with correct layering: only the topmost mounted `DismissibleLayer` responds, so closing a nested popover doesn't also close the dialog underneath it. |  |
| 13 | `Flex` | `direction?: FlexDirection`<br>`align?: FlexAlign`<br>`justify?: FlexJustify`<br>`wrap?: boolean`<br>`gap?: number \| string` — Gap between children. Applied as an inline style (not a Tailwind class) so arbitrary values work without needing to be present verbatim in…<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | A `display: flex` container with small, literal-CSS convenience props (`direction`, `align`, `justify`, `wrap`, `gap`) instead of hand-written Tailwind flex utility classes — thin Tailwind-token-driven layout primitives so consumers don't write raw flex/grid classes. | `flex` |
| 14 | `HStack` | `align?: FlexAlign`<br>`justify?: FlexJustify`<br>`gap?: number \| string`<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | `Flex` pinned to `direction="row"`, `align="center"`, and no wrapping — a horizontal row of children that stays on one line and overflows rather than wrapping. | `flex` |
| 15 | `Inline` | `align?: FlexAlign`<br>`justify?: FlexJustify`<br>`gap?: number \| string`<br>`wrap?: boolean`<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | `Flex` pinned to `direction="row"` with `wrap` on by default and `align="center"` — a horizontal run of children (tag lists, button groups, breadcrumbs) that wraps onto multiple lines instead of overflowing when it runs out of room. | `flex` |
| 16 | `Stack` | `align?: FlexAlign`<br>`justify?: FlexJustify`<br>`gap?: number \| string`<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | `Flex` pinned to `direction="column"` — a vertical stack of children with consistent spacing. | `flex` |
| 17 | `VStack` | extends `StackProps<E>` | Alias of `Stack` under a name some consumers reach for first coming from other component libraries — a thin pass-through wrapper rather than `const VStack = Stack` directly, so it gets its own `displayName` in devtools instead of mutating `Stack`'s (they're the same rendered output either way; there's no behavior to… | `flex` |
| 18 | `Wrap` | extends `InlineProps<E>` | Alias of `Inline` under a name some consumers reach for first coming from other component libraries — `Inline` already *is* "a wrapping row of children," so this is a thin pass-through wrapper (own `displayName`, same behavior) rather than a second implementation to keep in sync. | `flex` |
| 19 | `FocusScope` | `trapped?: boolean` — Trap Tab/Shift+Tab focus cycling within this scope's subtree — for modal contexts (`Dialog`). Non-modal contexts (a `Popover` that…<br>`onMountAutoFocus?: (event: Event) => void` — Called when the scope mounts and is about to move focus into itself; call `event.preventDefault()` to take over focusing yourself.<br>`onUnmountAutoFocus?: (event: Event) => void` — Called when the scope unmounts and is about to restore focus to whatever was focused before it mounted; call `event.preventDefault()` to…<br>extends `PrimitivePropsWithRef<'div'>` | Manages focus for a subtree: on mount, moves focus into itself (first focusable descendant, or the container itself if none); on unmount, restores focus to whatever was focused beforehand; while mounted, can optionally trap Tab cycling within itself (`trapped`). |  |
| 20 | `Form` | Inherits all standard `'form'` attributes. | Unstyled `form`. |  |
| 21 | `Grid` | `columns?: number \| string` — Number of equal-width columns, or a raw `grid-template-columns` value (e.g. `'200px 1fr 1fr'`) for anything more specific than an even…<br>`rows?: number \| string` — Same shape as `columns`, for row tracks.<br>`gap?: number \| string` — Applied as an inline style — see `Flex`'s `gap` doc for why.<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | A `display: grid` container. |  |
| 22 | `Heading` | `level?: HeadingLevel` — Sets both the default rendered tag (`h1`..`h6`) and its size/weight — override just the tag via `as` while keeping a different level's…<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | A heading with size/weight derived from `level` (1-6), independent of which tag actually renders — so document outline (`as`/tag) and visual hierarchy (`level`/size) can be set independently when they need to diverge, while defaulting to matching (`level={2}` renders an `h2` sized for a level-2 heading). |  |
| 23 | `Image` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | An unstyled `img` — exists so `image` has the same polymorphic `as`/ `asChild` escape hatches (framework image components, `next/image`, a `<picture>` wrapper, ...) as every other Nebula element primitive, instead of consumers reaching for a bare `<img>` that can't compose the same way. |  |
| 24 | `Input` | `invalid?: boolean` — Sets `aria-invalid="true"` — pair with a `FieldError`/`role="alert"` message once that component exists.<br>Inherits all standard `'input'` attributes. | Unstyled `input` — forwards a ref to the underlying DOM node (needed for imperative focus management, e.g. a form library focusing the first invalid field) and wires `invalid` to `aria-invalid` so screen readers announce the error state without you having to remember the attribute name. |  |
| 25 | `Label` | `required?: boolean` — Appends a visible `*` plus a screen-reader-only "(required)" so the requirement is announced even though the asterisk itself is…<br>Inherits all standard `'label'` attributes. | Unstyled `label`. |  |
| 26 | `Link` | `external?: boolean` — Marks this as an external link: adds `target="_blank"` and `rel="noopener noreferrer"` (prevents the new page from accessing…<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | An `a` with underline-on-hover + focus-visible ring by default, and an `external` convenience prop. |  |
| 27 | `NativeSelect` | `invalid?: boolean` — Sets `aria-invalid="true"`.<br>Inherits all standard `'select'` attributes. | Unstyled native `<select>` — no popup/listbox behavior to add on top of what the browser already provides (unlike `@nebula-lab/headless`'s `Select`, which builds a fully custom popup listbox for cases that need one), so this stays exactly here at the primitives layer, same treatment `Input`/ `Textarea` get: a thin… |  |
| 28 | `Overlay` | Inherits all standard `'div'` attributes. | A full-viewport `fixed inset-0` layer — the dimmed backdrop behind a modal `Dialog`. |  |
| 29 | `Paragraph` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | A `p` by another name, no classes of its own (unstyled primitives own behavior, not visuals — `@nebula-lab/react-ui`'s `Paragraph` supplies the comfortable-reading line-height), distinct from `Text` (meant for inline runs, no line-height opinion at any layer). |  |
| 30 | `Popper` | `children?: React.ReactNode` | Root of the Popper compound primitive — the shared anchor-positioning building block `Popover` and `Tooltip` (once built) are both meant to be built on, so overlay-positioning components share one `Popper` primitive rather than each reimplementing collision detection. |  |
| 31 | `PopperAnchor` | Inherits all standard `'div'` attributes. | Marks the element `PopperContent` measures and positions itself against. |  |
| 32 | `PopperContent` | `side?: Side` — Which side of the anchor to render on.<br>`sideOffset?: number` — Gap between the anchor and content along the `side` axis.<br>`align?: Align` — Alignment along the cross axis relative to the anchor.<br>`alignOffset?: number` — Additional offset along the cross axis.<br>`avoidCollisions?: boolean` — Flip to the opposite `side` if the preferred one doesn't fit, and shift along the cross axis to stay within the viewport.<br>`collisionPadding?: number` — Minimum distance to keep from the viewport edge when avoiding collisions.<br>extends `PrimitivePropsWithRef<'div'>` | Positions itself against `PopperAnchor`'s element using `position: fixed` — recomputed on window resize/scroll and, where available, via `ResizeObserver` on both the anchor and this content (so e.g. a dynamically-resizing anchor or content doesn't leave the popover misaligned). |  |
| 33 | `Portal` | `container?: Element \| DocumentFragment \| null` — Where to portal into. Defaults to `document.body` — pass a specific node for e.g. rendering inside a specific scroll container or a…<br>Inherits all standard `'div'` attributes. | Renders its children into `container` (default `document.body`) instead of in place in the React tree — the backbone every overlay (`Dialog`/`Popover`/`Tooltip`/`Toast`, once built) is portaled through, so `overflow: hidden`/`z-index` stacking contexts on an ancestor can't clip or bury them. |  |
| 34 | `Pre` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | A multi-line code block container — a `pre` by another name, no classes of its own (unstyled primitives own behavior, not visuals — `@nebula-lab/react-ui`'s `Pre` supplies the monospace/padding/scroll/ background styling). |  |
| 35 | `Presence` | `present: boolean` — Whether the content should be present. Going from `true` to `false` doesn't unmount immediately if a CSS animation/transition is running —…<br>`children: React.ReactElement<PresenceChildProps> \| ((props: { isPresent: boolean }) => React.ReactElement)` — A single element (needs to accept a `ref`), or a render function receiving `{ isPresent }` for cases where the exit-animation class itself… | Keeps `children` mounted through an exit animation instead of unmounting the instant `present` becomes `false` — the difference between a `Dialog`'s content snapping out of existence vs. actually playing its closing transition. |  |
| 36 | `Primitive` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | Implementation note (applies to every polymorphic component in this package — `Box`, `Flex`, `Text`, etc. all repeat this same shape): `React.forwardRef`'s type signature is `forwardRef<T, P>(render: (props: P, ref: Ref<T>) => ...)`, which can't express "T depends on P's own `E` type parameter." Two escape hatches… |  |
| 37 | `RovingFocusGroup` | `orientation?: Orientation`<br>`loop?: boolean` — Arrow key navigation wraps from the last item back to the first (and vice versa).<br>`defaultCurrentTabStopId?: string` — Which item is the initial tab stop; defaults to whichever item registers first.<br>extends `PrimitivePropsWithRef<'div'>` | Implements the "roving tabindex" keyboard pattern shared by `Tabs`, `RadioGroup`, `Menu`, and toolbars: only one item in the group is ever a Tab stop (`tabIndex={0}`) at a time — everyone else is `tabIndex={-1}` — and arrow keys move which item that is. |  |
| 38 | `FocusItem` | `focusable?: boolean` — Registers this item without making it a valid Tab stop — for a temporarily-disabled item that should stay in the DOM (and still be…<br>extends `PrimitivePropsWithRef<'span'>` | One focusable item inside a `RovingFocusGroup`. |  |
| 39 | `Slot` | `children?: React.ReactNode`<br>extends `React.HTMLAttributes<HTMLElement>` | `Slot` merges the props (and ref) it receives onto its single child element instead of rendering a DOM node of its own. |  |
| 40 | `Slottable` | `children?: React.ReactNode`<br>extends `React.HTMLAttributes<HTMLElement>` | Marks which child of a multi-child composite should receive the slotted props when a component has more than one child and needs to disambiguate. |  |
| 41 | `Spacer` | `grow?: number` — Flex-grow (and flex-shrink) factor — how aggressively this element claims the remaining free space relative to any other `Spacer`s in the…<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | An invisible, `flex-grow`ing filler — drop it between two children of a `Flex`/`HStack`/`Inline` to push them to opposite ends without hand- writing `justify="between"` (useful when there are more than two children and only *some* of the gaps should expand). |  |
| 42 | `Text` | `truncate?: boolean` — Single-line ellipsis truncation (`overflow: hidden; text-overflow: ellipsis; white-space: nowrap`).<br>Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | Generic inline/block text — no font-size or weight opinion by default (that's for `className`, or reach for `Heading` for headings). |  |
| 43 | `Textarea` | `invalid?: boolean` — Sets `aria-invalid="true"`.<br>`autoResize?: boolean` — Grows/shrinks the textarea's height to fit its content instead of showing a scrollbar — recalculated on every `input` event. Implemented by…<br>Inherits all standard `'textarea'` attributes. | Unstyled `textarea`, optionally auto-resizing to fit its content. |  |
| 44 | `VisuallyHidden` | Polymorphic — accepts an `as` prop to change the rendered element; inherits that element's standard attributes. | Hides content visually while keeping it in the accessibility tree — for text that a sighted user doesn't need (a redundant label, an icon button's accessible name) but a screen reader user does. |  |

