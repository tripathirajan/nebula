// Thin renamed re-export — see `dropdown-menu-portal.tsx` for the same
// rationale (`MenuContent` gates its own mount/unmount via `Presence`).
export { MenuPortal as ContextMenuPortal } from '../menu/menu-portal';
export type { MenuPortalProps as ContextMenuPortalProps } from '../menu/menu-portal';
