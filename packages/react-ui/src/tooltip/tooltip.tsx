// The root renders no DOM of its own (just scoped context + `Popper`) —
// nothing to style, re-exported as-is from `@nebula/styleless`.
export { Tooltip } from '@nebula/styleless/tooltip';
export type { TooltipProps } from '@nebula/styleless/tooltip';
