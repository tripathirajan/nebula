// The root renders no DOM of its own (just context + state) — nothing to
// style, re-exported as-is from `@nebula/styleless`.
export { NumberInput } from '@nebula/styleless/number-input';
export type { NumberInputProps } from '@nebula/styleless/number-input';
