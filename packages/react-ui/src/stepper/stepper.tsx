// Root has no visual chrome of its own — it's a `<nav>` landmark, same as
// `Pagination`'s root. Re-exported as-is (mirrors `DialogTrigger`-style
// zero-wrap re-exports elsewhere in this package).
export { Stepper } from '@nebula/styleless/stepper';
export type { StepperProps } from '@nebula/styleless/stepper';
export type { StepStatus } from '@nebula/styleless/stepper';
