// The root renders no visible DOM of its own (a `Popper` anchor scope plus
// context/state, same as `Popover`'s root) — nothing to style, re-exported
// as-is.
export { Select } from '@nebula/styleless/select';
export type { SelectProps } from '@nebula/styleless/select';
