// Renders no DOM of its own — just the selected label or placeholder text,
// rendered directly as `SelectTrigger`'s child. Nothing to style.
export { SelectValue } from '@nebula-lab/headless/select';
export type { SelectValueProps } from '@nebula-lab/headless/select';
