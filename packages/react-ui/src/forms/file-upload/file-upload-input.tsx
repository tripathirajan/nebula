// Visually hidden native `<input type="file">` (see the headless source) —
// nothing to style, re-exported as-is, same as `MenuTrigger`'s no-chrome
// treatment.
export { FileUploadInput } from '@nebula-lab/headless/file-upload';
export type { FileUploadInputProps } from '@nebula-lab/headless/file-upload';
