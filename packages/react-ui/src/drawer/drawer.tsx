// The root renders no DOM of its own (just context + state, reusing
// `Dialog`'s directly — see the headless source's header comment) —
// nothing to style, re-exported as-is.
export { Drawer } from '@nebula/headless/drawer';
export type { DrawerProps } from '@nebula/headless/drawer';
