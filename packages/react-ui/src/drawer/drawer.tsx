// The root renders no DOM of its own (just context + state, reusing
// `Dialog`'s directly — see the styleless source's header comment) —
// nothing to style, re-exported as-is.
export { Drawer } from '@nebula/styleless/drawer';
export type { DrawerProps } from '@nebula/styleless/drawer';
