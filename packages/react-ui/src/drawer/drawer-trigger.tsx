// A `DrawerTrigger` is typically a `Button` (or other already-styled
// element) passed `asChild` — re-exported as-is, same as `DialogTrigger`.
export { DrawerTrigger } from '@nebula-lab/headless/drawer';
export type { DrawerTriggerProps } from '@nebula-lab/headless/drawer';
