// The root renders no DOM of its own (just context + anchor state) —
// nothing to style, re-exported as-is from `@nebula/headless`.
export { Popover } from '@nebula/headless/popover';
export type { PopoverProps } from '@nebula/headless/popover';
