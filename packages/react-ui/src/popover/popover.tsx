// The root renders no DOM of its own (just context + anchor state) —
// nothing to style, re-exported as-is from `@nebula/styleless`.
export { Popover } from '@nebula/styleless/popover';
export type { PopoverProps } from '@nebula/styleless/popover';
