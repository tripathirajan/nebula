// The root renders no DOM of its own (just context + state) — nothing to
// style, re-exported as-is from `@nebula-lab/headless`.
export { HoverCard } from '@nebula-lab/headless/hover-card';
export type { HoverCardProps } from '@nebula-lab/headless/hover-card';
