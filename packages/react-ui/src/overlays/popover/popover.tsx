// The root renders no DOM of its own (just context + anchor state) —
// nothing to style, re-exported as-is from `@nebula-lab/headless`.
export { Popover } from '@nebula-lab/headless/popover';
export type { PopoverProps } from '@nebula-lab/headless/popover';
