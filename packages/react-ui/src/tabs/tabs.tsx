// The root renders a plain `div` with no visual chrome of its own (see
// `@nebula-lab/headless`'s `Tabs`) — layout/styling lives on `TabList`/`Tab`/
// `TabPanel`, so there's nothing for this layer to add here.
export { Tabs } from '@nebula-lab/headless/tabs';
export type { TabsProps } from '@nebula-lab/headless/tabs';
