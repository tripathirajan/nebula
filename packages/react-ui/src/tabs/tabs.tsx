// The root renders a plain `div` with no visual chrome of its own (see
// `@nebula/headless`'s `Tabs`) — layout/styling lives on `TabList`/`Tab`/
// `TabPanel`, so there's nothing for this layer to add here.
export { Tabs } from '@nebula/headless/tabs';
export type { TabsProps } from '@nebula/headless/tabs';
