// A `<nav>` landmark with no visual chrome of its own — `BreadcrumbList`
// carries the actual layout styling, same "wrapper vs. content" split
// `Card`/`CardContent` use.
export { Breadcrumb } from '@nebula-lab/headless/breadcrumb';
export type { BreadcrumbProps } from '@nebula-lab/headless/breadcrumb';
