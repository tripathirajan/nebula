// No visual chrome of its own (just a `div` carrying `data-state`/
// `data-disabled` for its `CollapsibleTrigger`/`CollapsibleContent` to key
// off of) — re-exported as-is from `@nebula/headless`.
export { Collapsible } from '@nebula/headless/collapsible';
export type { CollapsibleProps } from '@nebula/headless/collapsible';
