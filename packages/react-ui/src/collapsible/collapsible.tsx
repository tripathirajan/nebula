// No visual chrome of its own (just a `div` carrying `data-state`/
// `data-disabled` for its `CollapsibleTrigger`/`CollapsibleContent` to key
// off of) — re-exported as-is from `@nebula/styleless`.
export { Collapsible } from '@nebula/styleless/collapsible';
export type { CollapsibleProps } from '@nebula/styleless/collapsible';
