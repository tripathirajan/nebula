// No styling needed — a `DialogTrigger` is typically a `Button` (or any
// other already-styled element) passed `asChild`; re-exported as-is from
// `@nebula/headless` rather than adding a pass-through wrapper with nothing
// to add.
export { DialogTrigger } from '@nebula/headless/dialog';
export type { DialogTriggerProps } from '@nebula/headless/dialog';
