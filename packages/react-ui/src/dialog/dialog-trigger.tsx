// No styling needed — a `DialogTrigger` is typically a `Button` (or any
// other already-styled element) passed `asChild`; re-exported as-is from
// `@nebula-lab/headless` rather than adding a pass-through wrapper with nothing
// to add.
export { DialogTrigger } from '@nebula-lab/headless/dialog';
export type { DialogTriggerProps } from '@nebula-lab/headless/dialog';
