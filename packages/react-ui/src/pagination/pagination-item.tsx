// No chrome of its own — a plain `<li>` list-item wrapper; re-exported as-is
// from `@nebula-lab/headless` (see `PaginationLink`/`PaginationEllipsis` for
// the actual visual treatment).
export { PaginationItem } from '@nebula-lab/headless/pagination';
export type { PaginationItemProps } from '@nebula-lab/headless/pagination';
