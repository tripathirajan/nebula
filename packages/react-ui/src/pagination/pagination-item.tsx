// No chrome of its own — a plain `<li>` list-item wrapper; re-exported as-is
// from `@nebula/headless` (see `PaginationLink`/`PaginationEllipsis` for
// the actual visual treatment).
export { PaginationItem } from '@nebula/headless/pagination';
export type { PaginationItemProps } from '@nebula/headless/pagination';
