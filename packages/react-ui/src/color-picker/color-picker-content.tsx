// Thin renamed re-export of this package's own `PopoverContent` — mirrors
// `@nebula/styleless`'s `ColorPickerContent`-wraps-`PopoverContent`
// relationship, so this needs no separate styling of its own.
export { PopoverContent as ColorPickerContent } from '../popover/popover-content';
export type { PopoverContentProps as ColorPickerContentProps } from '../popover/popover-content';
