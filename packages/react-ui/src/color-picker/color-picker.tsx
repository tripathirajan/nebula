// Thin re-export straight from `@nebula/styleless`: mints its own ambient
// `Popover` internally and renders no visible DOM of its own — nothing here
// for this package to style, same as this package's own `Popover` root.
export { ColorPicker } from '@nebula/styleless/color-picker';
export type { ColorPickerProps } from '@nebula/styleless/color-picker';
