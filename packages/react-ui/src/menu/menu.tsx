// The root renders no visible DOM of its own (a `Popper` anchor scope plus
// context/state, same as `Popover`'s root) — nothing to style.
export { Menu } from '@nebula/styleless/menu';
export type { MenuProps } from '@nebula/styleless/menu';
