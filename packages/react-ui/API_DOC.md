# `@nebula-lab/react-ui` — API Reference

Generated from the actual TypeScript source (real exported prop/parameter names and types, not hand-transcribed) — regenerate with `node scripts/generate-api-docs.mjs` after adding/changing a component so this doesn't drift. See `ARCHITECTURE.md` at the repo root for how this package fits into the overall layering, and this package's own `README.md` for install/usage.

Cross-package prop types (e.g. a `react-ui` component's Props extending a `styleless` one) are shown as `extends `Type` (from `@nebula-lab/other-package`)` rather than flattened in place — look up that type in the other package's own API_DOC.md. Inherited native DOM attributes (from `PrimitivePropsWithRef`/`PolymorphicComponentPropsWithRef`) are summarized as a single note rather than enumerated, since they're just the target element's standard attributes.

---

## `@nebula-lab/react-ui`

Tailwind-styled components built on `primitives`/`headless`/`styleless`, plus the design token system.

| SN | Component | Props | Usage notes | Comments |
|---|---|---|---|---|
| 1 | `Button` | extends `StylelessButtonProps` (from `@nebula-lab/styleless/button`)<br>CVA variant props from `buttonVariants` (see this file's own `buttonVariants` recipe for the full option list). | Styled `Button` — the `react-ui` layer's job is purely visual, so this wraps `@nebula-lab/styleless`'s `Button` (which already gives `asChild` support, the `type="button"` default, and real `loading` semantics — `aria-busy`/`data-loading`/forced-`disabled`) rather than reaching for `@nebula-lab/primitives` directly,… | `actions` |
| 2 | `ButtonGroup` | `orientation?: 'horizontal' \| 'vertical'`<br>Inherits all standard `'div'` attributes. | Visually merges a row (or column) of adjacent `Button`/`IconButton`s into one segmented control — purely a CSS trick via child-combinator Tailwind classes (`[&>*:not(:first-child)]:...`), no shared state or roving tabindex of its own: each button keeps its own independent click behavior and its own place in the normal… | `actions` |
| 3 | `FAB` | extends `StylelessFABProps` (from `@nebula-lab/styleless/fab`)<br>CVA variant props from `fabVariants` (see this file's own `fabVariants` recipe for the full option list). | Floating Action Button — wraps `@nebula-lab/styleless`'s `FAB` (required `aria-label`, `Button`'s `loading` semantics) and adds the circular, elevated styling. | `actions` |
| 4 | `IconButton` | extends `StylelessIconButtonProps` (from `@nebula-lab/styleless/icon-button`)<br>CVA variant props from `buttonVariants` (see this file's own `buttonVariants` recipe for the full option list). | A square, icon-only `Button` — wraps `@nebula-lab/styleless`'s `IconButton` (which already enforces the required-`aria-label` contract and carries `Button`'s `loading` semantics), then reuses this package's own `buttonVariants` recipe for color/border/focus-ring so it always matches `Button`'s theming exactly,… | `actions` |
| 5 | `SplitButton` | extends `StylelessSplitButtonProps` (from `@nebula-lab/styleless/split-button`) | A primary action button attached to a second, smaller trigger button (typically an `IconButton` chevron opening a `DropdownMenu` of related actions). | `actions` |
| 6 | `Tree` | extends `HeadlessTreeProps` (from `@nebula-lab/headless/tree`) | Root `role="tree"` list — sets the base text size/color every nested `TreeItem` inherits. | `data-display/tree` |
| 7 | `TreeItem` | extends `HeadlessTreeItemProps` (from `@nebula-lab/headless/tree`) | One row — hover/selected fills read `--tree-hover-bg`/`--tree-selected-bg` (+ `-selected-text`) from `../tokens/component.ts`. | `data-display/tree` |
| 8 | `TreeItemToggle` | extends `HeadlessTreeItemToggleProps` (from `@nebula-lab/headless/tree`) | Small chevron button — rotates 90° when `data-state="expanded"` so a single `▸` glyph doubles as both states without a separate icon swap. | `data-display/tree` |
| 9 | `TreeGroup` | extends `HeadlessTreeGroupProps` (from `@nebula-lab/headless/tree`) | Nested `<ul>` for one item's children — indented so depth is visible; each further-nested `TreeGroup` compounds the same `pl-4`, same convention as `AccordionContent`'s single fixed padding rather than a depth-aware calculation. | `data-display/tree` |
| 10 | `TreeView` | — | (no doc comment found) | `data-display/tree` |
| 11 | `TreeViewItem` | — | (no doc comment found) | `data-display/tree` |
| 12 | `TreeViewItemToggle` | — | (no doc comment found) | `data-display/tree` |
| 13 | `TreeViewGroup` | — | (no doc comment found) | `data-display/tree` |
| 14 | `Accordion` | extends `HeadlessAccordionProps` (from `@nebula-lab/headless/accordion`) | Styled wrapper around `@nebula-lab/headless`'s `Accordion` — all ARIA/keyboard behavior (single/multiple, roving-tabindex, `collapsible`) comes from there unchanged; this layer only adds visual styling, per `react-ui`'s job description in `ARCHITECTURE.md`. | `data-display` |
| 15 | `AccordionItem` | extends `HeadlessAccordionItemProps` (from `@nebula-lab/headless/accordion`) | Adds the border between items — see `../tokens/component.ts`'s `accordionTokens`. | `data-display` |
| 16 | `AccordionHeader` | extends `HeadlessAccordionHeaderProps` (from `@nebula-lab/headless/accordion`) | Purely structural (a heading wrapper) — `flex` so `AccordionTrigger`'s chevron can sit at the far end. | `data-display` |
| 17 | `AccordionTrigger` | extends `HeadlessAccordionTriggerProps` (from `@nebula-lab/headless/accordion`) | Adds hover/focus styling and a chevron that rotates 180° off `data-state="open"` (set by the underlying `@nebula-lab/headless` trigger) — a plain CSS transform driven by a data attribute, no JS/animation library. | `data-display` |
| 18 | `AccordionContent` | extends `HeadlessAccordionContentProps` (from `@nebula-lab/headless/accordion`) | (no doc comment found) | `data-display` |
| 19 | `Avatar` | extends `StylelessAvatarProps` (from `@nebula-lab/styleless/avatar`)<br>CVA variant props from `avatarVariants` (see this file's own `avatarVariants` recipe for the full option list). | Styled wrapper around `@nebula-lab/styleless`'s `Avatar` — the image load/error tracking that lets `AvatarFallback` know when to render comes from there unchanged (moved down this session, since it's reusable non-visual state with no styling opinion, same bucket `PasswordInput`'s visibility toggle already lives in). | `data-display` |
| 20 | `AvatarImage` | extends `StylelessAvatarImageProps` (from `@nebula-lab/styleless/avatar`) | Styled wrapper around `@nebula-lab/styleless`'s `AvatarImage` — the load/error reporting to `Avatar`'s context, and hiding the `img` entirely on error, come from there unchanged. | `data-display` |
| 21 | `AvatarFallback` | extends `StylelessAvatarFallbackProps` (from `@nebula-lab/styleless/avatar`) | Styled wrapper around `@nebula-lab/styleless`'s `AvatarFallback` — the loading-status check and `delayMs` timer come from there unchanged. | `data-display` |
| 22 | `AvatarGroup` | extends `StylelessAvatarGroupProps` (from `@nebula-lab/styleless/avatar-group`) | Styled wrapper around `@nebula-lab/styleless`'s `AvatarGroup` — the `max` truncation math comes from there unchanged. | `data-display` |
| 23 | `Badge` | extends `Omit<PrimitivePropsWithRef<'span'>, 'color'>`<br>extends `VariantProps<typeof badgeVariants>` | A small status/count pill. | `data-display` |
| 24 | `Card` | Inherits all standard `'div'` attributes.<br>CVA variant props from `cardVariants` (see this file's own `cardVariants` recipe for the full option list). | A bordered container — `CardHeader`/`CardTitle`/`CardDescription`/ `CardContent`/`CardFooter` are purely presentational structure, no behavior, so (unlike `Accordion`/`Dialog`/`Popover`) there's no matching `@nebula-lab/headless` compound underneath this one; it's a thin `cn()` wrapper around `Primitive` directly,… | `data-display` |
| 25 | `CardHeader` | `bordered?: boolean` — Renders a dashed divider under the header, visually separating it from `CardContent` — set `false` if the header shouldn't be visually…<br>`icon?: React.ReactNode` — An icon (or any node) rendered beside the header's title/description stack, e.g. a section glyph next to `CardTitle`. Treated as decorative…<br>extends `CardProps` | An icon (or any node) rendered beside the header's title/description stack, e.g. a section glyph next to `CardTitle`. | `data-display` |
| 26 | `CardTitle` | Inherits all standard `'h3'` attributes. | An icon (or any node) rendered beside the header's title/description stack, e.g. a section glyph next to `CardTitle`. | `data-display` |
| 27 | `CardDescription` | Inherits all standard `'p'` attributes. | An icon (or any node) rendered beside the header's title/description stack, e.g. a section glyph next to `CardTitle`. | `data-display` |
| 28 | `CardContent` | — | An icon (or any node) rendered beside the header's title/description stack, e.g. a section glyph next to `CardTitle`. | `data-display` |
| 29 | `CardFooter` | — | An icon (or any node) rendered beside the header's title/description stack, e.g. a section glyph next to `CardTitle`. | `data-display` |
| 30 | `CardListItem` | `avatar?: React.ReactNode` — A leading `Avatar` — mutually exclusive with `icon` in practice (pass whichever fits the row), rendered in the same `h-10 w-10 shrink-0`…<br>`icon?: React.ReactNode` — A leading decorative icon, alternative to `avatar` for rows with no natural image (a transaction category, a file type) — rendered…<br>`title: React.ReactNode`<br>`description?: React.ReactNode`<br>`trailing?: React.ReactNode` — Trailing content on the row's right edge — an amount, a status `Badge`, a timestamp. Sits to the left of the overflow menu when both are…<br>`actions?: React.ReactNode` — `MenuItem`s for the row's overflow menu — same convention `DataTableBlock`/`ListingCard`'s row actions use. Omit to render no overflow…<br>`actionsLabel?: string`<br>`onClick?: () => void`<br>`className?: string`<br>`classNames?: CardListItemClassNames` — Per-slot class overrides — see `CardListItemClassNames`. | One row of a table's data rendered as a self-contained card instead of a `<tr>` — the small-screen fallback `DataTableBlock` switches to below its `cardBreakpoint` (a `<table>`'s fixed columns don't reflow onto a narrow viewport the way a stacked list of cards does), but also usable on its own for any "list of… | `data-display` |
| 31 | `Carousel` | `count: number` — Total number of slides — known upfront by the consumer (unlike `Tree`'s dynamic nesting), so it's a plain prop rather than something…<br>`index?: number` — Controlled: the current (0-indexed) slide.<br>`defaultIndex?: number` — Uncontrolled: the initial (0-indexed) slide.<br>`onIndexChange?: (index: number) => void`<br>`loop?: boolean` — Whether `CarouselNext`/`CarouselPrevious` wrap past the first/last slide instead of disabling at the bounds.<br>`orientation?: CarouselOrientation`<br>`autoSwipeInterval?: number` — Auto-advances the index on this interval (ms) — `undefined` (the default) means off. Advancing pauses while the user is actively dragging,…<br>Inherits all standard `'div'` attributes. | Root of the Carousel compound component. | `data-display` |
| 32 | `CarouselContent` | Inherits all standard `'div'` attributes. | The clipping viewport around the slide track — `CarouselItem`s are the track's flex children, each `shrink-0 w-full` (or `h-full` when vertical), and the whole track translates by `-index * 100%` via an inline `style` (a CSS transform, not scroll position, so `CarouselPrevious`/`Next`'s click-driven navigation doesn't… | `data-display` |
| 33 | `CarouselItem` | `index: number` — This slide's position (0-indexed) — supplied by the consumer, same convention `StepperItem`'s `index` prop uses, since a carousel's slide…<br>Inherits all standard `'div'` attributes. | `role="group"` + `aria-roledescription="slide"` per the WAI-ARIA Carousel pattern — `aria-label` announces this slide's position (`"2 of 5"`), read from `Carousel`'s `count`. | `data-display` |
| 34 | `CarouselPrevious` | Inherits all standard `'button'` attributes. | A round icon button — disabled at the first slide unless `Carousel`'s `loop` is set. | `data-display` |
| 35 | `CarouselNext` | Inherits all standard `'button'` attributes. | A round icon button — disabled at the last slide unless `Carousel`'s `loop` is set. | `data-display` |
| 36 | `CarouselIndicators` | `overlay?: boolean` — `true` (the default) absolutely positions the dot row over the trailing edge of the slide, matching how a full-bleed image carousel usually…<br>Inherits all standard `'div'` attributes. | Renders `count` dot buttons directly, one per slide — no separate `CarouselIndicator` sub-component/export, since a dot's only job is "jump to this index," which needs nothing from the consumer beyond which index it is (same "just render it inline" call `CommandGroup` makes for its `heading` rather than a separate… | `data-display` |
| 37 | `Chip` | `onDismiss?: () => void` — Called when the built-in "×" button is clicked — omit entirely for a non-dismissible chip (reach for `Tag` if it's never meant to be…<br>`dismissLabel?: string` — Accessible label for the dismiss button. Required whenever `onDismiss` is given.<br>extends `Omit<PrimitivePropsWithRef<'span'>, 'color'>`<br>extends `VariantProps<typeof chipVariants>` | The interactive/removable pill `Badge`'s own doc comment points at building separately rather than growing an `onDismiss` prop onto itself — this is that component. | `data-display` |
| 38 | `Collapsible` | — | (no doc comment found) | `data-display` |
| 39 | `CollapsibleTrigger` | extends `HeadlessCollapsibleTriggerProps` (from `@nebula-lab/headless/collapsible`) | (no doc comment found) | `data-display` |
| 40 | `CollapsibleContent` | extends `HeadlessCollapsibleContentProps` (from `@nebula-lab/headless/collapsible`) | No animation built in — pass `className` with `data-[state=open]:animate-in`/`data-[state=closed]:animate-out` (or a plain CSS transition) to animate expand/collapse, per the headless source's own doc comment. | `data-display` |
| 41 | `DataGrid` | `className?: string`<br>extends `Omit<StylelessDataGridProps<T>, 'classNames'>` | Themed wrapper over `@nebula-lab/styleless`'s `DataGrid` — a virtualized, WAI-ARIA Grid-pattern row/column grid (see that package's own doc comment for why this is a separate component from `DataTable`, not a variant of it: virtualization needs absolutely-positioned rows, which a native `<table>`'s row model resists). | `data-display` |
| 42 | `DataTable` | `className?: string`<br>extends `StylelessDataTableProps` (from `@nebula-lab/styleless/data-table`) | Wraps `@nebula-lab/styleless`'s `DataTable` (which owns the real behavior: controllable sort state, controllable row-selection state, both threaded to every descendant via context) and adds only the table's own Tailwind classes — this file's whole job is visual, matching every other `react-ui` wrapper's division of… | `data-display` |
| 43 | `DataTableHeader` | Inherits all standard `'thead'` attributes. | (no doc comment found) | `data-display` |
| 44 | `DataTableBody` | Inherits all standard `'tbody'` attributes. | (no doc comment found) | `data-display` |
| 45 | `DataTableRow` | `className?: string`<br>extends `StylelessDataTableRowProps` (from `@nebula-lab/styleless/data-table`) | Wraps `@nebula-lab/styleless`'s `DataTableRow` (which owns the real behavior: computing `data-state="selected"` from `DataTable`'s selection, via `id`) and adds only the hover/selected-tint classes — a selected row is visible even without looking at its checkbox. | `data-display` |
| 46 | `DataTableHead` | `className?: string`<br>extends `Omit<StylelessDataTableHeadProps, 'classNames'>` | Wraps `@nebula-lab/styleless`'s `DataTableHead` (which owns the real behavior: the sort `<button>`, `aria-sort` computation, and `data-active`/ `data-direction` on the `<th>`) and supplies every part's classes via its `classNames` prop — the sort-direction chevron reacts to the `<th>`'s own… | `data-display` |
| 47 | `DataTableCell` | Inherits all standard `'td'` attributes. | (no doc comment found) | `data-display` |
| 48 | `DataTableCaption` | Inherits all standard `'caption'` attributes. | (no doc comment found) | `data-display` |
| 49 | `DataTableSelectionCell` | `id: string` — The row's selection id — same id passed to the enclosing `DataTableRow`.<br>`aria-label?: string` — Accessible label for the checkbox — a row's own visible cells rarely double as one, unlike most other checkbox use in this project. | A `<td>` holding one row's selection `Checkbox`, wired directly to `DataTable`'s selection state. | `data-display` |
| 50 | `DataTableSelectAllCell` | `ids: string[]` — Every selectable row's id — known upfront by the consumer (same convention `Carousel`'s `count` and `StepperItem`'s `index` use), since…<br>`aria-label?: string` | A `<th>` holding the header row's "select all" `Checkbox` — `indeterminate` when some but not all of `ids` are selected, `checked` when all are, unchecked otherwise. | `data-display` |
| 51 | `DescriptionList` | Inherits all standard `'dl'` attributes. | A `<dl>` of key/value pairs — `DescriptionTerm`/`DescriptionDetails` are purely presentational structure, same "no matching lower-layer compound" treatment `Stat`/`Card` document. | `data-display` |
| 52 | `DescriptionTerm` | Inherits all standard `'dt'` attributes. | A `<dl>` of key/value pairs — `DescriptionTerm`/`DescriptionDetails` are purely presentational structure, same "no matching lower-layer compound" treatment `Stat`/`Card` document. | `data-display` |
| 53 | `DescriptionDetails` | Inherits all standard `'dd'` attributes. | A `<dl>` of key/value pairs — `DescriptionTerm`/`DescriptionDetails` are purely presentational structure, same "no matching lower-layer compound" treatment `Stat`/`Card` document. | `data-display` |
| 54 | `EmptyState` | Inherits all standard `'div'` attributes. | A placeholder for an empty list/search-result/inbox — `EmptyStateIcon`/ `EmptyStateTitle`/`EmptyStateDescription`/`EmptyStateAction` are purely presentational structure, same "no matching `@nebula-lab/headless` or `@nebula-lab/styleless` compound" treatment `Card`/`Stat`/`Timeline` already document (nothing here is a… | `data-display` |
| 55 | `EmptyStateIcon` | Inherits all standard `'div'` attributes. | Treated as decorative (`aria-hidden`) — same convention `CardHeader`'s icon slot uses. | `data-display` |
| 56 | `EmptyStateTitle` | Inherits all standard `'h3'` attributes. | Treated as decorative (`aria-hidden`) — same convention `CardHeader`'s icon slot uses. | `data-display` |
| 57 | `EmptyStateDescription` | Inherits all standard `'p'` attributes. | Treated as decorative (`aria-hidden`) — same convention `CardHeader`'s icon slot uses. | `data-display` |
| 58 | `EmptyStateAction` | Inherits all standard `'div'` attributes. | Treated as decorative (`aria-hidden`) — same convention `CardHeader`'s icon slot uses. | `data-display` |
| 59 | `FilterBar` | `filters: FilterBarFilter[]` — No sensible default — pass `[]` for an empty (but still usable, if `addOptions` is given) bar.<br>`onRemoveFilter?: (id: string) => void`<br>`onClearAll?: () => void`<br>`addOptions?: FilterBarAddOption[]` — Options offered in the "Add filter" dropdown — omit entirely to hide that control (e.g. a read-only filter summary).<br>`onAddFilter?: (id: string) => void`<br>`className?: string` | A row of active filter chips (`Chip`'s own built-in `onDismiss`, no custom dismiss-button logic needed here) plus an optional "Add filter" dropdown and a "Clear all" action. | `data-display` |
| 60 | `List` | `ordered?: boolean` — Renders `<ol>` instead of `<ul>` — set when item order is meaningful (matches the native semantics `Breadcrumb`'s own `<ol>` choice…<br>`dense?: boolean` — Tightens every descendant `ListItem`/`ListItemButton`'s vertical padding — reflected as `data-dense` on the root and read by both via…<br>Inherits all standard `'ul'` attributes. | A plain list of `ListItem`s — purely presentational structure, no `@nebula-lab/headless` compound underneath (unlike `Menu`/`Tree`, this has no interactive selection/focus behavior at all; for that, reach for `Listbox`/`Menu` instead). | `data-display` |
| 61 | `ListItem` | `disablePadding?: boolean` — Omit the default horizontal/vertical padding — set this when the item's real content is a full-bleed `ListItemButton`, which supplies its…<br>Inherits all standard `'li'` attributes. | One row in a `List` — `position: relative` by default so a `ListItemSecondaryAction` (absolutely positioned) has a positioning context to anchor to regardless of whether this item's main content is plain text, a `ListItemButton`, or a custom composition. | `data-display` |
| 62 | `ListItemButton` | `selected?: boolean` — Fills the row with `--list-item-selected-bg`/`-text` — for a row representing the current selection (an active nav link, the current…<br>Inherits all standard `'button'` attributes. | The clickable variant of a list row — a real `<button>`, so it comes with native keyboard operability (Tab/Enter/Space) and `disabled` handling for free, the same reasoning `MenuItem`/`Tab` document for reaching for a real interactive element instead of a `<div onClick>`. | `data-display` |
| 63 | `ListItemIcon` | Inherits all standard `'span'` attributes. | A leading, decorative icon slot — `aria-hidden`, since the row's accessible name comes from `ListItemText`/the button's own visible text, same convention `CardHeader`'s optional `icon` prop documents. | `data-display` |
| 64 | `ListItemAvatar` | Inherits all standard `'span'` attributes. | A leading avatar slot — just a `shrink-0` wrapper around whatever `Avatar` composition is passed as `children`; owns no avatar logic itself (that's `Avatar`'s job), mirroring `ListItemIcon`'s "layout slot, not a component" role. | `data-display` |
| 65 | `ListItemText` | `primary?: React.ReactNode` — The row's main line — renders as `children` instead when omitted, so `<ListItemText>Plain label</ListItemText>` still works for a…<br>`secondary?: React.ReactNode` — An optional second line below `primary`, e.g. an email, a timestamp, a preview snippet.<br>Inherits all standard `'div'` attributes. | The two-line primary/secondary text stack every list row's main content resolves to — `min-w-0 flex-1` so it truncates correctly instead of pushing a trailing `ListItemSecondaryAction`/trailing content out of the row when the text is long, the same `min-w-0`/`truncate` fix `DashboardOverview`'s trend text needed for… | `data-display` |
| 66 | `ListItemSecondaryAction` | Inherits all standard `'div'` attributes. | A trailing slot (an `IconButton`, a `Switch`, a timestamp) pinned to the row's right edge — absolutely positioned against `ListItem`'s own `position: relative`, so it sits outside `ListItemButton`'s clickable area instead of nesting one interactive control inside another, an invalid ARIA structure `PlanCards`' own doc… | `data-display` |
| 67 | `ListSubheader` | Inherits all standard `'li'` attributes. | A non-interactive section label between groups of `ListItem`s — a real `<li>` (valid anywhere inside a `<ul>`/`<ol>`, unlike a `<div>`), `sticky top-0` by default so it stays visible while its section scrolls past. | `data-display` |
| 68 | `PaymentMethodList` | `methods: PaymentMethod[]`<br>`onSetDefault?: (id: string) => void` — Omit to hide the "Set as default" row action entirely.<br>`onRemove?: (id: string) => void` — Omit to hide the "Remove" row action entirely.<br>`onAdd?: () => void` — Omit to hide the "Add payment method" button.<br>`className?: string`<br>`classNames?: PaymentMethodListClassNames` — Per-slot class overrides — see `PaymentMethodListClassNames`. | A saved payment method list — Minimals' Account → Billing "Payment Method Manager" (§3.10 SaaS/Billing). | `data-display` |
| 69 | `Sparkline` | `data: number[]` — The series to plot — order matters, values don't need to be sorted or non-negative.<br>`variant?: 'line' \| 'area'` — `line` draws just the stroke; `area` adds a fill fading to transparent down to the baseline, for the "dark stat card" look.<br>`strokeWidth?: number`<br>Inherits all standard `'svg'` attributes. | A dependency-free inline mini line/area chart — no matching `@nebula-lab/headless` compound, same "purely presentational, no ARIA behavior of its own" treatment `Stat`/`Timeline`/`EmptyState` already document. | `data-display` |
| 70 | `Stat` | Inherits all standard `'div'` attributes. | A single metric display — `StatLabel`/`StatValue`/`StatDescription` are purely presentational structure, same "no matching `@nebula-lab/headless` compound" treatment `Card` documents (single-file compound, not a folder-per-sub-part family, since none of these have behavior of their own either). | `data-display` |
| 71 | `StatLabel` | Inherits all standard `'p'` attributes. | A single metric display — `StatLabel`/`StatValue`/`StatDescription` are purely presentational structure, same "no matching `@nebula-lab/headless` compound" treatment `Card` documents (single-file compound, not a folder-per-sub-part family, since none of these have behavior of their own either). | `data-display` |
| 72 | `StatValue` | Inherits all standard `'p'` attributes. | A single metric display — `StatLabel`/`StatValue`/`StatDescription` are purely presentational structure, same "no matching `@nebula-lab/headless` compound" treatment `Card` documents (single-file compound, not a folder-per-sub-part family, since none of these have behavior of their own either). | `data-display` |
| 73 | `StatDescription` | Inherits all standard `'p'` attributes. | A single metric display — `StatLabel`/`StatValue`/`StatDescription` are purely presentational structure, same "no matching `@nebula-lab/headless` compound" treatment `Card` documents (single-file compound, not a folder-per-sub-part family, since none of these have behavior of their own either). | `data-display` |
| 74 | `StaticRating` | `value: number` — 0-5, need not be a whole number (e.g. `4.5`) — rounds to the nearest whole star for the filled count.<br>Inherits all standard `'span'` attributes. | A read-only star rating display — `n`/5 filled stars plus an `aria-label` announcing the score as a sentence. | `data-display` |
| 75 | `Tag` | `color?: VariantProps<typeof badgeVariants>['color']` — Same eight semantic color roles `Badge` exposes.<br>extends `Omit<PrimitivePropsWithRef<'span'>, 'color'>` | A non-interactive category/keyword label — reuses `Badge`'s own `badgeVariants` recipe pinned to `variant: 'outline'` (see `../badge/badge.tsx`) rather than a separate `tagVariants`, since `Tag` is exactly `Badge`'s outline shape with its own default color (`neutral`, vs. `Badge`'s `primary`) and no shape choice of… | `data-display` |
| 76 | `Timeline` | Inherits all standard `'ol'` attributes. | A vertical sequence of dated/ordered events — purely presentational structure, no matching `@nebula-lab/headless` compound, same treatment `List`/`Stat`/`Card` document (nothing here is a widget with its own ARIA behavior to separate out; it's a real `<ol>` since a timeline's items are inherently ordered, same… | `data-display` |
| 77 | `TimelineItem` | `last?: boolean` — Omits the connecting line below this item — set on the last item in the sequence so the line doesn't dangle past it.<br>Inherits all standard `'li'` attributes. | Omits the connecting line below this item — set on the last item in the sequence so the line doesn't dangle past it. | `data-display` |
| 78 | `TimelineTitle` | Inherits all standard `'p'` attributes. | Omits the connecting line below this item — set on the last item in the sequence so the line doesn't dangle past it. | `data-display` |
| 79 | `TimelineDescription` | Inherits all standard `'p'` attributes. | Omits the connecting line below this item — set on the last item in the sequence so the line doesn't dangle past it. | `data-display` |
| 80 | `TreeTable` | `data: T[]`<br>`columns: TreeTableColumn<T>[]`<br>`defaultExpanded?: string[]` — Row ids expanded by default, before any toggle.<br>`indent?: number` — Indent per depth level, in pixels.<br>extends `Omit<DataTableProps, 'children' \| 'data'>` | A hierarchical table — composes react-ui's own `DataTable` parts (`DataTableHeader`/`DataTableBody`/`DataTableRow`/`DataTableHead`/ `DataTableCell`) rather than reinventing table chrome, the same way `AlertDialog`/`Menubar` reuse an existing sibling's building blocks. | `data-display` |
| 81 | `VirtualList` | `items: T[]` — The full (virtual) item set — not just the currently visible slice; `useVirtualizer` decides which of these actually render.<br>`estimateItemHeight?: number` — Best-guess row height (px) before a row's real rendered height is measured — see `useVirtualizer`'s `estimateSize`.<br>`height: number` — Fixed viewport height (px) of the scrollable region.<br>`getItemId?: (item: T, index: number) => string \| number` — Returns a stable id for an item — used as the rendered row's `key` instead of array index, so React doesn't misattribute state across…<br>`renderItem: (item: T, index: number) => React.ReactNode`<br>`className?: string`<br>`itemClassName?: string` | A virtualized single-column list — composes `@nebula-lab/hooks`' `useVirtualizer` directly, the same "pure windowing math, no ARIA behavior split worth decoupling" project-owner call `DataGrid` documents (no `@nebula-lab/headless`/ `@nebula-lab/styleless` layer underneath). | `data-display` |
| 82 | `Draggable` | `id: string` — Identifies this item to whatever `Droppable`/`SortableItem` it's dropped onto.<br>`disabled?: boolean`<br>Inherits all standard `'div'` attributes. | Makes its content draggable via the native HTML5 Drag and Drop API (`draggable`, `dragstart`/`dragend`) — built directly in `react-ui`, no `@nebula-lab/headless` layer underneath (project owner decision, see `AGENTS.md`'s `react-ui` row): drag-and-drop *is* its own behavior, but the native API it's built on has no… | `drag-and-drop` |
| 83 | `NEBULA_DRAG_MIME_TYPE` | `id: string` — Identifies this item to whatever `Droppable`/`SortableItem` it's dropped onto.<br>`disabled?: boolean`<br>Inherits all standard `'div'` attributes. | The MIME type this project's drag-and-drop components read/write on `DataTransfer` — namespaced so a page with its own unrelated native drag-and-drop doesn't collide with it. | `drag-and-drop` |
| 84 | `Droppable` | `disabled?: boolean`<br>`onDrop?: (draggedId: string, event: React.DragEvent<HTMLDivElement>) => void` — Called with the dropped `Draggable`'s `id` once a drop actually lands here — not called for a drag that leaves without dropping.<br>extends `Omit<PrimitivePropsWithRef<'div'>, 'onDrop' \| 'onDragOver'>` | Accepts a `Draggable`'s drop via the native HTML5 Drag and Drop API. | `drag-and-drop` |
| 85 | `Sortable` | `items: string[]` — The list's current order, by id — the source of truth `SortableItem`s reorder against.<br>`onReorder: (items: string[]) => void`<br>`orientation?: 'vertical' \| 'horizontal'` — Purely a layout hint for the built-in `flex-col`/`flex-row` styling — reordering logic itself doesn't depend on it.<br>Inherits all standard `'div'` attributes. | Root of the Sortable compound component — reorders a list of `SortableItem`s by combining `Draggable`'s drag-start and `Droppable`'s drop-target behavior directly into each item (rather than composing separate `Draggable`/`Droppable` components under the hood), since a sortable item is always both at once: everything… | `drag-and-drop` |
| 86 | `SortableItem` | `id: string`<br>`disabled?: boolean`<br>extends `Omit<PrimitivePropsWithRef<'div'>, 'onDrop' \| 'onDragOver'>` | One reorderable row/card — both draggable (drag-starts carry its own `id`) and a drop target (a drop from another `SortableItem` in the same `Sortable` moves that item to just before this one, via `sortable-context.ts`'s `moveItem`). | `drag-and-drop` |
| 87 | `SwipeableCards` | `items: T[]` — The full card set, front to back.<br>`renderCard: (item: T, index: number) => React.ReactNode`<br>`index?: number` — Controlled: the current (0-indexed, frontmost) card.<br>`defaultIndex?: number` — Uncontrolled: the initial frontmost card.<br>`onIndexChange?: (index: number) => void`<br>`loop?: boolean` — Whether swiping past the last/first card wraps instead of resisting.<br>`getItemId?: (item: T, index: number) => string \| number` — Stable id for a card — used as its `key` instead of array index.<br>`className?: string`<br>`cardClassName?: string`<br>`aria-label?: string` | A stacked deck of cards you swipe through — no `CarouselPrevious`/`Next` buttons, unlike `Carousel`: the interaction model here (think a mobile banking app's debit/credit card switcher) is "swipe the front card away," not "step through a linear track," so a fanned 3D stack with peeking neighbors is the right visual,… | `drag-and-drop` |
| 88 | `Alert` | `title?: React.ReactNode`<br>`icon?: React.ReactNode` — Defaults to a severity-appropriate icon per `color`. Pass `null` to render no icon at all.<br>`onDismiss?: () => void` — Called when the built-in dismiss button is clicked — omit entirely for a non-dismissible alert.<br>`dismissLabel?: string` — Accessible label for the dismiss button. Required whenever `onDismiss` is given.<br>extends `Omit<PrimitivePropsWithRef<'div'>, 'color'>`<br>extends `VariantProps<typeof alertVariants>` | A status message box — `role="alert"` for assertive, auto-announced severities (`warning`/`danger`); `info`/`success` get `role="status"` instead, since a polite, non-interrupting announcement is more appropriate for good-news/neutral messages (mirrors the WAI-ARIA distinction between `alert` and `status` live… | `feedback` |
| 89 | `AlertPopup` | `trigger?: React.ReactNode` — Element that opens the popup, e.g. a `Button` — rendered `asChild` via `AlertDialogTrigger`. Omit when you drive `open` externally instead…<br>`open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`icon?: AlertPopupIcon \| React.ReactNode \| null` — One of the 5 built-in severity icons, a custom node, or `null` for no icon at all — see `isIconKeyword` above for how a custom node that…<br>`title: React.ReactNode`<br>`description?: React.ReactNode`<br>`primaryAction: AlertPopupAction` — Always closes the popup on click, same as `AlertDialogAction`'s own contract — filled, colored to match `icon`'s severity.<br>`secondaryAction?: AlertPopupAction` — Plain bordered button beside `primaryAction` — omit for a single-button ("OK") popup.<br>`backdrop?: AlertDialogOverlayProps['backdrop']` — Passed straight through to the overlay — see `AlertDialogOverlay`'s own `backdrop` doc.<br>`blurIntensity?: AlertDialogOverlayProps['blurIntensity']` — Passed straight through to the overlay — see `AlertDialogOverlay`'s own `blurIntensity` doc; ignored when `backdrop="solid"`.<br>`className?: string` | A ready-to-use confirmation/status popup — a ["SweetAlert"](https://sweetalert2.github.io/)-style centered icon, title, message, and action button(s) — versus `AlertDialog`, which is the same underlying primitive but requires assembling the title/ description/action layout yourself. | `feedback` |
| 90 | `CircularProgress` | `size?: number` — Diameter in pixels.<br>`strokeWidth?: number` — Stroke width in pixels.<br>`color?: ProgressColor` — Track/indicator color pair — same eight-role palette `Progress`/`Badge`/`Chip` use.<br>extends `Omit<HeadlessProgressProps, 'color'>` | An SVG-ring variant of `Progress` — same `@nebula-lab/headless` `Progress` underneath (unchanged `role="progressbar"` + `aria-valuenow`/`valuemin`/ `valuemax` semantics), just a different visual: a ring built from two stacked `<circle>`s (a dim full track, a bright arc sized via `stroke-dasharray`/`stroke-dashoffset`)… | `feedback` |
| 91 | `Progress` | extends `HeadlessProgressProps` (from `@nebula-lab/headless/progress`)<br>CVA variant props from `progressTrackVariants` (see this file's own `progressTrackVariants` recipe for the full option list). | Styled wrapper around `@nebula-lab/headless`'s `Progress`/`ProgressIndicator` pair — the `role="progressbar"` semantics, `aria-valuenow`/`valuemin`/ `valuemax`/`valuetext`, and indeterminate handling all come from there unchanged. | `feedback` |
| 92 | `Skeleton` | extends `HeadlessSkeletonProps` (from `@nebula-lab/headless/skeleton`)<br>CVA variant props from `skeletonVariants` (see this file's own `skeletonVariants` recipe for the full option list). | Styled wrapper around `@nebula-lab/headless`'s `Skeleton` — the `aria-hidden` behavior comes from there unchanged. | `feedback` |
| 93 | `Spinner` | extends `HeadlessSpinnerProps` (from `@nebula-lab/headless/spinner`)<br>CVA variant props from `spinnerVariants` (see this file's own `spinnerVariants` recipe for the full option list). | Styled wrapper around `@nebula-lab/headless`'s `Spinner` — the `role="status"` + visually-hidden label announcement come from there unchanged. | `feedback` |
| 94 | `Toast` | extends `HeadlessToastProps` (from `@nebula-lab/headless/toast`) | A single notification card — reads `--toast-bg`/`-text`/`-border` (`neutral`/`neutral-content`, same "pop off the page" reasoning `tooltipTokens` uses — see `../tokens/component.ts`). | `feedback` |
| 95 | `ToastPortal` | — | (no doc comment found) | `feedback` |
| 96 | `ToastViewport` | extends `HeadlessToastViewportProps` (from `@nebula-lab/headless/toast`) | Fixed to the bottom-right corner, stacking newest-on-top — the conventional toast placement; override `className` for a different corner. | `feedback` |
| 97 | `ToastTitle` | extends `HeadlessToastTitleProps` (from `@nebula-lab/headless/toast`) | (no doc comment found) | `feedback` |
| 98 | `ToastDescription` | extends `HeadlessToastDescriptionProps` (from `@nebula-lab/headless/toast`) | (no doc comment found) | `feedback` |
| 99 | `ToastAction` | extends `HeadlessToastActionProps` (from `@nebula-lab/headless/toast`) | An underlined text action (e.g. "Undo") — deliberately not a full filled `Button`, so it reads as secondary to the toast's own message. | `feedback` |
| 100 | `ToastClose` | extends `HeadlessToastCloseProps` (from `@nebula-lab/headless/toast`) | (no doc comment found) | `feedback` |
| 101 | `Autocomplete` | — | (no doc comment found) | `forms/combobox` |
| 102 | `AutocompleteInput` | — | (no doc comment found) | `forms/combobox` |
| 103 | `AutocompletePortal` | — | (no doc comment found) | `forms/combobox` |
| 104 | `AutocompleteContent` | — | (no doc comment found) | `forms/combobox` |
| 105 | `AutocompleteItem` | — | (no doc comment found) | `forms/combobox` |
| 106 | `Combobox` | — | (no doc comment found) | `forms/combobox` |
| 107 | `ComboboxInput` | extends `HeadlessComboboxInputProps` (from `@nebula-lab/headless/combobox`) | Styled identically to `Input` — a combobox's text field visually is one. | `forms/combobox` |
| 108 | `ComboboxPortal` | — | (no doc comment found) | `forms/combobox` |
| 109 | `ComboboxContent` | extends `HeadlessComboboxContentProps` (from `@nebula-lab/headless/combobox`) | Same surface treatment as `SelectContent`/`PopoverContent`. | `forms/combobox` |
| 110 | `ComboboxItem` | extends `HeadlessComboboxItemProps` (from `@nebula-lab/headless/combobox`) | `data-highlighted` (virtual, via `aria-activedescendant` — see the headless source) drives the fill, same "state attribute, not real focus" pattern `Command`'s items use, since real focus always stays on `ComboboxInput` here too. | `forms/combobox` |
| 111 | `Calendar` | extends `CalendarSingleProps \| CalendarRangeProps` | A month-grid date picker core — the WAI-ARIA [Date Picker Dialog](https://www.w3.org/WAI/ARIA/apg/patterns/dialog-modal/examples/datepicker-dialog/) grid pattern: `role="grid"` over day `role="gridcell"`s, each a real `<button>`, with a single roving tabindex (only the focused date is `tabIndex={0}`; every other date… | `forms` |
| 112 | `Checkbox` | extends `HeadlessCheckboxProps` (from `@nebula-lab/headless/checkbox`) | Styled wrapper around `@nebula-lab/headless`'s `Checkbox` — tri-state behavior (`checked`/`false`/`'indeterminate'`), keyboard handling (Space), and hidden-native-input form participation all come from there unchanged. | `forms` |
| 113 | `CheckboxGroup` | extends `HeadlessCheckboxGroupProps` (from `@nebula-lab/headless/checkbox-group`) | (no doc comment found) | `forms` |
| 114 | `CheckboxGroupItem` | extends `HeadlessCheckboxGroupItemProps` (from `@nebula-lab/headless/checkbox-group`) | Same box + checkmark treatment as this package's own `Checkbox` (see that component's doc comment) — the headless source renders the identical underlying `Checkbox` DOM structure per item, this only exists as a thin wrapper because item styling needs re-applying since we can't wrap `react-ui`'s already-styled… | `forms` |
| 115 | `ColorPicker` | — | (no doc comment found) | `forms` |
| 116 | `ColorPickerTrigger` | extends `HeadlessColorPickerTriggerProps` (from `@nebula-lab/headless/color-picker`) | The swatch button — unlike `PopoverTrigger`/`MenuTrigger` (typically an already-styled element passed `asChild`), this one needs its own chrome: the background color it displays *is* the current value (set inline by the headless source), so this only adds the border/rounding/focus-ring frame around that color, not a… | `forms` |
| 117 | `ColorPickerPortal` | — | (no doc comment found) | `forms` |
| 118 | `ColorPickerContent` | — | (no doc comment found) | `forms` |
| 119 | `ColorPickerHexInput` | extends `HeadlessColorPickerHexInputProps` (from `@nebula-lab/headless/color-picker`) | Built on `@nebula-lab/primitives`' bare `Input` (see the headless source), not this package's own styled `Input` — so this needs the same `--input-*` treatment applied directly, rather than getting it for free by wrapping `react-ui`'s `Input`. | `forms` |
| 120 | `DatePicker` | `value?: Date`<br>`defaultValue?: Date`<br>`onValueChange?: (date: Date \| undefined) => void`<br>`open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`minDate?: Date`<br>`maxDate?: Date`<br>`isDateDisabled?: (date: Date) => boolean`<br>`disabled?: boolean`<br>`placeholder?: React.ReactNode`<br>`formatDate?: (date: Date) => string`<br>`weekStartsOn?: 0 \| 1`<br>`locale?: string`<br>`className?: string` | A single self-contained component — not a compound (`DatePickerTrigger`/ `DatePickerContent`/etc.) — because unlike `MultiSelect`'s items or `AlertDialog`'s title/actions, there's no arbitrary consumer content to compose here: the trigger is always "a button showing the formatted date or a placeholder" and the popup… | `forms` |
| 121 | `DateRangePicker` | `value?: CalendarRangeValue`<br>`defaultValue?: CalendarRangeValue`<br>`onValueChange?: (range: CalendarRangeValue) => void`<br>`open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`minDate?: Date`<br>`maxDate?: Date`<br>`isDateDisabled?: (date: Date) => boolean`<br>`disabled?: boolean`<br>`placeholder?: React.ReactNode`<br>`formatDate?: (date: Date) => string`<br>`weekStartsOn?: 0 \| 1`<br>`locale?: string`<br>`className?: string` | `DatePicker`'s two-endpoint sibling — same self-contained (not compound) shape and same reasoning for why: the trigger and popup content are both fixed, nothing here is consumer-composed. | `forms` |
| 122 | `EmailField` | extends `StylelessEmailInputProps` (from `@nebula-lab/styleless/email-input`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Styled `type="email"` `Input` preset — wraps `@nebula-lab/styleless`'s `EmailInput` and reuses `Input`'s exact class recipe (`inputVariants`), same "preset over the one real `Input`" shape `SearchField` established. | `forms` |
| 123 | `Field` | extends `HeadlessFieldProps` (from `@nebula-lab/headless/field`) | Vertical stack layout for a label/control/description/error group — all the `id`/`aria-*` wiring between those sub-parts comes unchanged from the headless source. | `forms` |
| 124 | `FieldLabel` | extends `LabelProps` (from `@nebula-lab/primitives/label`) | (no doc comment found) | `forms` |
| 125 | `FieldControl` | — | (no doc comment found) | `forms` |
| 126 | `FieldDescription` | Inherits all standard `'p'` attributes. | (no doc comment found) | `forms` |
| 127 | `FieldError` | Inherits all standard `'p'` attributes. | (no doc comment found) | `forms` |
| 128 | `FileUpload` | — | (no doc comment found) | `forms` |
| 129 | `FileUploadDropzone` | extends `HeadlessFileUploadDropzoneProps` (from `@nebula-lab/headless/file-upload`) | Dashed drop-zone card — `data-dragging-over` (set by the headless source while a drag is over it) fills the border with `--file-upload-dropzone-active-border`. | `forms` |
| 130 | `FileUploadInput` | — | (no doc comment found) | `forms` |
| 131 | `FileUploadFileList` | extends `HeadlessFileUploadFileListProps` (from `@nebula-lab/headless/file-upload`) | (no doc comment found) | `forms` |
| 132 | `FileUploadFileItem` | extends `HeadlessFileUploadFileItemProps` (from `@nebula-lab/headless/file-upload`) | (no doc comment found) | `forms` |
| 133 | `FileUploadRemoveTrigger` | extends `HeadlessFileUploadRemoveTriggerProps` (from `@nebula-lab/headless/file-upload`) | Defaults to a small "×" icon when no `children` is given — same convention `BreadcrumbSeparator`'s default chevron uses. | `forms` |
| 134 | `Form` | — | Styled `Form` — wraps `@nebula-lab/primitives`' unstyled `Form` (which already defaults `noValidate` and calls `event.preventDefault()` before your `onSubmit` runs) with the vertical-stack field layout most forms in this design system already reach for by hand (`flex flex-col gap-4`). | `forms` |
| 135 | `ImageUpload` | `dropzoneLabel?: string` — Text shown inside the dropzone when empty.<br>`className?: string` — Applied to the outer wrapping `<div>` — `FileUpload`'s root renders no DOM of its own, so this component supplies the one wrapper element…<br>extends `Omit<FileUploadProps, 'accept' \| 'children'>` | A ready-to-use image uploader — composes `@nebula-lab/headless`'s `FileUpload` (defaulting `accept="image/*"`) with `@nebula-lab/styleless`'s `ImagePreview` (which owns the real object-URL-lifecycle behavior) inside a themed thumbnail grid. | `forms` |
| 136 | `Input` | extends `StylelessInputProps` (from `@nebula-lab/styleless/input`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Styled `input` — wraps `@nebula-lab/styleless`'s `Input` (itself a thin pass- through of `@nebula-lab/primitives`' `Input`, which gives `invalid` -> `aria-invalid` wiring and ref forwarding) rather than reaching past it to `primitives` directly, per `react-ui`'s "builds on `styleless`, not around it" rule (see… | `forms` |
| 137 | `MultiSelect` | `open?: boolean`<br>`defaultOpen?: boolean`<br>`onOpenChange?: (open: boolean) => void`<br>`value?: string[]` — Controlled: the currently selected values.<br>`defaultValue?: string[]`<br>`onValueChange?: (value: string[]) => void`<br>`disabled?: boolean`<br>`children?: React.ReactNode` | Root of the MultiSelect compound component — built directly in `react-ui` with no `@nebula-lab/headless` layer underneath, the same project-owner call `Carousel`/`DataTable`/`DataGrid` make: this composes two already-built pieces (this package's own `Popover` for anchor positioning, and `@nebula-lab/headless`'s… | `forms` |
| 138 | `MultiSelectTrigger` | `placeholder?: React.ReactNode` — Rendered when nothing is selected yet.<br>`maxLabels?: number` — Caps how many selected labels are spelled out before collapsing to "N selected".<br>extends `React.ComponentPropsWithoutRef<'button'>` | Styled like `SelectTrigger`/`Input` (same border/bg/text triple + rotating chevron), but its visible content is computed from `MultiSelect`'s own label registry rather than a single `SelectValue` child — up to `maxLabels` selected labels are spelled out comma-joined, beyond that it collapses to a `"N selected"`… | `forms` |
| 139 | `MultiSelectPortal` | — | (no doc comment found) | `forms` |
| 140 | `MultiSelectContent` | extends `HeadlessPopoverContentProps` (from `@nebula-lab/headless/popover`) | The popup panel — a `PopoverContent` (this package's own `--popover-*` surface treatment) with a `@nebula-lab/headless` `Listbox` (`type="multiple"`) rendered directly inside it as the one child, rather than two visually distinct layers. | `forms` |
| 141 | `MultiSelectItem` | `textValue?: string` — The accessible/displayed label registered with `MultiSelect` for `MultiSelectTrigger`'s summary — defaults to `children` when `children` is…<br>extends `ListboxOptionProps` | A built-in checkbox square (not a bare checkmark, unlike this package's own `SelectItem`) is the deliberate visual distinction — a multi-select option is *toggled*, not simply "the one current choice", so it reads more like `CheckboxGroupItem` than `SelectItem`; reuses `checkboxTokens`' exact box+checkmark treatment… | `forms` |
| 142 | `NativeSelect` | extends `PrimitiveNativeSelectProps` (from `@nebula-lab/primitives/native-select`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Styled native `<select>` — wraps `@nebula-lab/primitives`' unstyled `NativeSelect` (which already gives `invalid` -> `aria-invalid` wiring) with `Input`'s own `inputVariants` recipe, so a native select reads as pixel-identical to `Input`/`Select` rather than needing its own visual language. | `forms` |
| 143 | `NumberInput` | — | (no doc comment found) | `forms` |
| 144 | `NumberInputField` | extends `HeadlessNumberInputFieldProps` (from `@nebula-lab/headless/number-input`) | Same `--input-*` treatment `react-ui`'s own `Input` uses — built on `@nebula-lab/primitives`' bare `Input` (see the headless source), not this package's styled one, so needs it applied directly, same reasoning `ColorPickerHexInput` documents. | `forms` |
| 145 | `NumberInputIncrement` | extends `HeadlessNumberInputIncrementProps` (from `@nebula-lab/headless/number-input`) | Defaults to a "+" icon when no `children` is given — same convention `RatingItem`'s default star and `BreadcrumbSeparator`'s default chevron use. | `forms` |
| 146 | `NumberInputDecrement` | extends `HeadlessNumberInputDecrementProps` (from `@nebula-lab/headless/number-input`) | Defaults to a "-" icon when no `children` is given — see `NumberInputIncrement`. | `forms` |
| 147 | `OTPInput` | `className?: string`<br>extends `HeadlessOTPInputProps` (from `@nebula-lab/headless/otp-input`) | Lays out its `OTPInputSlot`s in a row — all the auto-advance/backspace/paste-splicing behavior comes unchanged from the headless source. | `forms` |
| 148 | `OTPInputSlot` | extends `HeadlessOTPInputSlotProps` (from `@nebula-lab/headless/otp-input`) | Built on `@nebula-lab/primitives`' bare `Input` (see the headless source), same `--input-*` treatment `ColorPickerHexInput`/`NumberInputField` apply directly for the same reason, sized/centered as one square digit box. | `forms` |
| 149 | `PasswordField` | extends `StylelessPasswordInputProps` (from `@nebula-lab/styleless/password-input`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Wraps `@nebula-lab/styleless`'s `PasswordInput` (which owns the real behavior: `visible` state, `type="password"\|"text"` swapping, `aria-pressed` wiring) and supplies the eye/eye-off icon pair via `renderToggle` plus the `absolute`-positioned toggle button styling via `toggleClassName` — this file's whole job is… | `forms` |
| 150 | `PasswordStrengthIndicator` | `className?: string`<br>extends `Omit<StylelessPasswordStrengthIndicatorProps, 'classNames'>` | Wraps `@nebula-lab/styleless`'s `PasswordStrengthIndicator` (which owns the real behavior: the length/character-class scoring heuristic and the `role="meter"` semantics) and supplies every part's Tailwind classes via its `classNames` prop — this file's whole job is visual, matching every other `react-ui` wrapper's… | `forms` |
| 151 | `RadioGroup` | extends `HeadlessRadioGroupProps` (from `@nebula-lab/headless/radio-group`) | Styled wrapper around `@nebula-lab/headless`'s `RadioGroup` — roving-tabindex arrow-key navigation and single-select state come from there unchanged. | `forms` |
| 152 | `RadioGroupItem` | extends `HeadlessRadioGroupItemProps` (from `@nebula-lab/headless/radio-group`) | Styled wrapper around `@nebula-lab/headless`'s `RadioGroupItem`. | `forms` |
| 153 | `Rating` | extends `HeadlessRatingProps` (from `@nebula-lab/headless/rating`) | (no doc comment found) | `forms` |
| 154 | `RatingItem` | extends `HeadlessRatingItemProps` (from `@nebula-lab/headless/rating`) | Renders a built-in filled/outline star by default (toggled purely off the headless source's `data-state`), same "no icon dependency" approach `AccordionTrigger`'s chevron uses — pass `children` to swap in a different icon entirely. | `forms` |
| 155 | `SearchField` | extends `StylelessSearchInputProps` (from `@nebula-lab/styleless/search-input`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Wraps `@nebula-lab/styleless`'s `SearchInput` (the `type="search"` preset) and adds a leading search icon — like `PasswordField`, a `react-ui`- layer-only visual addition (no `@nebula-lab/headless` counterpart), since a native `type="search"` input already gives the platform's own clear- button/Escape-to-clear… | `forms` |
| 156 | `SegmentedControl` | extends `Omit<HeadlessToggleGroupSingleProps, 'type'>` | A single-select visual variant of `ToggleGroup` — same headless behavior (`@nebula-lab/headless`'s `ToggleGroup` with `type="single"` fixed, since a segmented control by definition allows exactly one active segment), just a continuous filled-track look instead of `ToggleGroup`'s individually bordered, gapped buttons. | `forms` |
| 157 | `SegmentedControlItem` | extends `HeadlessToggleGroupItemProps` (from `@nebula-lab/headless/toggle-group`) | A single segment — the active one fills solid with `--color-primary` instead of `Toggle`'s subtler `--toggle-on-bg` tint, reading as a track-and-thumb control rather than a row of individually-pressed buttons. | `forms` |
| 158 | `Select` | — | (no doc comment found) | `forms` |
| 159 | `SelectTrigger` | extends `HeadlessSelectTriggerProps` (from `@nebula-lab/headless/select`) | Styled like `Input` (same border/bg/text triple) with a built-in chevron that rotates on `data-state=open` — reads as a form field, since a `Select` trigger fills the same visual role an `<input>`/native `<select>` would. | `forms` |
| 160 | `SelectValue` | — | (no doc comment found) | `forms` |
| 161 | `SelectPortal` | — | (no doc comment found) | `forms` |
| 162 | `SelectContent` | extends `HeadlessSelectContentProps` (from `@nebula-lab/headless/select`) | The popup listbox panel — same `base-100`/`base-300`/`base-content` surface triple as `PopoverContent`/`DialogContent`. | `forms` |
| 163 | `SelectItem` | extends `HeadlessSelectItemProps` (from `@nebula-lab/headless/select`) | A built-in checkmark appears at `data-state=selected` — reads real DOM focus (`FocusItem`'s roving tabindex), not a virtual highlight the way `Command`'s items do, so this styles off `:focus-visible` rather than a `data-highlighted` attribute. | `forms` |
| 164 | `Slider` | — | (no doc comment found) | `forms` |
| 165 | `SliderTrack` | extends `HeadlessSliderTrackProps` (from `@nebula-lab/headless/slider`) | `data-orientation` (set by the headless source) switches between a full-width horizontal bar and a full-height vertical one. | `forms` |
| 166 | `SliderRange` | extends `HeadlessSliderRangeProps` (from `@nebula-lab/headless/slider`) | Positioned off the `--nebula-slider-range-start`/`-end` inline custom properties the headless source sets — spans between the low/high thumb (or from the track's start to the single thumb, for a plain slider). | `forms` |
| 167 | `SliderThumb` | extends `HeadlessSliderThumbProps` (from `@nebula-lab/headless/slider`) | Positioned off the `--nebula-slider-thumb-percent` inline custom property the headless source sets, centered on the track's cross-axis. | `forms` |
| 168 | `Switch` | extends `HeadlessSwitchProps` (from `@nebula-lab/headless/switch`) | Styled wrapper around `@nebula-lab/headless`'s `Switch` — on/off behavior, `role="switch"`, and hidden-native-input form participation all come from there unchanged. | `forms` |
| 169 | `TelField` | extends `StylelessTelInputProps` (from `@nebula-lab/styleless/tel-input`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Styled `type="tel"` `Input` preset — wraps `@nebula-lab/styleless`'s `TelInput` and reuses `Input`'s exact class recipe (`inputVariants`). | `forms` |
| 170 | `TextArea` | — | Styled `textarea` — wraps `@nebula-lab/styleless`'s `Textarea` (itself a thin pass-through of `@nebula-lab/primitives`' `Textarea`, which gives `invalid` -> `aria-invalid` wiring, ref forwarding, and the optional `autoResize` behavior) the same way this package's `Input` wraps `styleless`'s `Input`; there's no… | `forms` |
| 171 | `TimePicker` | extends `Omit<InputProps, 'type'>` | An `Input` preset to `type="time"` — same "`react-ui`-layer-only convenience, no `@nebula-lab/headless` counterpart" reasoning `SearchField`/ `PasswordField` document: a native `type="time"` input already gives a fully keyboard-operable, locale-aware (12h/24h per the user's own OS setting) time-entry UI for free,… | `forms` |
| 172 | `Toggle` | extends `HeadlessToggleProps` (from `@nebula-lab/headless/toggle`) | `data-state="on"\|"off"` (set by the headless source) fills the button when pressed — same `base-200`/`primary`-adjacent treatment other selectable-row components in this package use. | `forms` |
| 173 | `ToggleGroup` | extends `HeadlessToggleGroupProps` (from `@nebula-lab/headless/toggle-group`) | `data-orientation` (set by the headless source) switches the row between horizontal and vertical layout. | `forms` |
| 174 | `ToggleGroupItem` | extends `HeadlessToggleGroupItemProps` (from `@nebula-lab/headless/toggle-group`) | Same `data-state="on"\|"off"` fill treatment as this package's own `Toggle` — a `ToggleGroupItem` is a `Toggle` underneath (see the headless source), just with its pressed state driven by the group instead of local state. | `forms` |
| 175 | `UrlField` | extends `StylelessUrlInputProps` (from `@nebula-lab/styleless/url-input`)<br>CVA variant props from `inputVariants` (see this file's own `inputVariants` recipe for the full option list). | Styled `type="url"` `Input` preset — wraps `@nebula-lab/styleless`'s `UrlInput` and reuses `Input`'s exact class recipe (`inputVariants`). | `forms` |
| 176 | `AspectRatio` | — | Styled `AspectRatio` — thin re-export of `@nebula-lab/primitives`' `AspectRatio`, constraining content to a fixed width/height ratio regardless of its own intrinsic size (video/image/chart containers). | `layout` |
| 177 | `Box` | — | Styled `Box` — thin re-export of `@nebula-lab/primitives`' `Box`, the plainest possible polymorphic building block (a `Primitive` by another name, zero injected classes or structural opinion). | `layout` |
| 178 | `Center` | — | Styled `Center` — thin re-export of `@nebula-lab/primitives`' `Flex` with both axes centered, for centering a single child (a spinner, an empty-state illustration) both horizontally and vertically. | `layout` |
| 179 | `Container` | — | Styled `Container` — thin re-export of `@nebula-lab/primitives`' `Container` (horizontally centers content, caps width at `size`, adds responsive horizontal padding). | `layout` |
| 180 | `Flex` | — | Styled `Flex` — a thin re-export of `@nebula-lab/primitives`' unstyled `Flex` (`direction`/`align`/`justify`/`wrap`/`gap` convenience props instead of hand-written Tailwind flex classes). | `layout` |
| 181 | `Footer` | Inherits all standard `'footer'` attributes. | A page/app-level `<footer>` — same purely-presentational treatment as `Header`, just `border-t` instead of `border-b`. | `layout` |
| 182 | `Grid` | — | Styled `Grid` — thin re-export of `@nebula-lab/primitives`' `Grid`. | `layout` |
| 183 | `Header` | Inherits all standard `'header'` attributes. | A page/app-level `<header>` — purely presentational structure, no behavior, so (like `Card`) there's no matching `@nebula-lab/headless` compound underneath this one; a thin `cn()` wrapper around `Primitive` directly. | `layout` |
| 184 | `HStack` | — | Styled `HStack` — thin re-export of `@nebula-lab/primitives`' `Flex` pinned to a single-line horizontal row (no wrap — overflow scrolls/clips rather than wrapping; reach for `Inline`/`Wrap` when it should wrap). | `layout` |
| 185 | `Inline` | — | Styled `Inline` — thin re-export of `@nebula-lab/primitives`' `Flex` pinned to a horizontal row that wraps (tag lists, button groups, breadcrumbs) instead of overflowing. | `layout` |
| 186 | `Main` | Inherits all standard `'main'` attributes. | The page's `<main>` landmark — `flex-1` so it fills remaining space alongside a `Sidebar`/`Header`/`Footer` in a flex/grid page shell. | `layout` |
| 187 | `Paper` | Inherits all standard `'div'` attributes.<br>CVA variant props from `paperVariants` (see this file's own `paperVariants` recipe for the full option list). | A bordered or elevated surface for arbitrary content — like `Card`, but without `Card`'s compound header/title/content/footer sub-parts: reach for `Paper` for a generic raised panel (a floating toolbar, a custom layout that doesn't fit `Card`'s title+body shape), and `Card` for anything that actually wants that… | `layout` |
| 188 | `Section` | Inherits all standard `'section'` attributes. | A `<section>` landmark for one distinct region of page content — vertical spacing between its own children only (`space-y-4`), no background/border of its own, same "neutral content region" treatment `Main` uses. | `layout` |
| 189 | `Separator` | `orientation?: 'horizontal' \| 'vertical'`<br>`decorative?: boolean` — Purely visual (`role="none"`, no `aria-orientation`) vs. a semantic boundary announced to assistive tech (`role="separator"`). Most…<br>Inherits all standard `'div'` attributes. | A thin rule, horizontal or vertical, styled off `--separator-bg` (see `../tokens/component.ts`), which currently points at `--color-base-300` — the same token flagged as a known WCAG 1.4.11 failure in `CONTRAST_AUDIT.md`. | `layout` |
| 190 | `Spacer` | — | Styled `Spacer` — thin re-export of `@nebula-lab/primitives`' `Spacer`, an invisible `flex-grow`ing filler for pushing flex children to opposite ends without hand-writing `justify="between"`. | `layout` |
| 191 | `Stack` | — | Styled `Stack` — thin re-export of `@nebula-lab/primitives`' `Flex` pinned to `direction="column"`. | `layout` |
| 192 | `Surface` | Inherits all standard `'div'` attributes. | The plainest possible themed background — just `--surface-bg`/`-text`, no border/shadow/radius at all (unlike `Paper`/`Card`). | `layout` |
| 193 | `VStack` | — | Styled `VStack` — alias of `Stack` under a name consumers coming from other component libraries reach for first. | `layout` |
| 194 | `Wrap` | — | Styled `Wrap` — alias of `Inline` under a name consumers coming from other component libraries reach for first. | `layout` |
| 195 | `Audio` | Inherits all standard `'audio'` attributes. | A themed `<audio>` — same "just adds rounding, native controls stay in charge" treatment as `Video`. | `media` |
| 196 | `Image` | — | Styled `Image` — wraps `@nebula-lab/primitives`' unstyled `Image` and adds `display: block` (an `<img>` is inline by default, which leaves a few px of baseline gap under it inside a flex/block container — a common, easy-to-miss layout bug) plus a `max-w-full` responsive default. | `media` |
| 197 | `ImagePreview` | extends `StylelessImagePreviewProps` (from `@nebula-lab/styleless/image-preview`) | Styled `ImagePreview` — wraps `@nebula-lab/styleless`'s `ImagePreview` (which owns the real behavior: turning a selected `File` into a renderable image via `URL.createObjectURL`, revoked on unmount/change) with the same thumbnail treatment `ImageUpload`'s own grid already uses, for a bare single-image preview outside… | `media` |
| 198 | `Logo` | `size?: number` — Pixel size for both width and height — the mark's viewBox is square. Ignored if `width`/`height` are passed explicitly.<br>extends `React.SVGProps<SVGSVGElement>` | The nebula wordmark's icon — an original four-point sparkle/star mark (not a literal "N" letterform), picked to evoke the project's own name rather than trace a specific external reference. | `media` |
| 199 | `Video` | Inherits all standard `'video'` attributes. | A themed `<video>` — purely presentational (rounded, clipped corners), no custom player chrome of its own; native browser `controls` are shown by default (pass `controls={false}` to drive playback entirely yourself). | `media` |
| 200 | `BottomNav` | Inherits all standard `'nav'` attributes. | A fixed, mobile-only tab bar — `<nav>` landmark pinned to the viewport bottom, hidden at `md:` and above (this is a phone-width navigation pattern; wider viewports should use `Sidebar`/`Navbar` instead). | `navigation` |
| 201 | `BottomNavItem` | `icon: React.ReactNode` — Icon rendered above the label — treated as decorative (`aria-hidden`), same convention `CardHeader`'s `icon` prop uses; the accessible name…<br>`label: string` — Visible label rendered below the icon, and also this item's accessible name.<br>`active?: boolean` — Marks this item as the current page — sets `aria-current="page"` (mirrors `PaginationLink`'s convention) and `data-state="active"` for…<br>Inherits all standard `'a'` attributes. | One tab-bar destination — renders as an `<a>` by default; `asChild` it onto a router `Link` the same way `Button`/`PaginationLink` support wrapping a real anchor, e.g. `<BottomNavItem asChild><Link to="/">...`. | `navigation` |
| 202 | `Breadcrumb` | — | (no doc comment found) | `navigation` |
| 203 | `BreadcrumbList` | extends `HeadlessBreadcrumbListProps` (from `@nebula-lab/headless/breadcrumb`) | (no doc comment found) | `navigation` |
| 204 | `BreadcrumbItem` | extends `HeadlessBreadcrumbItemProps` (from `@nebula-lab/headless/breadcrumb`) | (no doc comment found) | `navigation` |
| 205 | `BreadcrumbLink` | extends `HeadlessBreadcrumbLinkProps` (from `@nebula-lab/headless/breadcrumb`) | (no doc comment found) | `navigation` |
| 206 | `BreadcrumbPage` | extends `HeadlessBreadcrumbPageProps` (from `@nebula-lab/headless/breadcrumb`) | (no doc comment found) | `navigation` |
| 207 | `BreadcrumbSeparator` | extends `HeadlessBreadcrumbSeparatorProps` (from `@nebula-lab/headless/breadcrumb`) | Defaults to a chevron icon when no `children` is given — a consumer can still pass e.g. `"/"` or their own icon instead. | `navigation` |
| 208 | `Navbar` | Inherits all standard `'nav'` attributes. | A horizontal `<nav>` row — distinct from `Header` in tag (`nav` vs. `header`) and intent: `Header` is the page/app-level landmark a `Navbar` often lives inside of, alongside a logo or page title, rather than being the same element under two names. | `navigation` |
| 209 | `NavigationMenu` | extends `HeadlessNavigationMenuProps` (from `@nebula-lab/headless/navigation-menu`) | Root `<nav>` — relative positioning so a consumer's own `NavigationMenuContent` can be absolutely placed against it if not portalled. | `navigation` |
| 210 | `NavigationMenuList` | extends `HeadlessNavigationMenuListProps` (from `@nebula-lab/headless/navigation-menu`) | Lays top-level items out in a row — mirrors `TabList`'s row layout. | `navigation` |
| 211 | `NavigationMenuItem` | — | (no doc comment found) | `navigation` |
| 212 | `NavigationMenuTrigger` | extends `HeadlessNavigationMenuTriggerProps` (from `@nebula-lab/headless/navigation-menu`) | Built-in chevron rotates 180° when `data-state="open"` — same `data-[state=open]:rotate-180` treatment `AccordionTrigger` uses. | `navigation` |
| 213 | `NavigationMenuPortal` | — | (no doc comment found) | `navigation` |
| 214 | `NavigationMenuContent` | extends `HeadlessNavigationMenuContentProps` (from `@nebula-lab/headless/navigation-menu`) | The mega-menu panel — same `base-100`/`base-300`/`base-content` surface triple as `PopoverContent`/`DialogContent` (see `../tokens/component.ts`'s header comment for why), including the same fade/scale `data-[state]` transition (see `PopoverContent`'s doc comment for why). | `navigation` |
| 215 | `NavigationMenuLink` | extends `HeadlessNavigationMenuLinkProps` (from `@nebula-lab/headless/navigation-menu`) | A plain top-level link — `data-[active]` (set when `active` is passed) recolors the text via `--navigation-menu-link-active-text`, same convention `PaginationLink`'s active fill uses but text-only since a link isn't a button-shaped hit target. | `navigation` |
| 216 | `Pagination` | — | (no doc comment found) | `navigation` |
| 217 | `ELLIPSIS` | — | (no doc comment found) | `navigation` |
| 218 | `PaginationList` | extends `HeadlessPaginationListProps` (from `@nebula-lab/headless/pagination`) | A simple flex row with gap between page items — `PaginationItem` itself has no chrome, `PaginationLink`/`Previous`/`Next` carry the visual treatment. | `navigation` |
| 219 | `PaginationItem` | — | (no doc comment found) | `navigation` |
| 220 | `PaginationLink` | extends `HeadlessPaginationLinkProps` (from `@nebula-lab/headless/pagination`) | One page number — filled with `--pagination-link-active-bg`/`-active-text` on `data-state="active"` (set by the underlying `@nebula-lab/headless` link), a subtle hover background otherwise (see `../tokens/component.ts`). | `navigation` |
| 221 | `PaginationPrevious` | extends `HeadlessPaginationPreviousProps` (from `@nebula-lab/headless/pagination`) | Same visual treatment as `PaginationLink`, minus the active-page fill (it's never itself "the current page"). | `navigation` |
| 222 | `PaginationNext` | extends `HeadlessPaginationNextProps` (from `@nebula-lab/headless/pagination`) | Same visual treatment as `PaginationPrevious`. | `navigation` |
| 223 | `PaginationEllipsis` | extends `HeadlessPaginationEllipsisProps` (from `@nebula-lab/headless/pagination`) | Muted, non-interactive "…" marker between the same `h-9 min-w-9` box `PaginationLink` uses, so collapsed runs line up visually with real page links. | `navigation` |
| 224 | `SideNav` | Inherits all standard `'nav'` attributes. | The vertical nav landmark meant to render inside `Sidebar`'s children — `Sidebar` itself is a bare `<aside>` with no opinion on content (see its own doc comment), so `SideNav` is the `<nav>` landmark + vertical-stack layout that content actually needs. | `navigation` |
| 225 | `SideNavItem` | `icon: React.ReactNode` — Icon rendered left of the label — treated as decorative (`aria-hidden`), same convention `BottomNavItem`'s `icon` prop uses; the accessible…<br>`label: string` — Visible label rendered right of the icon, and also this item's accessible name.<br>`active?: boolean` — Marks this item as the current page — sets `aria-current="page"` and `data-state="active"` for styling, same convention `BottomNavItem`…<br>`collapsed?: boolean` — Shrinks to an icon-only square (matching `Sidebar`'s own `collapsed` width) — `label` still renders for the accessible name, visually…<br>Inherits all standard `'a'` attributes. | One sidebar nav destination — renders as an `<a>` by default; `asChild` it onto a router `Link` the same way `BottomNavItem`/`Button` support wrapping a real anchor, e.g. `<SideNavItem asChild><Link to="/">...`. | `navigation` |
| 226 | `SideNavGroup` | `label: string` — The section label rendered above `children`, e.g. `"Settings"`. Always required — an unlabeled visual grouping isn't distinguishable from a…<br>`children: React.ReactNode`<br>`collapsed?: boolean` — Visually hides `label` (`sr-only`, not unmounted — a screen reader still announces the section) to match a collapsed `Sidebar`'s icon-only…<br>`className?: string` | A labeled section inside `SideNav` — e.g. grouping "Reports"/"Analytics" under a "Data" heading, the same "section label above related items" pattern many real sidebar nav bars use. | `navigation` |
| 227 | `Sidebar` | `side?: 'left' \| 'right'` — Which edge the border (and, by convention, this sidebar) sits on.<br>`collapsed?: boolean` — Shrinks the rail from `w-64` (256px) to `w-16` (64px) — an icon-only collapsed state, the same pattern `BottomNavItem`'s icon/label pairing…<br>Inherits all standard `'aside'` attributes. | A fixed-width `<aside>` for app navigation/filters — `side` only flips which edge gets the border (`border-r` for a left sidebar, `border-l` for a right one); actual page placement is the consumer's own layout (flex/ grid) concern, same as every other component here not hardcoding position. | `navigation` |
| 228 | `Stepper` | — | (no doc comment found) | `navigation` |
| 229 | `StepperList` | extends `HeadlessStepperListProps` (from `@nebula-lab/headless/stepper`) | Lays steps out in a row (or column, via `data-[orientation=vertical]`) with even gaps. | `navigation` |
| 230 | `StepperItem` | extends `HeadlessStepperItemProps` (from `@nebula-lab/headless/stepper`) | A single step's layout slot — `flex-1` on the horizontal axis so items share the row evenly (matches `TabList`'s trigger-sizing approach), full width on the vertical axis instead. | `navigation` |
| 231 | `StepperTrigger` | extends `HeadlessStepperTriggerProps` (from `@nebula-lab/headless/stepper`) | Wraps `StepperIndicator`/`StepperTitle` — disabled steps fade rather than fully hide, so the sequence stays visible. | `navigation` |
| 232 | `StepperIndicator` | extends `HeadlessStepperIndicatorProps` (from `@nebula-lab/headless/stepper`) | A small circle badge — `upcoming` reads `--stepper-indicator-bg`/`-text`, `current` and `complete` each swap to their own pair (see `../tokens/component.ts`). | `navigation` |
| 233 | `StepperTitle` | extends `HeadlessStepperTitleProps` (from `@nebula-lab/headless/stepper`) | The `current` step's title is bolded so the active step reads clearly at a glance. | `navigation` |
| 234 | `StepperDescription` | extends `HeadlessStepperDescriptionProps` (from `@nebula-lab/headless/stepper`) | Muted secondary line under `StepperTitle` — opacity modifier applied directly on the class, not baked into a token (see `component.ts`'s file header). | `navigation` |
| 235 | `StepperSeparator` | extends `HeadlessStepperSeparatorProps` (from `@nebula-lab/headless/stepper`) | The connecting line between two steps — a 1px rule on the axis perpendicular to reading order, sized to fill the remaining row/column space. | `navigation` |
| 236 | `Tabs` | — | (no doc comment found) | `navigation` |
| 237 | `TabList` | extends `HeadlessTabListProps` (from `@nebula-lab/headless/tabs`) | Styled wrapper around `@nebula-lab/headless`'s `TabList` — arrow-key roving navigation (`role="tablist"`) comes from there unchanged. | `navigation` |
| 238 | `Tab` | extends `HeadlessTabProps` (from `@nebula-lab/headless/tabs`) | Styled wrapper around `@nebula-lab/headless`'s `Tab` — selection, `role="tab"`, and automatic/manual activation-mode handling all come from there unchanged. | `navigation` |
| 239 | `TabPanel` | extends `HeadlessTabPanelProps` (from `@nebula-lab/headless/tabs`) | Styled wrapper around `@nebula-lab/headless`'s `TabPanel` — mount/unmount (`forceMount`) and `role="tabpanel"` wiring come from there unchanged. | `navigation` |
| 240 | `AlertDialog` | — | (no doc comment found) | `overlays/dialog` |
| 241 | `AlertDialogTrigger` | — | (no doc comment found) | `overlays/dialog` |
| 242 | `AlertDialogPortal` | — | (no doc comment found) | `overlays/dialog` |
| 243 | `AlertDialogOverlay` | `backdrop?: 'solid' \| 'blur'` — `'solid'` (the default) is a flat `--dialog-overlay-bg` tint at 50% opacity — unchanged from before this prop existed. `'blur'` is a real…<br>`blurIntensity?: BackdropBlurIntensity` — Same `blurIntensity` contract as `Backdrop`'s own prop — ignored when `backdrop="solid"`.<br>extends `HeadlessDialogOverlayProps` (from `@nebula-lab/headless/dialog`) | `'solid'` (the default) is a flat `--dialog-overlay-bg` tint at 50% opacity — unchanged from before this prop existed. | `overlays/dialog` |
| 244 | `AlertDialogContent` | extends `HeadlessAlertDialogContentProps` (from `@nebula-lab/headless/alert-dialog`) | Same fixed, centered card treatment as `DialogContent` (reads the same `--dialog-content-*` tokens — an alert dialog is the same visual surface, just with a different dismissal contract), but deliberately has **no** built-in icon close button: an `AlertDialogContent` is never dismissed by an incidental click, only by… | `overlays/dialog` |
| 245 | `AlertDialogTitle` | extends `HeadlessDialogTitleProps` (from `@nebula-lab/headless/dialog`) | (no doc comment found) | `overlays/dialog` |
| 246 | `AlertDialogDescription` | extends `HeadlessDialogDescriptionProps` (from `@nebula-lab/headless/dialog`) | (no doc comment found) | `overlays/dialog` |
| 247 | `AlertDialogCancel` | extends `HeadlessAlertDialogCancelProps` (from `@nebula-lab/headless/alert-dialog`) | Plain, unfilled treatment — sits beside `AlertDialogAction`'s filled `danger` button without competing with it visually. | `overlays/dialog` |
| 248 | `AlertDialogAction` | extends `HeadlessAlertDialogActionProps` (from `@nebula-lab/headless/alert-dialog`) | The confirming button (e.g. "Delete") — same close-on-click contract as `AlertDialogCancel` (both are `DialogClose` underneath, see the headless source), but styled to look like a filled `danger`-toned `Button` rather than `AlertDialogCancel`'s plain treatment, since this is the one that commits the… | `overlays/dialog` |
| 249 | `Dialog` | — | (no doc comment found) | `overlays/dialog` |
| 250 | `DialogTrigger` | — | (no doc comment found) | `overlays/dialog` |
| 251 | `DialogPortal` | — | (no doc comment found) | `overlays/dialog` |
| 252 | `DialogOverlay` | `backdrop?: 'solid' \| 'blur'` — `'solid'` (the default) is a flat `--dialog-overlay-bg` tint at 50% opacity — unchanged from before this prop existed. `'blur'` is a real…<br>`blurIntensity?: BackdropBlurIntensity` — Same `blurIntensity` contract as `Backdrop`'s own prop — ignored when `backdrop="solid"`.<br>extends `HeadlessDialogOverlayProps` (from `@nebula-lab/headless/dialog`) | The dimmed backdrop — styled off `--dialog-overlay-bg` at low opacity (see `../tokens/component.ts`). | `overlays/dialog` |
| 253 | `DialogContent` | `hideCloseButton?: boolean` — Hide the built-in top-right close icon (e.g. for a dialog that only closes via an explicit in-body action).<br>extends `HeadlessDialogContentProps` (from `@nebula-lab/headless/dialog`) | Fixed, centered card — styled off `--dialog-content-bg`/`-border`/`-text` (see `../tokens/component.ts`). | `overlays/dialog` |
| 254 | `DialogTitle` | extends `HeadlessDialogTitleProps` (from `@nebula-lab/headless/dialog`) | (no doc comment found) | `overlays/dialog` |
| 255 | `DialogDescription` | extends `HeadlessDialogDescriptionProps` (from `@nebula-lab/headless/dialog`) | (no doc comment found) | `overlays/dialog` |
| 256 | `DialogClose` | extends `HeadlessDialogCloseProps` (from `@nebula-lab/headless/dialog`) | Unopinionated beyond a focus ring — used both as `DialogContent`'s built-in icon close button (see `dialog-content.tsx`) and as a plain text button a consumer drops into the body/footer (e.g. `<DialogClose>Cancel</DialogClose>`), which look nothing alike, so this doesn't bake in a specific appearance. | `overlays/dialog` |
| 257 | `Drawer` | — | (no doc comment found) | `overlays/dialog` |
| 258 | `DrawerTrigger` | — | (no doc comment found) | `overlays/dialog` |
| 259 | `DrawerPortal` | — | (no doc comment found) | `overlays/dialog` |
| 260 | `DrawerOverlay` | `backdrop?: 'solid' \| 'blur'` — Same `backdrop` contract as `DialogOverlay`'s own doc — `'solid'` (default) is the flat 50%-opacity tint, `'blur'` is a real frosted-glass…<br>`blurIntensity?: BackdropBlurIntensity` — Same `blurIntensity` contract as `Backdrop`'s own prop — ignored when `backdrop="solid"`.<br>extends `HeadlessDrawerOverlayProps` (from `@nebula-lab/headless/drawer`) | The dimmed backdrop — styled off `--drawer-overlay-bg` (see `../tokens/component.ts`), kept as its own token rather than reusing `--dialog-overlay-bg` even though both currently point at the same semantic color, so restyling one doesn't silently affect the other. | `overlays/dialog` |
| 261 | `DrawerContent` | `hideCloseButton?: boolean` — Hide the built-in top-right close icon.<br>extends `HeadlessDrawerContentProps` (from `@nebula-lab/headless/drawer`) | The sliding panel — fixed to whichever edge `side` (default `'right'`) names, sized to a fixed cross-axis dimension and full-bleed on the other, with a `data-[state=closed]` translate-out transform per side (this is the "consumer's own CSS" work the headless `DrawerContent`'s doc comment calls out as unowned by that… | `overlays/dialog` |
| 262 | `DrawerTitle` | extends `HeadlessDrawerTitleProps` (from `@nebula-lab/headless/drawer`) | (no doc comment found) | `overlays/dialog` |
| 263 | `DrawerDescription` | extends `HeadlessDrawerDescriptionProps` (from `@nebula-lab/headless/drawer`) | (no doc comment found) | `overlays/dialog` |
| 264 | `DrawerClose` | extends `HeadlessDrawerCloseProps` (from `@nebula-lab/headless/drawer`) | (no doc comment found) | `overlays/dialog` |
| 265 | `Sheet` | — | (no doc comment found) | `overlays/dialog` |
| 266 | `SheetTrigger` | — | (no doc comment found) | `overlays/dialog` |
| 267 | `SheetPortal` | — | (no doc comment found) | `overlays/dialog` |
| 268 | `SheetOverlay` | — | (no doc comment found) | `overlays/dialog` |
| 269 | `SheetContent` | — | (no doc comment found) | `overlays/dialog` |
| 270 | `SheetTitle` | — | (no doc comment found) | `overlays/dialog` |
| 271 | `SheetDescription` | — | (no doc comment found) | `overlays/dialog` |
| 272 | `SheetClose` | — | (no doc comment found) | `overlays/dialog` |
| 273 | `ContextMenu` | — | (no doc comment found) | `overlays/menu` |
| 274 | `ContextMenuTrigger` | — | (no doc comment found) | `overlays/menu` |
| 275 | `ContextMenuPortal` | — | (no doc comment found) | `overlays/menu` |
| 276 | `ContextMenuContent` | extends `HeadlessMenuContentProps` (from `@nebula-lab/headless/menu`) | Same `base-100`/`base-300`/`base-content` surface treatment as `PopoverContent`/`SelectContent` — including the same `--motion-duration-fast`/`--elevation-anchored` fade/scale transition (see `PopoverContent`'s doc comment for why). | `overlays/menu` |
| 277 | `ContextMenuItem` | extends `HeadlessMenuItemProps` (from `@nebula-lab/headless/menu`) | Highlights on real `:focus` (plain, not `:focus-visible`) — a `MenuItem` genuinely moves DOM focus for both keyboard nav and pointer-hover (see the headless source's `onPointerMove` handler), unlike `Command`/`Combobox`'s items, which only ever get a *virtual* highlight while real focus stays on their input. | `overlays/menu` |
| 278 | `ContextMenuCheckboxItem` | extends `HeadlessMenuCheckboxItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuItem`, plus a built-in checkmark shown at `data-state=checked` in a reserved left-hand slot. | `overlays/menu` |
| 279 | `ContextMenuRadioGroup` | — | (no doc comment found) | `overlays/menu` |
| 280 | `ContextMenuRadioItem` | extends `HeadlessMenuRadioItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuCheckboxItem`, but a dot instead of a checkmark — the visual `RadioGroupItem` uses for the same "one selected of many" relationship. | `overlays/menu` |
| 281 | `ContextMenuSeparator` | extends `HeadlessMenuSeparatorProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays/menu` |
| 282 | `ContextMenuLabel` | extends `HeadlessMenuLabelProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays/menu` |
| 283 | `DropdownMenu` | — | (no doc comment found) | `overlays/menu` |
| 284 | `DropdownMenuTrigger` | — | (no doc comment found) | `overlays/menu` |
| 285 | `DropdownMenuPortal` | — | (no doc comment found) | `overlays/menu` |
| 286 | `DropdownMenuContent` | extends `HeadlessMenuContentProps` (from `@nebula-lab/headless/menu`) | Same `base-100`/`base-300`/`base-content` surface treatment as `PopoverContent`/`SelectContent` — including the same `--motion-duration-fast`/`--elevation-anchored` fade/scale transition (see `PopoverContent`'s doc comment for why). | `overlays/menu` |
| 287 | `DropdownMenuItem` | extends `HeadlessMenuItemProps` (from `@nebula-lab/headless/menu`) | Highlights on real `:focus` (plain, not `:focus-visible`) — a `MenuItem` genuinely moves DOM focus for both keyboard nav and pointer-hover (see the headless source's `onPointerMove` handler), unlike `Command`/`Combobox`'s items, which only ever get a *virtual* highlight while real focus stays on their input. | `overlays/menu` |
| 288 | `DropdownMenuCheckboxItem` | extends `HeadlessMenuCheckboxItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuItem`, plus a built-in checkmark shown at `data-state=checked` in a reserved left-hand slot. | `overlays/menu` |
| 289 | `DropdownMenuRadioGroup` | — | (no doc comment found) | `overlays/menu` |
| 290 | `DropdownMenuRadioItem` | extends `HeadlessMenuRadioItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuCheckboxItem`, but a dot instead of a checkmark — the visual `RadioGroupItem` uses for the same "one selected of many" relationship. | `overlays/menu` |
| 291 | `DropdownMenuSeparator` | extends `HeadlessMenuSeparatorProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays/menu` |
| 292 | `DropdownMenuLabel` | extends `HeadlessMenuLabelProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays/menu` |
| 293 | `Menu` | — | (no doc comment found) | `overlays/menu` |
| 294 | `MenuTrigger` | — | (no doc comment found) | `overlays/menu` |
| 295 | `MenuPortal` | — | (no doc comment found) | `overlays/menu` |
| 296 | `MenuContent` | extends `HeadlessMenuContentProps` (from `@nebula-lab/headless/menu`) | Same `base-100`/`base-300`/`base-content` surface treatment as `PopoverContent`/`SelectContent` — including the same `--motion-duration-fast`/`--elevation-anchored` fade/scale transition (see `PopoverContent`'s doc comment for why). | `overlays/menu` |
| 297 | `MenuItem` | extends `HeadlessMenuItemProps` (from `@nebula-lab/headless/menu`) | Highlights on real `:focus` (plain, not `:focus-visible`) — a `MenuItem` genuinely moves DOM focus for both keyboard nav and pointer-hover (see the headless source's `onPointerMove` handler), unlike `Command`/`Combobox`'s items, which only ever get a *virtual* highlight while real focus stays on their input. | `overlays/menu` |
| 298 | `MenuCheckboxItem` | extends `HeadlessMenuCheckboxItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuItem`, plus a built-in checkmark shown at `data-state=checked` in a reserved left-hand slot. | `overlays/menu` |
| 299 | `MenuRadioGroup` | — | (no doc comment found) | `overlays/menu` |
| 300 | `MenuRadioItem` | extends `HeadlessMenuRadioItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuCheckboxItem`, but a dot instead of a checkmark — the visual `RadioGroupItem` uses for the same "one selected of many" relationship. | `overlays/menu` |
| 301 | `MenuSeparator` | extends `HeadlessMenuSeparatorProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays/menu` |
| 302 | `MenuLabel` | extends `HeadlessMenuLabelProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays/menu` |
| 303 | `Backdrop` | `variant?: 'solid' \| 'blur'` — `'solid'` (the default) is a flat tint at 50% opacity — the classic modal-barrier scrim. `'blur'` is a real frosted-glass material (iOS…<br>`blurIntensity?: BackdropBlurIntensity` — How strong the frosted-glass effect reads — ignored when `variant="solid"`. `'subtle'` (12px blur) suits a lightweight surface where the…<br>extends `OverlayProps` (from `@nebula-lab/primitives/overlay`) | The styled counterpart of `@nebula-lab/primitives`' bare `Overlay` (`fixed inset-0`, zero color/blur of its own by design) — a standalone, theme-aware backdrop usable anywhere a full-viewport scrim is needed, not just behind `Dialog`/`Drawer`/`AlertPopup`. | `overlays` |
| 304 | `Command` | extends `HeadlessCommandProps` (from `@nebula-lab/headless/command`) | The palette's outer card — same `base-100`/`base-300`/`base-content` surface treatment as `DialogContent`/`PopoverContent`, since a command palette is usually rendered inside a consumer-supplied `Dialog`. | `overlays` |
| 305 | `CommandInput` | extends `HeadlessCommandInputProps` (from `@nebula-lab/headless/command`) | Sits flush across the top of the palette with a bottom rule instead of `Input`'s all-around border — visually reads as "one search field," not a boxed form control inside a box. | `overlays` |
| 306 | `CommandList` | extends `HeadlessCommandListProps` (from `@nebula-lab/headless/command`) | Scrollable results area — capped height so a long result set scrolls instead of growing the palette off-screen. | `overlays` |
| 307 | `CommandItem` | extends `HeadlessCommandItemProps` (from `@nebula-lab/headless/command`) | `data-highlighted` (not real focus — see the headless source) drives the fill via `--command-item-highlighted-bg`, same "state attribute, not local state" pattern every other data-driven row in this package uses. | `overlays` |
| 308 | `CommandGroup` | extends `HeadlessCommandGroupProps` (from `@nebula-lab/headless/command`) | `heading` renders as small muted caps text above the group's items — the group `<div>` itself has no visible chrome beyond spacing. | `overlays` |
| 309 | `CommandEmpty` | extends `HeadlessCommandEmptyProps` (from `@nebula-lab/headless/command`) | (no doc comment found) | `overlays` |
| 310 | `CommandSeparator` | extends `HeadlessCommandSeparatorProps` (from `@nebula-lab/headless/command`) | (no doc comment found) | `overlays` |
| 311 | `HoverCard` | — | (no doc comment found) | `overlays` |
| 312 | `HoverCardTrigger` | — | (no doc comment found) | `overlays` |
| 313 | `HoverCardPortal` | — | (no doc comment found) | `overlays` |
| 314 | `HoverCardContent` | extends `HeadlessHoverCardContentProps` (from `@nebula-lab/headless/hover-card`) | Anchor-positioned preview card — same `base-100`/`base-300`/`base-content` surface treatment as `PopoverContent`, since a hover card is "more page surface," not a high-contrast callout like `Tooltip`. | `overlays` |
| 315 | `Menubar` | extends `HeadlessMenubarProps` (from `@nebula-lab/headless/menubar`) | Unlike this package's own `Menu` (whose root renders no visible DOM of its own), `Menubar`'s root *is* the horizontal bar itself — it renders `RovingFocusGroup` directly rather than through a portal, so it's the one root in this family that needs real layout styling. | `overlays` |
| 316 | `MenubarMenu` | — | (no doc comment found) | `overlays` |
| 317 | `MenubarTrigger` | extends `HeadlessMenubarTriggerProps` (from `@nebula-lab/headless/menubar`) | A visible bar button (unlike `MenuTrigger`, typically an already-styled `Button` passed `asChild`) — `Menubar`'s triggers ("File", "Edit", ...) are plain labels directly in the bar, so this needs its own chrome: padding, hover, and a `data-state=open` fill matching whichever menu is currently showing. | `overlays` |
| 318 | `MenubarPortal` | — | (no doc comment found) | `overlays` |
| 319 | `MenubarContent` | extends `HeadlessMenuContentProps` (from `@nebula-lab/headless/menu`) | Same `base-100`/`base-300`/`base-content` surface treatment as `PopoverContent`/`SelectContent` — including the same `--motion-duration-fast`/`--elevation-anchored` fade/scale transition (see `PopoverContent`'s doc comment for why). | `overlays` |
| 320 | `MenubarItem` | extends `HeadlessMenuItemProps` (from `@nebula-lab/headless/menu`) | Highlights on real `:focus` (plain, not `:focus-visible`) — a `MenuItem` genuinely moves DOM focus for both keyboard nav and pointer-hover (see the headless source's `onPointerMove` handler), unlike `Command`/`Combobox`'s items, which only ever get a *virtual* highlight while real focus stays on their input. | `overlays` |
| 321 | `MenubarCheckboxItem` | extends `HeadlessMenuCheckboxItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuItem`, plus a built-in checkmark shown at `data-state=checked` in a reserved left-hand slot. | `overlays` |
| 322 | `MenubarRadioGroup` | — | (no doc comment found) | `overlays` |
| 323 | `MenubarRadioItem` | extends `HeadlessMenuRadioItemProps` (from `@nebula-lab/headless/menu`) | Same row treatment as `MenuCheckboxItem`, but a dot instead of a checkmark — the visual `RadioGroupItem` uses for the same "one selected of many" relationship. | `overlays` |
| 324 | `MenubarSeparator` | extends `HeadlessMenuSeparatorProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays` |
| 325 | `MenubarLabel` | extends `HeadlessMenuLabelProps` (from `@nebula-lab/headless/menu`) | (no doc comment found) | `overlays` |
| 326 | `Popover` | — | (no doc comment found) | `overlays` |
| 327 | `PopoverTrigger` | — | (no doc comment found) | `overlays` |
| 328 | `PopoverPortal` | — | (no doc comment found) | `overlays` |
| 329 | `PopoverContent` | extends `HeadlessPopoverContentProps` (from `@nebula-lab/headless/popover`) | Anchor-positioned card — styled off `--popover-content-bg`/`-border`/ `-text` (see `../tokens/component.ts`), radius from `--radius-popover` (smaller than `Dialog`'s `--radius-dialog` by default in this theme's token values, though both currently share the same `2rem` — see `tokens/primitive.ts`). | `overlays` |
| 330 | `PopoverClose` | extends `HeadlessPopoverCloseProps` (from `@nebula-lab/headless/popover`) | Same unopinionated-beyond-a-focus-ring approach as `Dialog`'s `DialogClose` — see that file for why. | `overlays` |
| 331 | `Tooltip` | — | (no doc comment found) | `overlays` |
| 332 | `TooltipTrigger` | — | (no doc comment found) | `overlays` |
| 333 | `TooltipPortal` | — | (no doc comment found) | `overlays` |
| 334 | `TooltipContent` | extends `HeadlessTooltipContentProps` (from `@nebula-lab/headless/tooltip`) | Small, high-contrast label anchored to `TooltipTrigger` — styled off `--tooltip-content-bg`/`-text` (see `../tokens/component.ts`), which deliberately reads the `neutral` semantic role rather than the `base-100`/`base-content` surface pair `Popover`/`Dialog` use, so a tooltip visually pops off the page instead of… | `overlays` |
| 335 | `ThemeProvider` | `children: React.ReactNode`<br>`defaultTheme?: Theme`<br>`storageKey?: string` — localStorage key the resolved preference is persisted under. | Wraps the app, persists the user's light/dark/system preference, and reflects it on `<html>` two ways: a `data-theme` attribute (`"light"` / `"dark"` / `"system"` — the original mechanism) and a `.dark` class, toggled together, since `theme.css` now matches on *either* (see `tokens/generate.ts`) — `.dark` is there to… | `theming` |
| 336 | `ThemeSwitcher` | `size?: ButtonProps['size']`<br>`variant?: 'icon' \| 'dropdown'` — `'icon'` — a compact icon-only toggle group (sun/moon/monitor), one control per option. `'dropdown'` — a single icon button (its icon… | A three-way light/dark/system switcher — built purely from this package's own `IconButton`/`DropdownMenu` and `ThemeProvider`, no new primitives. | `theming` |
| 337 | `Blockquote` | Inherits all standard `'blockquote'` attributes. | A quoted passage — purely presentational, no `@nebula-lab/primitives`/ `@nebula-lab/headless` counterpart underneath (unlike `Text`/`Heading`/ `Code`, which wrap an existing unstyled primitive), same "thin `cn()` wrapper directly around `Primitive`" treatment this session's `Header`/ `Footer`/etc. use — there was… | `typography` |
| 338 | `Code` | — | Styled inline `Code` — the unstyled primitive carries no classes of its own (unstyled primitives own behavior, not visuals), so this layer is the sole source of the monospace/pill-background look, via `--code-bg`/`-text` (see `../tokens/component.ts`). | `typography` |
| 339 | `CodeBlock` | `className?: string`<br>extends `Omit<StylelessCodeBlockProps, 'classNames'>` | Wraps `@nebula-lab/styleless`'s `CodeBlock` (which owns the real behavior: `copied` state, the clipboard write, splitting `code` into lines) and supplies every part's Tailwind classes via its `classNames` prop — this file's whole job is visual, matching every other `react-ui` wrapper's division of labor with its… | `typography` |
| 340 | `Heading` | — | Styled `Heading` — wraps `@nebula-lab/primitives`' unstyled, polymorphic `Heading` (which already derives size/weight from `level`, independent of the rendered tag) and adds this theme's heading font stack (`--font-heading`, same variable `DialogTitle` already reads) plus the default body text color. | `typography` |
| 341 | `Kbd` | Inherits all standard `'kbd'` attributes. | A keyboard-shortcut label (e.g. "⌘" + "K") — same "no lower-layer counterpart" treatment `Blockquote` documents. | `typography` |
| 342 | `KbdShortcut` | `keys: React.ReactNode[]` — Each key in the sequence, rendered as its own `Kbd` pill — e.g. `['⌘', 'K']` or `['Ctrl', 'Shift', 'P']`.<br>`separator?: React.ReactNode` — Rendered between consecutive keys. Pass `null` for tight symbol-style sequences (e.g. `⌘K`) instead of `Ctrl` `+` `K`.<br>extends `Omit<PrimitivePropsWithRef<'span'>, 'children'>` | A `Kbd` sequence renderer — same "no lower-layer counterpart" treatment `Kbd` itself already documents (purely presentational, no ARIA pattern, no state). | `typography` |
| 343 | `Label` | — | Styled `Label` — wraps `@nebula-lab/primitives`' unstyled `Label` (which already gives the `required` asterisk + screen-reader announcement) and adds the same `text-sm font-medium text-[var(--field-label-text)]` treatment `FieldLabel` uses, for a standalone labeled control that isn't wrapped in `Field` (`FieldLabel`… | `typography` |
| 344 | `Link` | — | Styled `Link` — wraps `@nebula-lab/primitives`' unstyled `Link` (which already gives underline-on-hover, a focus-visible ring, and the `external` convenience prop) and adds this theme's primary color. | `typography` |
| 345 | `Markdown` | `children: string` — Raw markdown source — see this component's doc comment for exactly what subset is supported.<br>`className?: string` | Renders a **practical subset** of Markdown as themed `react-ui` components — not a full CommonMark implementation (see `markdown-utils.ts`'s header comment for why: no npm registry access in this sandbox to add a real parser, and hand-rolling one that's actually spec-correct isn't a responsible substitute). | `typography` |
| 346 | `Paragraph` | — | Styled `Paragraph` — the unstyled primitive carries no classes of its own (unstyled primitives own behavior, not visuals), so this layer is the sole source of the comfortable reading line-height — for body copy, as distinct from `Text` (no line-height opinion at any layer, meant for inline runs). | `typography` |
| 347 | `Pre` | — | Styled `Pre` — the unstyled primitive carries no classes of its own (unstyled primitives own behavior, not visuals), so this layer is the sole source of the monospace/padding/scroll/background look — the same `--code-block-*` tokens `CodeBlock` uses, so a bare `<Pre><Code>...>` block (no copy button, no line numbers)… | `typography` |
| 348 | `Text` | — | Styled `Text` — wraps `@nebula-lab/primitives`' unstyled, polymorphic `Text` (which already gives `as`/`truncate`) and only adds this theme's default body color (`--text-color`, see `../tokens/component.ts`) — no font-size or weight opinion here either, same as the primitive it wraps; reach for `Heading` for headings. | `typography` |

